# Stage278 原生 C++ 直连 41407 交易前置验证

- line_id：`futures_trend`
- 当前模式：day
- 记录时间：2026-05-20 13:55 CST
- 工作区/分支：`/Users/bytedance/Desktop/person/vnpy` / `master`
- 阶段性质：CTP/券商测试柜台链路归因；只读连接验证
- 是否重要突破：是
- 是否触发A/B：否

## 外部调研与判断

- 参考资料：SimNow Mac CP SDK `TraderapiMduserapi_6.7.7_MacOS_CP.zip`，本地已验证 MD5 `bbb85d8789008ee81094aca87b2c9715`，版本字符串 `v6.7.7_MacOS_CP_20240716 15:00:00`。
- 我的判断：券商建议“先试最简单登录，不要用第三方封装库”是正确排查路径。Python/vn.py 不是直接连接 CTP，而是通过 `vnpy_ctp` 扩展封装调用底层 C++ 动态库；因此原生 C++ 能通而 Python 封装不通时，应优先归因为封装/ABI/调用约定兼容问题，而不是券商端口、账号、AppID/AuthCode 必然有问题。

## 本次变更

- 新增脚本：
  - `examples/portfolio_backtesting/run_ctp_stage278_native_cpp_td_login_probe.cpp`
  - `examples/portfolio_backtesting/run_ctp_stage278_native_cpp_td_login_probe.sh`
- 修改脚本：无
- 删除脚本：无
- 新增参数：
  - `CTP_NATIVE_TD_WAIT_SECONDS`：原生 C++ TD-only 探针等待秒数
- 修改参数：无
- 删除参数：无

## 回测/归因参数

- 数据区间：不涉及行情回测
- 账户规模：Stage78-1 正式执行口径仍为 `500000`；本阶段只读 CTP 链路验证不使用策略资金参数
- 成本口径：不涉及
- 样本过滤：只测试券商评测/CP 交易前置 `tcp://182.140.218.46:41407`
- 策略/归因口径：不发单，只做认证、登录、结算确认、账户查询、持仓查询

## 结果

- 期末权益：不适用
- 总收益：不适用
- 最大回撤：不适用
- Sharpe：不适用
- 总滑点：不适用
- 总交易次数：不适用
- 胜率：不适用
- 其他关键指标：
  - 原生 C++ API 版本：`v6.7.7_MacOS_CP_20240716 15:00:00`
  - 交易前置：`tcp://182.140.218.46:41407`
  - `OnFrontConnected`：成功
  - `ReqAuthenticate ret=0`，`OnRspAuthenticate ErrorID=0`
  - `ReqUserLogin ret=0`，`OnRspUserLogin ErrorID=0`
  - 登录回报包含 `FrontID=15`、`TradingDay=20260520`、`LoginTime=13:55:36`
  - `ReqSettlementInfoConfirm ret=0`，`OnRspSettlementInfoConfirm ErrorID=0`
  - `ReqQryTradingAccount ret=0`，账户快照收到 `account_count=1`
  - `ReqQryInvestorPosition ret=0`，持仓快照收到 `position_count=32`
  - 汇总：`front_connected=true auth_ok=true login_ok=true settlement_ok=true account_count=1 position_count=32`
  - 委托/撤单 API 调用次数：`0`
  - CTP 返回的 `ErrorMsg=��ȷ` 是 GBK 字符串未转码显示；以 `ErrorID=0` 为准，语义为成功。

## 输出文件

- report：本 stage 文件
- summary：终端原生 C++ 探针输出
- orders：无，未调用报单或撤单接口
- daily：不适用
- quality：不适用

## 结论

- 本阶段结论：`41407` 交易前置、当前账号、AppID/AuthCode、Mac CP SDK 组合在原生 C++ 下完整通过认证、登录、结算确认、账户和持仓查询。此前 `vnpy_ctp`/Python TD-only 在 `41407` 无连接回调并退出 `139`，不应再优先归因为券商端口未开、账号未授权或收盘问题；更可能是 `vnpy_ctp` Python 封装与该 CP 交易库之间的兼容问题。
- 是否进入下一步：是
- 下一步：
  1. 短期 Stage78-1 虚拟盘仍优先使用已经完整验证的 `41207/41215` vn.py 路线。
  2. 如果券商或后续实盘环境必须走 `414xx/CP`，需要二选一：要么重编/替换 `vnpy_ctp` 使其匹配 CP 6.7.7 Mac 库，要么做一个极小原生 C++ 交易桥，只暴露登录、查询、报单、撤单、回报落盘接口，再由 Python 策略进程调用。
  3. 在 C++ 桥接进入发单前，仍必须保留 Stage78-1 SOP 的 fresh snapshot、dry-run、1手 smoke-order、显式确认和日终对账闸门。

## 过拟合反思

- 运行前判断：否。
- 运行后判断：否。
- 原因：这是链路分层归因，不涉及修改策略参数、品种池、信号逻辑或选择收益更好的样本；它只是在确认底层 API 与封装层的真实故障边界。

## 继续价值反思

- 运行前判断：有价值。
- 运行后判断：有价值。
- 原因：本阶段把问题从“券商端/账号/交易时段/端口”收敛到“Python 封装兼容层”，显著减少了无效沟通和重复排查。后续是否继续做 C++ 桥，应取决于最终实盘是否必须使用 CP/414xx；若 `412xx` 生产路线可用，则不应为了工程洁癖过早扩大 C++ 交易系统范围。

## 合入建议

- 是否更新本线 `LINE.md`：是
- 是否更新 `research/registry.md`：是
- 是否追加根目录 `memory.md/back_log.md`：暂不追加；这是执行链路突破，但仍属于 `futures_trend` 线内阶段记录。
