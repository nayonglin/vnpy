# Stage001 AI 排名年度 Jackknife 资格审计预声明

- line_id：`futures_trend_ai_rank_year_jackknife_qualification`
- 当前模式：`day`
- 记录时间：`2026-07-13 01:31 CST`
- 工作区/分支：`/Users/bytedance/Desktop/person/vnpy` / `codex/stage130-option-probe`
- 阶段性质：看 future60 结果前冻结输入、年度扰动、共识排名、标签和机械决策
- 是否重要突破：否
- 是否触发A/B：否

## 冻结输入与公式

- 五类输入 SHA、57个 Stage124 daily 内容 manifest SHA、行数和唯一键按 `LINE.md` 固定。
- baseline feature panel `4,446` 行、78月；base selected 总行数 `608`，但资格月从 `2022-01` 起且要求当月 baseline selected `8`。
- 只替换 score 中 `0.24 * rank_all_cycle_profit`；其余 `0.20/0.15/0.14/0.10/0.07/0.06/0.04` 权重及 raw feature 全部原样。
- deleted year 必须 `< eval_year`；禁止删除 eval 年 YTD 或未来年。
- 共识 Top8 使用 baseline + 所有 deleted-year variant 的平均 ordinal rank；不使用 future60 作为聚合权重。

## 冻结标签与比较

- future60 必须是 eval_date 后严格 `60` 个 global dates；eval 当日不进入。
- 产品 daily 必须对60个日期逐日完整；缺任一日，该产品标签为 incomplete。
- 只有 baseline-only 与 consensus-only 数量相等、每条标签完整的月份才进入 edge gate；无换仓月不伪造0 edge，单独计数。
- raw edge 为换入 future60 PnL 合计减换出合计；percentile edge 为换入/换出在当月完整产品 future60 PnL 横截面百分位均值差。

## 冻结硬门

- 完整比较月 `>=42`；实际换仓月 `>=24`；有效年每年换仓月 `>=3`。
- total raw edge、median monthly raw edge、median monthly percentile edge 全部严格 `>0`。
- 正 edge 年 `>=4`、负 edge 年 `0`；early `2022-2023`、late `2024-2026` 总 edge 均严格 `>0`。
- 最好年份正 edge / 全部正 edge `<=0.60`。
- 任一 gate 失败即 `CLOSE_LINE_YEAR_JACKKNIFE_RANK_INELIGIBLE`；不执行策略回测或参数救援。

## 测试先行

- base rank 完全重现、删除年不碰其他特征、只删过去年、tie-break、平均名次、严格60日、不完整标签、换入换出守恒、无换仓月、年度分组和机械 decision 必须有正负测试。

## 反思

- 过拟合：运行前为中等；单规格、无阈值扫描、跨年硬门可控制但不能消除研究序列后验风险。
- 继续价值：只值一次资格审计；通过也只允许四锚点真引擎 canary 预声明。

