# 趋势策略相关门替换研究线

- line_id: `futures_trend_covariance_gate_replacement`
- 创建时间: `2026-07-10 18:45 CST`
- 当前模式: `day`
- 资产/策略: 商品期货趋势 / 当前 C9 15w 独立研究分支
- 当前状态: Stage001 因执行语义 P1 改判无效并关闭
- 基准: 当前官方 AI 文件 + 当前 C9/15w 正式风险和执行逻辑

## 研究动机

上一条日期对齐边际协方差预算叠加在当前 `20` 日同向相关门之后，结果只保留 `53.19%` 收益；但旧门与新门改变的最终开仓重叠仅 `3` 行，不能据此判断新方法作为替代层是否无效。本线不救窗口、阈值或权重，只做一次结构替换。

## A/B/C 冻结

- A：当前官方 AI + 当前 C9，保留现有 `20` 日同向相关门。
- B：与 A 完全相同，但关闭现有同向相关门；B 只用于判断旧门的独立价值。
- C：关闭现有同向相关门，改用严格截至当日、同日候选可见、`63` 个日期对齐收益的边际协方差预算。
- 三臂保持相同官方 AI 月池、资金、成本、品种池、单品种 `25%` 保证金上限、最多 `4` 个持仓、增量保证金、止损、`0.5R` 当日止损重试一次和退出规则。

## 硬门槛

- C 相对 A 的全周期收益保留 `>=70%`。
- C 相对 A 的全周期最大回撤改善 `>=3pp`。
- C 相对 A 的 2022 年内最大回撤改善 `>=5pp`。
- C 相对 A 的固定压力窗 `2022-07-15 -> 2024-05-10` 最大回撤改善 `>=3pp`。
- C 的 broker10 峰值不得高于 A。
- C 所有 available 样本必须严格为 `63` 个收益，未来日期违规 `0`，最后共同日期滞后 `0`。
- C 不得放大替换层输入手数、不得把正手数变成零、不得缩小 `cross<=0` 的分散化候选；潜在开仓覆盖率 `>=80%`。
- 独立 agent review 不得有 P0/P1。

## 反过拟合边界

- 不扫描 `20/40/60/63/120` 窗口、相关阈值、weight floor、整数取整、产品、方向或日期。
- B 不是候选，不因 B 的结果继续调旧门。
- Stage001 失败即关闭本线；只有全部硬门槛通过才做逐半年多周期。

## 开始前判断

- 过拟合：否。只检验“叠加”与“替换”的机制差异，不按历史结果选择参数。
- 继续价值：有。当前 C9 已有相关门，先消除模块职责重叠，才能判断边际协方差方法本身。

## Stage001 结果与独立审查

- A：期末权益 `5,996,631.00`，总收益 `3897.7540%`，最大回撤 `-55.3701%`，Sharpe `1.3967`，broker10 `88.3398%`。
- B：期末权益 `5,329,186.30`，总收益 `3452.7909%`，最大回撤 `-59.0163%`，Sharpe `1.3559`，broker10 `95.7795%`。
- C：缺陷实现描述值为期末权益 `3,787,533.70`，总收益 `2425.0225%`，最大回撤 `-55.7874%`，Sharpe `1.2924`，broker10 `92.2180%`；不能当成预声明 replacement 的有效估计。
- 收益保留 `0.6222`；C-A 全周期/2022/主压力窗回撤变化 `-0.4173/-4.3359/-0.4173pp`；broker10 变化 `+3.8782pp`，全部绩效门槛失败。
- 独立 review：`P0=0/P1=1/P2=3`。三臂配置与 AI `504` 行、`55` 个 eval_date、归一化 hash 均一致；summary/stress 复算正确。
- P1：C 继承上游同日计划手数问题。`2020-05-11` CF 计划 `6` 手，rb 按 CF `6` 手计算并从 `11 -> 7`，当日正式 forced-margin 随后把 CF 目标降到 `2` 手。直接污染 `1/25` 个 opened 同日可见候选，但发生在 2020 年，后续路径依赖全部受影响。
- 本线 decision 曾漏接上游 `same_day_final_target_semantics_ok=False`，错误写出 `semantics_ok=True`；现已修正。
- 最终决策：`invalid_semantics_and_close_no_rescue`。不实现两阶段 preview：它与 marginal 手数互相改变 forced-margin 目标，复杂度高；当前结果同时远离全部绩效门槛，没有合理继续价值。

## 结束反思

- 过拟合：否。一次冻结结构实验，发现 P1 后直接作废，没有按结果调窗口、阈值或手数。
- 继续价值：本线无。只保留“旧 20 日相关门有保护作用”和执行顺序 P1 的工程经验。
- 不做逐半年、不晋级、不扫参数；后续转 Stage013 当前 AI 重验独立线。
