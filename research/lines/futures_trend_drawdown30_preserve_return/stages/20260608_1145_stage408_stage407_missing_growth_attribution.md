# Stage408 Stage407 2025 右尾增长缺失归因

- line_id：`futures_trend_drawdown30_preserve_return`
- 当前模式：`day`
- 记录时间：`2026-06-08 11:45 CST`
- 阶段性质：Stage407 定点归因，不是新回测
- 是否重要突破：否，但解释用户指出的 2025 增长缺失
- 正式配置：未修改
- CTP/下单：未连接 CTP，未调用 order API

## 外部调研与判断

- 参考资料：
  - Man Group `A Trend Following Deep Dive: The Optimal Market Mix for a Trend Follower`：趋势跟踪市场组合本身会改变右尾捕捉效率。
  - Trend-following / CTA 公开资料普遍强调：策略收益往往来自少数加速趋势，市场选择和风险预算会决定是否吃到这些右尾。
- 我的判断：用户框出的增长缺失不是鸡蛋自身亏损导致，而是“AI 重排挤掉盈利腿 -> 路径进入低风险档 -> 真正右尾来时手数被压到约 0.1 倍”的路径效应。

## 定位区间

- 用户图中红框对应的主要缺口窗口可量化为 `2025-04-16 -> 2025-07-25`。
- 该 100 日窗口：
  - A 正式版增长 `+5,605,230`
  - B 仅 maxpos5 增长 `+4,952,370`
  - C 原池 + 鸡蛋参与 AI 重排 top9 增长 `+90,830`
  - A-C 增长缺口 `+5,514,400`

## 产品归因

### `2025-04-16 -> 2025-07-25`

- C 相对 A 少赚主要来自：
  - `jm`：A `+3,254,640`，C `+207,870`，缺口 `+3,046,770`
  - `si`：A `+1,315,650`，C `+73,500`，缺口 `+1,242,150`
  - `lc`：A `+677,260`，C `0`，缺口 `+677,260`
  - `fu`：A `+303,480`，C `0`，缺口 `+303,480`
  - `fg`：A `+236,940`，C `+15,840`，缺口 `+221,100`
- C 在该窗口没有鸡蛋贡献：`jd` 活跃天数 `0`、PnL `0`。

### 更宽视觉区间 `2025-05-01 -> 2025-09-30`

- A 增长 `+4,186,905`
- B 增长 `+3,721,115`
- C 增长 `+233,530`
- C 相对 A 少赚主要来自：
  - `jm` 缺口 `+2,373,570`
  - `fu` 缺口 `+779,560`
  - `si` 缺口 `+553,475`
  - `au` 缺口 `+272,700`
  - `lc` 缺口 `+151,290`
  - `fg` 缺口 `+136,200`
- C 的鸡蛋在该宽区间为 `+251,810`，但完全抵不过 `jm/fu/si/lc/fg` 的右尾缺口。

## 机制链条

1. AI 重排首先挤掉 `fu`
   - `2025-04-30`：C 用 `jd.DCE` 替换 `fu.SHFE`
   - `2025-05-30`：C 用 `jd.DCE` 替换 `fu.SHFE`
   - `2025-06-30`：C 用 `jd.DCE` 替换 `fu.SHFE`
   - `2025-07-31`：C 用 `jd.DCE` 替换 `fu.SHFE`
   - `2025-08-29`：C 用 `jd.DCE` 替换 `fu.SHFE`
   - `2025-09-30`：C 用 `jd.DCE` 替换 `jm.DCE`
2. C 错过 `fu` 的盈利腿
   - `2025-06-12 -> 2025-06-25`，A/B 分别开 `fu2509.SHFE` 多 `281/248` 手，C 没有开仓。
   - 该腿 A `+303,480`、B `+267,840`、C `0`。
3. C 后续进入低风险档
   - `2025-06-18 -> 2025-06-25`，C 开 `MA509.CZCE` 多 `170` 手并亏 `-149,600`。
   - 由于前序错过 `fu` 赢利腿，C 在 6 月后半段的路径明显弱于 A/B。
4. 7 月真正大右尾来时，C 手数约等于“同风险下应有手数”的 `0.1` 倍
   - `jm`：A `142` 手；按 C 当时权益若同风险应约 `85.6` 手，实际 `9` 手，实际/应有 `0.105`
   - `si`：A `179` 手；按 C 当时权益若同风险应约 `104.1` 手，实际 `10` 手，实际/应有 `0.096`
   - `fg`：A `359` 手；按 C 当时权益若同风险应约 `208.7` 手，实际 `24` 手，实际/应有 `0.115`
   - `cu`：A `29` 手；按 C 当时权益若同风险应约 `17.6` 手，实际 `2` 手，实际/应有 `0.114`

## 结论

- 红框增长消失不是因为鸡蛋在那段亏钱；在核心窗口 `2025-04-16 -> 2025-07-25`，鸡蛋甚至没有实际活跃。
- 真正原因是路径效应：
  1. 鸡蛋参与 AI 后，在 2025 年 4-8 月连续替代 `fu.SHFE`；
  2. C 错过 `fu` 盈利腿，且后续 `ma/cu` 等路径更弱；
  3. 到 `jm/si/fg` 大右尾来时，C 的开仓手数被风险档压到约 `0.1` 倍；
  4. 因此 `jm/si/fg/lc/fu` 的右尾没有吃到，资金曲线直接横住。
- 这说明 Stage407 的问题不是“鸡蛋这个品种有毒”，而是当前 AI 重排把正式版右尾保留机制打破了。

## 过拟合反思

- 本阶段不是过拟合，是定点归因；没有用该窗口反向调参数。
- 若后续按 2025 年、按 `fu`、按 `jm` 做专门白名单/黑名单，则会转为过拟合。

## 继续价值反思

- 作为 Stage407 主策略候选，没有继续价值。
- 作为理解 AI gate 和设计非挤占式鸡蛋 sleeve 的证据，有继续价值。

## TODO

- 不救 Stage407 的 `topN/maxpos`。
- 若继续鸡蛋方向，优先测试“鸡蛋独立小 sleeve，不参与主 AI 排队，也不触发主账户连败风险档”的结构。
