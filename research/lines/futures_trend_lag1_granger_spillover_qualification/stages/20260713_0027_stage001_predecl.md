# Stage001 Lag-1 Granger 网络资格预声明

- line_id：`futures_trend_lag1_granger_spillover_qualification`
- 当前模式：`day`
- 记录时间：`2026-07-13 00:27 CST`
- 工作区/分支：`/Users/bytedance/Desktop/person/vnpy` / `codex/stage130-option-probe`
- 阶段性质：看结果前冻结数据、统计与机械决策合同
- 是否重要突破：否
- 是否触发A/B：否

## 冻结输入

- Stage131 query events：`365` 行、`365` 唯一 event，冻结 SHA 必须与当前文件一致。
- full-market tradable universe：只取 eligibility 文件中 `eligible=1` 的固定 `57` 产品。
- 本地日线：`.vntrader/database.db`；运行前后都记录 SHA256。
- 不读取 current-C9 交易结果、closed lots、daily equity、drawdown 或未来 event return。

## 冻结统计合同

- T-1 实际合约选约、同合约收益、132共同日、lag1 Granger、global `fdr_bh alpha=0.05`、early66/late66 系数同号，定义以 `LINE.md` 为准。
- 资格事件必须至少有1条稳定 incoming edge，且完整 leader pair 至少 `29/56`。
- 不允许运行后修改 lag、窗口、alpha、FDR、覆盖率、leader universe 或稳定边定义。

## 冻结机械决策

- 数据完整性、T-1、同合约收益、全事件/2022/逐年覆盖任一失败：`CLOSE_LINE_LAG1_GRANGER_NETWORK_INELIGIBLE`。
- 全部门通过：`ALLOW_STAGE002_NETWORK_SIGNAL_PREDECL_ONLY`。
- 两种决策下都保持 `ready_for_strategy_ab=false`、`ready_for_live=false`；Stage001 不运行策略回测。

## 测试先行

- synthetic contract roll 必须证明换月日仍使用新选合约自身前收，不出现跨合约价格直接相除。
- synthetic leader->target 必须由 statsmodels lag1 检验识别，反向和随机序列不得被错误标成同一稳定边。
- event history 最大日期必须严格小于 entry date；不足132日保持不可用。
- global BH 和 early/full/late 同号逻辑必须有正负测试。

## 反思

- 过拟合：预声明阶段否；若运行后改成 event-level FDR、挑年份/品种或扫描 lag/window，则是。
- 继续价值：只值得运行一次冻结资格审计；失败后无参数救援价值。
