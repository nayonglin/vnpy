# futures_trend_rebuilt_c9_15w_optimization - 重建版 C9/15w 优化线

## 定位

- 资产：商品期货。
- 当前基准：功能性重建后的当前线上 C9/15w 版本 `official_live_stage847_c9_15w_stage819_05r_stop_retry_once`。
- 当前 profile：`stage847_c9_15w_stage819_05r_stop_retry_live`。
- AI 池：当前重建 Stage182 池 `qmt_roll_stage182_ai_product_pool_live_inference_combined_stage78_eligibility_stage182_ai_product_pool_live_inference_v1.csv`，当前文件 sha256 `530620be7feca7f6d375baa9792e766a7c4cbc81dee959fe1dee82bf021f31ce`。
- 研究目标：在承认当前版本不是删除前旧产物字节级复刻的前提下，把“当前可复验、可上线延续”的重建版作为新的优化母本；当前用户目标已更新为 `2020-01-01` 到 `2025-06-30` 任意日起点、回测周期大于一年正收益、全周期收益保留 `80%+`、基础品种池加鸡蛋、AI 选品进一步优化、识别超高质量信号并加大风险投入。
- 独立性：本线只写 `research/lines/futures_trend_rebuilt_c9_15w_optimization/` 下的记录、工具和输出；实验脚本、输出命名应使用 `rebuilt_c9_15w_opt` 或本线 Stage 号前缀；不得污染 `futures_trend_stage819_intraday_rules`、旧 `futures_trend_c9_minrisk_highquality` 或当前官方实盘执行链路。

## 基准口径

- 基准阶段：`Stage167 C9 15w 线上版本多周期回测与 AI 池审计`。
- 基准记录：`research/lines/futures_trend_stage819_intraday_rules/stages/20260701_0217_stage167_c9_live_multiperiod_ai_audit.md`。
- 基准回测：从 `2018-01-01` 起每半年一个独立冷启动起点，统一结束 `2026-06-30`，共 `17` 个窗口。
- 基准 AI 审计：`PASS 858`、`PRE_AI_HISTORY 60`、`FAIL 0`；首个 Stage182 快照 `2019-12-31` 之后的候选月份全部带 AI enabled 和 signal-date 元数据。
- 基准结果：正收益 `17/17`；期末权益最低/中位/最高 `152,851.60 / 455,463.70 / 14,900,482.00`；总收益最低/中位/最高 `1.9011% / 203.6425% / 9,833.6547%`；最差最大回撤 `-56.2069%`；回撤中位 `-47.2779%`；Sharpe 最低/中位/最高 `0.2860 / 1.1937 / 1.4786`；peak broker10 margin/equity `96.6295%`。
- 基准图表：
  - `examples/portfolio_backtesting/backtest_outputs/qmt_roll_stage167_c9_live_15w_multiperiod_ai_audit_performance_chart_stage167_c9_live_15w_multiperiod_ai_audit_v1.png`
  - `examples/portfolio_backtesting/backtest_outputs/qmt_roll_stage167_c9_live_15w_multiperiod_ai_audit_absolute_equity_chart_stage167_c9_live_15w_multiperiod_ai_audit_v1.png`
  - `examples/portfolio_backtesting/backtest_outputs/qmt_roll_stage167_c9_live_15w_multiperiod_ai_audit_ai_audit_chart_stage167_c9_live_15w_multiperiod_ai_audit_v1.png`

## 基线边界

- 本线基准是“当前功能性重建版”，不是删除前旧正式产物的 1:1 字节级复原。
- 不允许把旧记录中的更好指标直接当作本线达标线；旧记录只能作为历史对照和风险提示。
- AI 相关实验必须显式写明使用的 AI 池文件、hash、eval_date 范围和是否冻结月度池；不得悄悄切换到最新池或旧池。
- 实盘执行逻辑、Phase D、Stage929/930/931、CTP/SimNow、邮件和 launchd 不属于本线默认可改范围；若某实验未来可能进入实盘，必须另走官方 SOP、健康门和用户显式确认。

## 外部调研种子

- Bailey、Borwein、Lopez de Prado、Zhu 的 PBO/backtest overfitting 框架提醒：多回测、多参数、多候选会显著提高样本内虚假发现概率；本线所有优化必须做预声明和多起点验证，不能 winner-picking。
- Hurst、Ooi、Pedersen 的长期趋势跟随研究提示：趋势跟随的核心价值来自跨市场、跨周期的右尾与分散化；优化不应为了压局部回撤而切掉趋势右尾。
- 后续每个真正优化阶段开始前，必须重新做与该方向相关的网上/GitHub/论文调研，并在 stage 文件中写明采纳、否决或融合的理由。

## 研究假设

- 当前 C9/15w 重建版的主要问题不是 AI 池漏用，而是路径左尾：早期起点最大回撤约 `-55%` 到 `-56%`，DD50 失败较多。
- 右尾很强，不能用简单过滤、缩手、时间止损、固定止盈或单品种黑名单牺牲趋势复利底座。
- 更有价值的方向可能在账户层风险状态、入场后风险预算释放纪律、保证金压力治理、信号质量只读标签与新外生信息源，而不是继续扫 `R` 倍数、重试次数、月份、品种、方向或窗口。

## 预声明评估标准

- A：Stage167 当前重建 C9/15w 基准。
- C：本线新增候选。
- 必看指标：
  - 多周期起点的期末权益、总收益、最大回撤、Sharpe。
  - DD30/DD40/DD50 fail count。
  - broker10 峰值、broker100 fail count。
  - 交易次数、滑点、胜率、成本压力。
  - AI 池审计是否仍为 post-AI `FAIL=0`。
- 晋级倾向：
  - 最大回撤和 DD50 fail 明显下降。
  - 收益保留有清晰阈值，默认不低于基准中位收益的 `80%`，且不得只靠牺牲早期右尾换平滑。
  - Sharpe 不明显恶化。
  - broker10/broker100 不恶化。
  - 改善不能集中在单一年份、单个起点、单品种、单方向或单个右尾事件。

## 反过拟合约束

- 不按 `2018-01`、`2022-07`、`2026-01` 等具体起点定制规则。
- 不按单品种、交易所、方向、月份、节假日或具体合约黑名单补丁化。
- 不扫 `0.25R/0.5R/1R`、初始风险比例、确认窗口、止损倍数、重试次数、仓位比例等参数；每条候选必须先有第一性解释，再冻结一个版本做验证。
- 不用未来 MFE/MAE、最终盈亏、日终后才知道的信息，去定义实时交易规则。
- 代理审计只能决定是否值得写真实组合引擎；不能把代理收益当作正式回测结论。
- 每个阶段开始和结束都要写明：是否过拟合、是否仍值得继续。

## 当前状态

- Stage001 已立线。当前线基准固定为 Stage167 当前重建 C9/15w 多周期回测与 AI 审计结果。
- Stage002 已完成目标基准审计。当前重建版尚不满足用户目标：年度负收益行 `29` 个，主要集中在 `2023` 和截至 `2026-06-30` 的半年度样本；最差年度行为 `2021-07` 起点的 `2022` 年，年度收益 `-16.9640%`。Stage167 中位总收益 `203.6425%`，后续候选的 `80%` 收益保留参考线为 `162.9140%`。`jd.DCE` 在 full-market universe 中可用，但不在当前 Stage182 AI 池，也不在 Stage167 候选。
- Stage003 已完成负年度与鸡蛋接入归因。负年度不是 AI 未启用导致；正年度的 broker10 峰值和开仓广度反而更高，负年度更像有效趋势机会少、AI block 占比更高、开仓数和 opened products 更低。鸡蛋数据可用但未入 AI 池/候选，历史 Stage405/406/407 提示不得直接塞入共享 AI rerank/topN。
- Stage004 已完成历史反证清单与下一阶段护栏。共整理 `15` 类禁试形状和 `7` 条允许原则；共享 AI 池加鸡蛋、默认最小风险再恢复、no-follow 降仓、OR 退出、保本、DD 地板、同手数加仓均不得继续扫参救援。下一步只允许“核心 C9 不挤占 + jd 独立/非挤占 + 入场可见质量标签 + 小额独立加风险预算”的冻结代理。
- Stage005 已完成冻结代理数据可行性审计。当前 Stage167 entry candidates 有核心候选、AI rank/score、OI确认和组合状态，但缺当前重建版逐笔 closed-lot/outcome、entry/first-minute quality labels；旧 Stage016 只能作为特征蓝图。`jd.DCE` 在 full-market universe 可用，但不在当前 Stage182 selected/latest pool，也没有 full-universe monthly score matrix。
- Stage006 已完成当前重建版质量特征绑定器。用 Stage901 `_run_live_c9` 重跑同口径 `17` 个冷启动起点，复跑摘要与 Stage167 完全一致；保存 `trades/entry_risk/entry_candidates/closed_lots/quality_features/curves`，并补了绝对权益资金曲线。当前 `closed_lots=3401`，但当时只用 `stage861_visual_atlas` 绑定，`entry_first_bar_available=899/3401=26.4334%`；旧 Stage016 交集标签在该窄源下只有 `ai4_6_entry_or_first_aligned=27` 笔、`2` 年、PnL `9,140`。
- Stage007 已完成分钟源覆盖修复与质量标签重绑。根因是 Stage006 分钟源选窄：`stage152_complete` 覆盖 `3311/3401=97.3537%`，显著高于 Stage861 的 `26.4334%`。重绑后 `ai4_6_entry_or_first_aligned=306` 笔、`13` 产品、`7` 年、PnL `22,617,180`；`ai4_6_not_aligned=497` 笔、PnL `-2,341,570.2`；`aligned_not_ai4_6=1067` 笔、`9` 年、PnL `38,025,240`。这是有价值的高质量信号候选，但仍只是只读证据，不能直接加仓。
- Stage008 已完成高质量标签小额非挤占加风险只读代理。固定 `tag_ai4_6_entry_or_first_aligned`、固定 `ADD_RISK_FRACTION=0.25`，不扫参；选中 `306` 笔，增量 PnL 代理 `5,654,295`。17 个起点中收益改善/不变/变差为 `16/1/0`，最大回撤改善/不变/变差为 `15/2/0`；中位收益从 `203.6425%` 到 `227.6866%`，最差最大回撤从 `-56.2069%` 到 `-55.2574%`。但 `annual_negative_rows=29` 未改善，且本阶段没有真实成交/保证金/broker10 约束，不能作为候选上线。
- Stage009 已按用户更新后的目标补密集起点口径审计。目标起点范围改为 `2020-01-01` 到 `2025-06-30`，周期 `>365` 自然日。现有运行曲线子周期审计显示：若结束日固定为 `2026-06-30`，Stage008 proxy 的 `13,267` 个窗口负收益为 `0`，最低收益 `17.7001%`；若严格要求任意结束日、任意 `>1` 年周期都正收益，Stage008 proxy 在 `7,215,647` 个窗口里仍有 `427,688` 个负收益窗口，最差 `-54.2509%`，主要集中在 `2022-07-15 -> 2023-07-17/18/24` 附近。全周期 `80%` 收益保留 `17/17` 通过。本阶段不是每日独立冷启动证明。
- Stage010 已完成 Stage009 最差窗口左尾归因。焦点窗口为 `2022-07-15 -> 2023-07-17`，覆盖 `10` 个冷启动账户；最差 source `2018-01` 的 proxy 窗口收益 `-54.2509%`、base `-55.2146%`。Stage008 高质量加风险在窗口内合计贡献 `+606,005`，方向是缓冲不是放大；但窗口内已平仓 lot 净实现盈亏仅 `-10,575`，而 base 窗口权益变动合计 `-19,235,925`，残差 `-19,225,350`，说明主问题在窗口内持仓浮亏/日级 holding_pnl 和账户层风险暴露。闭合 lot 层面 `SM.CZCE short` 最差，但不得据此做单品种/方向黑名单。
- Stage011 已完成焦点窗口 positions 重跑与日级持仓 PnL 归因。`10` 个 source 重跑到 `2023-07-17`，一致性最大差异 `9.31e-10`，positions 可完全解释账户曲线；窗口持仓净损失 `-19,235,925` 中，窗口起点已有仓位亏损占比 `38.08%`，窗口后新增/交易仓位亏损占比 `61.92%`。最大拖累为 `SM.CZCE short opened_or_traded_after_focus_start`，但仍不得黑名单化；最大单日合计亏损 `2022-07-18` 为 holding_pnl 冲击，`2022-11-28` 则出现 broker10 约 `90%` 的保证金压力。
- Stage012 已完成新增/交易仓位入场前账户状态诊断。焦点窗口新增/交易仓位日级净 PnL `-11,911,535`；焦点入场 `293` 笔 realized PnL `-10,990,495`、胜率 `23.8908%`，而同 source `2020+` 基准入场 realized PnL `75,341,647.6`、胜率 `47.5890%`。坏窗口中 `rank_4_6`、`ai4_6_entry_or_first_aligned` 等历史上强势桶也亏损，说明高质量标签不能脱离账户状态单独加风险。
- Stage013 已完成账户状态小风险试探真实引擎候选。规则冻结为：组合回撤 `>=30%` 且有效活跃持仓 `<=1` 时，`flat_entry` 新开仓先降到 `1` 手试探；不按品种、日期、方向黑名单，官方线上 C9/15w 配置未改。真实引擎 `17/17` 起点正收益，期末收益最小/中位/最大 `1.9011% / 238.3687% / 9880.1302%`，最差最大回撤从 Stage006 的 `-56.2069%` 改善到 `-43.7940%`，全周期 `80%` 收益保留 `17/17`，AI 月度审计 `PASS 858`、`PRE_AI_HISTORY 60`、`FAIL 0`。但严格任意结束日 `>1` 年窗口仍有负收益 `330,947/7,215,647`，最差 `-43.7940%`；因此用户目标未达成，Stage013 只能保留为有价值的研究候选/归因资产，不能直接上线。
- Stage014 已完成 Stage013 剩余负窗口失败地图。Stage013 相比 Stage008 proxy 负窗口减少 `96,741` 个，最差收益改善 `10.4569pp`，但 top500 最差窗口 `500/500` 都有 pilot gate 触发，说明小风险试探生效但不足。剩余最差窗口集中在 `2022-07 -> 2023-07/2023-10`，固定周期 `366/540/730/1095` 天负收益率分别为 `12.9419% / 12.9450% / 4.8788% / 0.2069%`，问题主要是 `1-2` 年恢复段，而不是长期右尾消失。
- Stage015 已完成 Stage013 触发后确认信号与 `jd.DCE` 非挤占可行性只读归因。pilot 事件 `639` 个，entry_risk 匹配 `639/639`，closed-lot 匹配 `623/639`；`2022-2023` focus pilot lot `235` 笔，原始 1 手 PnL `-19,700`。3/5/10 日确认从“入场开始加风险”的上界看有筛选性，但按“确认后再加风险”的可交易代理在焦点段全部为负，例如 `confirm_10d_combo=True` 原始 PnL `91,420`、post-confirm extra PnL `-1,132,760`；`confirm_5d_close_positive=True` 原始 PnL `171,100`、post-confirm extra PnL `-4,367,930`。`jd.DCE` 旧 full-market 证据也不支持直接加入共享 AI 池：`2022-2023` future top-half 仅 `9/24=37.5%`，`2023` 仅 `2/12=16.67%`。Stage015 不晋级真实引擎候选。
- Stage016 已完成账户压力只读归因。`broker10>=80%` 不是 Stage013 剩余左尾的干净前置信号：样本 `24` 天，后续 `252/366` 交易日负收益率均为 `0%`。Top100 最差窗口起点中位 broker10 只有 `28.6952%`、中位 drawdown `-1.1315%`、中位活跃品种 `3`；压力是在前 `63-126` 个交易日发展出来的。`active4_near_peak` 是风险标签，后续 `252` 日负收益率 `22.0588%`，但中位收益仍 `101.8748%`、均值 `196.7189%`，不能直接禁开/减仓。Stage016 不晋级真实引擎候选。
- Stage017 已完成外生 regime/volatility 只读归因。使用当前重建数据管线已物化的 market daily 和 full-market AI monthly predictions，不引入新数据供应链、不改策略。`high_vol_low_eff` 后续 `252` 交易日负收益率 `31.5021%`，显著高于 all market days 的 `14.6258%`；Top100 最差窗口前 `63` 日 `high_vol_low_eff` 天数占比中位 `44.4444%`；`2022-2023` focus entry cash PnL `-3,727,166`；同期 AI top8 该桶未来 `60d` 平均 PnL `-1,351.25`。但 `high_vol_high_eff` 在曲线 forward 上很差、在 focus entry cash PnL 上为正，说明不能简单按高波动禁开。Stage017 只提供 Stage018 冻结真实引擎验证价值，不是上线规则。
- Stage018 已完成 high-vol/low-efficiency 小风险试探真实引擎。规则冻结为：前一交易日 causal 市场状态为 `high_vol_low_eff` 时，`flat_entry` 新开仓降到 1 手；vol/eff 分桶只使用此前历史 expanding quantile，不用同日/未来信息。结果不达标且不晋级：正收益起点仍 `17/17`，严格任意结束日 `>1` 年负窗口从 Stage013 的 `330,947` 降到 `280,114`，但最差窗口仍 `-43.7940%`，全周期 `80%` 收益保留只有 `10/17`；早期右尾收益被显著削弱，例如 `2018-07` 总收益从 Stage013 `9880.1302%` 降到 Stage018 `7413.9603%`。不得继续扫 regime 分位、min_history、手数或叠加简单 drawdown/active 条件救参。
- Stage019 已完成 Stage018 失败归因。Stage018 gate 事件 `234` 个，匹配 Stage013 closed-lot `232` 个，累计减少手数 `9,088`；被减少仓位在 Stage013 的实现盈亏代理为 `7,709,757.17`，capped 敏感性为 `7,868,242.17`，手数完全一致事件也为正 `933,604.00`。收益保留失败的 `7` 个 source 被砍 PnL 代理 `7,665,477.17`，实际期末权益差 `-17,606,410.80`。结论：Stage018 主要问题是切断 2021 起形成的右尾，不是一个值得继续救参的风控形状。
- Stage020 已完成 Stage013 + 高质量标签加风险只读代理。固定 `tag_ai4_6_entry_or_first_aligned` 和 `25%` 非挤占加风险，不扫标签组合或倍率；选中 `233` 笔，Stage013 realized PnL `6,119,015.00`，代理增量 PnL `1,529,753.75`。收益改善/不变/变差 vs Stage013 为 `16/1/0`，80% 收益保留 vs Stage006 和 Stage013 均为 `17/17`，年度负收益行降到 `14`；但严格任意结束日 `>1` 年负窗口仍 `323,323/7,215,647`，最差 `-41.6745%`，最大回撤改善/不变/变差只有 `8/2/7`。结论：高质量信号加风险有收益辅助价值，但不能单独达成目标，不晋级真实引擎、不改线上、不触发 A/B。
- Stage021 已完成 full-market 共识选择器与 jd 非挤占只读代理。固定 `AI top8 AND simple trend top8` 和 `25%` 非挤占加风险，不扫 topN 或倍率；在已有 Stage013 lots 上选中 `100` 笔，Stage013 realized PnL `11,707,033.20`，代理增量 PnL `2,926,758.30`，Stage021 combo 收益改善/不变/变差 vs Stage020 为 `16/1/0`，80% 收益保留 `17/17`。但严格任意结束日 `>1` 年负窗口只从 Stage020 `323,323` 降到 `321,446`，最差反而为 `-42.0358%`，未达标。jd 审计显示 jd 共识月份仅 `2` 个，均在 `2022H2` 且未来 `60d` PnL 为 `-760/-760`，不支持直接加入共享 AI topN；jd 只能保留为非挤占观察或等待新信息源。
- Stage022 已完成账户层生存线与利润锁定只读审计。预声明 primary 为 `3x` 本金后锁定新增高水位利润 `50%`，其他阈值只作为 sensitivity，不作为候选；不改 C9、不接 CTP、不调用下单。Stage021 baseline 严格任意结束日 `>1` 年负窗口 `321,446`，primary 降到 `310,012`，但最差仍 `-42.0358%`，到 `2026-06-30` 最差 `18.0338%`，全周期 `80%` 收益保留仅 `10/17`。sensitivity 最少负窗口为 `3x_lock67` 的 `305,800`，仍不达标且同样收益保留 `10/17`。结论：锁盈/备用桶可以降低绝对资金缺口，但会压早期右尾，不能作为目标主解，不晋级、不触发 A/B。
- Stage023 已完成剩余负窗口前置信号只读归因。用 Stage021 combo 曲线把 `2020-01-01` 到 `2025-06-30` 的 `13,267` 个可审计起点压成起点级标签：未来任意 `>365` 天结束曾为负的起点 `2,071` 个，起点负率 `15.6102%`，最差仍 `-42.0358%`。最强条件为 `loss_and_high_vol_low_eff`，样本 `664`、负起点率 `36.7470%`、lift `2.3540`；最强分桶为 `joint_regime=high_vol_high_eff`，样本 `946`、负起点率 `66.2791%`、lift `4.2459`。稳定性审计显示这些信号主要集中在 `2021-2022`，例如 `high_vol_high_eff` 在 `2022` 负率 `84.2881%`、但 `2020/2024` 为 `0%`；因此只能作为真实引擎候选筛选器，不得直接上线或扫阈值。
- Stage024 已完成因果 `high_vol_high_eff` 暂停新开仓真实引擎候选。固定规则为：前一交易日因果 regime 为 `high_vol_high_eff` 时暂停新的 `flat_entry`，不强平已有仓位，不影响非 `flat_entry`，不按品种、方向、日期或 source 定制；官方线上 C9/15w 配置未改，不连接 CTP，不调用下单。真实引擎 `17/17` 起点正收益，期末收益最小/中位/最大 `1.9011% / 232.0954% / 10189.7746%`，全周期 `80%` 收益保留 `17/17`，到 `2026-06-30` 负窗口 `0`、最差 `26.3434%`；但严格任意结束日 `>1` 年负窗口仍有 `298,012`，严格最差收益 `-44.0955%`，最差最大回撤 `-44.0955%`。暂停事件 `156` 个，减少手数 `13,576`。结论：它比 Stage013/Stage021 减少部分严格负窗口，并比 Stage018 更保留右尾，但最差回撤未解决且略差于 Stage013，所以不晋级、不触发 A/B，只保留为归因资产。
- Stage025 已完成 Stage024 暂停失败只读归因。Stage024 相对 Stage013 新增负窗口 `39,043`、消除负窗口 `71,978`、净减少 `32,935`，但 `2021-01` source 净新增 `14,833` 个负窗口，`2022-07` source 净新增 `3,370` 个负窗口。暂停 episode 聚合后 `113` 个，`252` 交易日后 equity-delta 总和 `+24,949,883.6`、正贡献率 `81.42%`，说明暂停多数方向有效；但 `2022-03-28` 的 `fu/hc/jm`、`2022-08-30 SA` 等 episode 对 `2021-01` 生命周期是明显右尾错杀。结论：`high_vol_high_eff` 内部混有“该躲的假趋势”和“必须保留的恢复段右尾交易”，单一 hard regime gate 不能达标，停止同类 regime 阈值/手数救参。
- Stage026 已完成恢复段右尾 episode 入场前识别只读归因。Stage024 暂停 episode 共 `113` 个，`252d` 右尾错杀 `21` 个、错杀率 `18.5841%`，暂停有益 `92` 个，`252d` delta 总和 `+24,949,883.6`、中位 `+110,065`。最强条件为 `ai_consensus_any`，样本 `18`、`9` 个 source、但仅 `2` 个日期，错杀率 `61.1111%`、lift `3.2884`；最强分桶同样是 `ai_consensus_flag_share=high`。由于样本少且日期集中，不能写真实引擎。结论：没有找到跨 source、足够样本且不依赖日期集中的入场前可见前兆，应关闭 hard regime gate 方向。
- Stage027 已完成 Stage024 剩余 worst-window 持仓路径归因。选择 Stage024 top `1000` worst windows，按 `source + start_date` 去重后取 `50` 个代表窗口；用 Stage024 真实引擎重放 `10` 个 source 到 `2024-03-15`，positions 到曲线最大一致性误差 `5.8208e-11`。代表窗口聚合净 PnL `-6,468,110`，其中 holding PnL `-6,135,740`，占净损失 `94.8614%`；已有仓位亏损占比 `11.0293%`，窗口后新开/交易仓位亏损占比 `88.9707%`。最大品种方向拖累是 `fu.SHFE long opened_or_traded_after_window_start`，但只作归因，不得黑名单化。
- Stage028 已完成 Stage024/Stage027 入场前账户状态与候选排序只读归因。只取 Stage027 代表坏窗口涉及的 `3` 个 source、日期不晚于 `2023-10-23` 的 opened `flat_entry`，样本 `250` 笔，坏窗口开仓 `137` 笔，基准坏窗口率 `54.8000%`。稳定候选条件集中在账户已受伤状态：`drawdown_abs_ge30` 坏窗口率 `98.8095%`、lift `1.8031`，`drawdown_abs_ge20` 坏窗口率 `95.1613%`、lift `1.7365`，`loss_streak_ge3` 坏窗口率 `93.1034%`、lift `1.6990`，`drawdown_ge20_and_volume_gt1` 坏窗口率 `90.1961%`、lift `1.6459`。反证点是单看 `selected_volume > 1` 并不更坏，坏窗口率仅 `37.9747%`；`AI rank <=4/<=8` 的 lift 只有 `1.0841/1.0802`。结论：下一步只允许冻结验证“账户回撤/连败状态下的新开仓释放纪律”，不能继续按 AI rank、pairwise rank、品种、方向、日期或粗暴压正常仓救参。
- Stage029 已完成账户受伤状态下暂停新 `flat_entry` 的冻结真实引擎验证。规则为组合回撤 `>=20%` 或 `loss_streak>=3` 时暂停新的 `flat_entry`，不强平已有仓、不改 rollover/add/reverse、不按品种/方向/日期/source 定制；官方线上 C9/15w 配置未改，不连接 CTP，不调用下单。AI 月度审计 `PASS 858`、`PRE_AI_HISTORY 60`、post-AI `FAIL 0`。该规则触发暂停 `2,869` 次、减少手数 `21,042`，最差最大回撤改善到 `-27.4723%`，但收益被严重压制：正收益起点仅 `9/17`，最小/中位/最大收益为 `-14.2267% / 8.5300% / 406.8007%`，80% 收益保留仅 `1/17`，严格任意结束日 `>1` 年负窗口仍有 `230,729/7,215,647`，到 `2026-06-30` 口径也出现 `433/13,267` 个负窗口。结论：账户受伤状态是有效风险标签，但“直接暂停新开仓”过粗，错杀恢复段右尾，不晋级、不触发 A/B。
- Stage030 已完成 Stage029 暂停规则失败归因。暂停事件 `2,869` 个中，`2,794` 个匹配 Stage006 同日同品种方向 flat-entry 候选，匹配率 `97.3858%`；其中 `2,587` 个在 Stage006 实际打开，打开匹配率 `90.1708%`，对应 Stage006 selected_volume 合计 `203,281`。closed-lot 精确收益代理只命中 `22` 个，但 realized PnL 代理 `+1,249,440`、正/负 `15/7`，说明暂停集合不是纯亏损集合。Stage029 vs Stage006 期末权益差合计 `-62,204,602.80`。结论：Stage029 不是在过滤无效候选，而是在账户受伤后大面积关闭 C9 原本会开的机会；直接暂停/小手数化账户受伤 flat-entry 路线停止，不再扫 DD/loss_streak 阈值。
- Stage031 已完成 Stage029 信号日/成交日滞后匹配归因。修正 Stage030 同日 closed-lot 匹配过严的问题后，Stage006 实际打开的暂停事件 `2,587` 个，0-7 日内匹配 closed-lot `2,517` 个，匹配率 `97.2942%`；Stage006 滞后 realized PnL `60,296,707.20`，其中 `loss_streak_only` 为 `58,285,212.40`，candidate AI rank `1-9` 为 `60,903,256.60`，rank `>9` 为 `-838,450.20`。结论：账户受伤后的 loss_streak 状态不是天然坏集合，而是 C9 恢复右尾的重要来源；后续只能研究入场前可见、极低自由度、跨 source/年份/产品成立的高质量恢复标签，不能按产品、月份或局部右尾写豁免。
- Stage032 已完成 Stage031 恢复段高质量标签只读审计。Stage031 恢复集合 lot/event/PNL 为 `2,606` / `2,517` / `60,296,707.20`；`tag_entry_or_first_aligned` 覆盖 `1,150` 笔、PNL `60,459,052.40`、PNL 占比 `1.0027`、source 正率 `100%`；`rank_1_9_and_entry_or_first` PNL `58,108,480.00`、PNL 占比 `0.9637`。但纯入场前标签失败或样本不足：`preentry_core` PNL 仅 `3,113,093.60`，`OI confirm` PNL `-8,607,389.10`，full-market consensus top8 只有 `86` 笔、`2` 年。结论：下一步若进入真实引擎，只能验证“开仓日早段确认后加风险”，不能在下单前直接加风险，也不能按产品/日期/small consensus 样本交易化。
- Stage033 已完成 `AI rank 1-9 + entry_or_first_aligned + 25%` 非挤占风险释放只读代理。选中 `796` 笔，Stage013 realized PnL `14,242,295.90`，代理增量 PnL `3,560,573.98`；17 个半年起点收益全部改善，最大回撤 `16/17` 改善，80% 收益保留 vs Stage006/Stage013 均 `17/17`。但严格任意结束日 `>1` 年负窗口仅从 Stage013 `330,947` 降到 Stage033 `324,091`，最差仍 `-42.3664%`，不达标。结论：早段质量标签有信息量，但不能单独修复左尾，不进入真实加仓引擎；下一步转账户级 selector、可交易早段执行约束或外生信息源。
- Stage034 已完成 Stage033 剩余负窗口前置信号归因。Stage033 口径可审计起点 `13,267` 个，其中未来任意 `>1` 年负起点 `2,122` 个、负率 `15.9946%`，最差 `-42.3664%`；最强条件仍是 `market_high_vol_high_eff`，样本 `946`、负起点率 `66.2791%`、lift `4.1438`，最强分桶同为 `joint_regime=high_vol_high_eff`。结论：Stage033 后剩余左尾仍由同一坏环境前兆主导，但 Stage024/025/026 已反证单一 hard gate；下一步不能重复 high_vol_high_eff 暂停，而要拆分该 regime 内右尾错杀与真坏窗口，或转外生信息源。
- Stage035 已完成 `high_vol_high_eff` 内部右尾/坏窗口拆解。该 regime 共 `946` 个起点，严格负 `627`、负率 `66.2791%`，严重负 `541`、最差 `-42.3664%`。坏窗口更像“前期账户/holding PnL 已大幅扩张后的过热回吐”：负窗口 `63d` 收益中位 `26.0295%`、`63d` holding PnL 中位 `471,065`，非负窗口分别为 `11.1204%`、`32,510`；最强坏窗口条件 `overheat_63d_gt20_consensus_1_3` 样本 `124`、负率 `100%`。恢复右尾则常出现在已深度受伤后：`recovery_dd_le_-30` 样本 `81`、严格负率 `0%`。决策 `stage035_internal_overheat_vs_recovery_split_found_needs_engine_validation`，下一步只能做冻结真实引擎验证：抑制过热回吐，同时显式保护深回撤恢复右尾。
- Stage036 已完成 Stage013 母本 + `high_vol_high_eff` 过热抑制 + 恢复右尾保护真实引擎验证。规则只在 `63d>20%` 且 AI consensus top8 count 为 `1-3` 的过热状态，把新 `flat_entry` cap 到 `1` 手，并显式豁免 `dd>=30%` 或 `63d<=-20%` 的恢复状态。结果不达标且不晋级：严格任意 `>1` 年负窗口 `515,884/7,215,647`，严格最差 `-44.3589%`，80% 收益保留 `16/17`，最差最大回撤 `-44.3589%`；事件仅 `10` 个，全部为 `2022-08-31` 的 `SA.CZCE` long，减少手数 `1,160`。短期确实减少 SA 亏损，但扰动 loss_streak/权益路径后使 `2021-01` 起点终点少约 `792,300`，说明该过热 cap 形状脆弱，停止继续扫阈值或手数。
- Stage037 已完成当前重建 C9 外生/AI 特征资格审计，决策 `stage037_feature_eligibility_audit_complete_no_trade_rule`。本阶段不做收益回测、不改正式配置、不连接 CTP、不调用订单 API。结论：可进入下一步候选级 PIT 只读预测力审计的历史字段只有 `stage182_ai_pool_rank_score`、`oi_price_confirm_fields`、`account_state_fields`；`pairwise_selection_features` 只在 `7.2016%` 候选、`18.82%` opened 候选上启用，降为 forward/观察层；`post_entry_first_minute_quality` 在当前 Stage036 候选流里只是 schema 残留、active ratio `0`，不能用于入场前 AI 选品；basis/inventory/sentiment 外生 ledger 只有 `3` runs / `2` received dates 且 history selector rows `0`，只能 forward monitor；`jd.DCE` full-market 月度证据为 `50` 行、AI top8 `11` 月、simple top8 `11` 月、consensus `2` 月且 60d PnL 合计 `-1,520`，仍不支持进入共享 AI 池。
- Stage038 已完成 Stage006 当前重建母本的候选级 PIT 特征矩阵与 OOS 预测力审计，决策 `stage038_has_preentry_oos_quality_candidates_needs_proxy_engine`。本阶段只读，不做收益回测、不改正式配置、不连接 CTP、不调用订单 API。样本为 `2020-01-02` 到 `2026-06-24` 的 `2,787` 个 opened flat-entry open-trade 聚合样本，full-market as-of 覆盖 `71.8335%`；稳定 OOS 条件为 `full_market_ai_top8`、`ai_rank_1_6`、`account_injured`。其中 `full_market_ai_top8` 最像入场前超高质量信号候选：`349` 笔、`17` 个 source、`5` 年、`14` 产品，total PnL `19,525,419.2`、mean PnL lift `2.4811`、OOS `3/3` fold 正收益、最小 fold PnL `1,351,300`。`ai_rank_1_6` 是宽 AI 质量层，`account_injured` 更像恢复状态线索而不是质量信号。OI 确认在信号日到成交日 `<=7` 天 as-of 回填后有 `1,036` 样本，但 total PnL `-10,862,524`、OOS `1/4` fold 正收益，不应作为加风险条件。下一步只允许冻结一个低自由度 Stage039 proxy，不允许扫 rank/topN/阈值。
- Stage039 已完成 `full_market_ai_top8 + 25%` 非挤占风险 closed-lot 只读 proxy，决策 `stage039_full_market_ai_top8_proxy_not_enough_no_param_rescue`。本阶段不是真实组合引擎、不改正式配置、不连接 CTP、不调用订单 API。结果：选中 `399` lots，selected realized PnL `15,805,148.2`，proxy delta `3,951,287.05`；17 个半年起点收益全部改善、80% 收益保留 vs Stage006/Stage013 均 `17/17`，到 `2026-06-30` 口径负窗口 `0` 且最差收益从 Stage013 `26.6753%` 提到 `34.2414%`。但严格任意 `>1` 年负窗口从 Stage013 `330,947/7,215,647` 恶化到 `332,446/7,215,647`，严格最差收益从 `-43.7940%` 恶化到 `-44.1402%`；`2022-07` 起点最大回撤也从 `-43.7940%` 恶化到 `-44.1402%`。结论：`full_market_ai_top8` 能加右尾和终点收益，但不是左尾解法，不继续救 topN/simple/OI/倍率。
- Stage040 已完成 Stage039 负窗口迁移与 delta 归因，决策 `stage040_stage039_added_negatives_mixed_with_denominator_effect`。严格窗口总数 `7,215,647`；Stage013 负窗口 `330,947`，Stage039 负窗口 `332,446`，净增 `1,499`。Stage039 修复 Stage013 负窗口 `6,905`，新增负窗口 `8,404`；新增负窗口里 `7,731` 个即 `91.9919%` 是 Stage039 绝对期末权益仍不低于 Stage013 的分母效应，绝对期末权益更低的真实 delta 变差窗口 `673` 个。结论：Stage039 不是大面积真实恶化，但也没有解决核心 common-negative 左尾；不能据此按产品/日期救参。
- Stage041 已完成关键日期独立日级冷启动探针，决策 `stage041_selected_daily_cold_start_confirms_left_tail_not_only_subwindow_artifact`。从 Stage040 最差窗口选 `8` 个具体起点真实跑 Stage013 到 `2026-06-30`，全部 `8/8` 起点仍存在 `>366` 天负结束日；Stage013 探针最差 `-31.0300%`、到终点最差 `63.1754%`，Stage041 `full_market_ai_top8 +25%` proxy 最差 `-30.7717%`、到终点最差 `72.7442%`。结论：严格左尾不是半年曲线子窗口的纯审计假象；AI top8 只轻微缓冲，远未达成“任意日级起点一年以上正收益”。
- Stage042 已完成扩展独立日级冷启动探针，决策 `stage042_expanded_probe_confirms_left_tail_persistent_not_ai_top8_solved`。从 Stage040 窗口按 `both_negative / added_negative_absolute_worse / added_negative_denominator / fixed_by_stage039` 分层选 `32` 个具体日级起点，真实跑 Stage013 到 `2026-06-30` 并叠加 Stage039 `full_market_ai_top8 +25%` closed-lot proxy；Stage013 与 Stage042 proxy 均 `32/32` 起点存在 `>366` 天负结束日。Stage013 探针最差 `-36.5967%`、到终点最差 `55.0954%`；Stage042 proxy 最差 `-36.4775%`、到终点最差 `64.2809%`，但最差最大回撤从 `-45.8976%` 到 `-46.2637%` 略恶化。结论：AI top8 能抬终点和右尾，但扩展日级样本仍确认左尾持续，不是目标解法。
- Stage043 已完成现金收益账户外层可行性审计，决策 `stage043_cash_yield_overlay_not_enough_requires_new_exogenous_source`。只读复用 Stage042 的 `32` 个日级冷启动曲线，按 `overlay_equity = strategy_equity + 150000 * annual_yield * elapsed_days / 365` 测试账户外层现金收益，不改交易信号、不改持仓路径、不连接 CTP、不调用订单 API。结果：Stage042 proxy 要把全部 `>365` 天窗口打到非负，所需简单年化现金收益率为 `34.2296%`；固定 `8%` 年化仍有 `4,847` 个负结束日，固定 `20%` 年化仍有 `634` 个负结束日，只有 `40%` 年化才清零。结论：普通现金管理/备用金利息不是足够强的左尾解法，账户外层普通收益路线不作为主解继续。
- Stage044 已完成外生数据源资格库存审计，决策 `stage044_external_inventory_found_basis_warehouse_need_pit_no_trade_rule`。只审计本地已存在外生缓存，不写交易规则、不改官方 C9、不连接 CTP、不调用订单 API。当前能作为下一步历史候选但必须先补 PIT 规则的是 `domestic_basis_history_backfill` 与 `domestic_warehouse_receipt_history`，覆盖 `2020-01-02` 到 `2026-04-17` 且覆盖 `2022-2023` 左尾；`domestic_member_rank_history` 从 `2023-01-03` 才开始，`external_forward_ledger` 只有 `2026-05-29` 到 `2026-06-03`，`cftc_cot_disaggregated_weekly` 需要跨市场映射，均不能直接进入历史 selector。结论：下一步只允许做 `basis/warehouse` 的 T+1/PIT 点时化与坏窗口覆盖归因，不能把 backfill 外生数据直接塞入 AI 选品。
- Stage045 已完成 `basis/warehouse` 的 T+1/PIT 候选级外生特征审计，决策 `stage045_external_pit_candidate_found_requires_proxy_engine`。本阶段只读复用 Stage038 的 `2,787` 个 opened flat-entry 样本，每条外生数据只允许在 `data_date+1` 后使用，分位数为单品种 expanding percentile 且至少 `60` 个历史观测后输出；不写交易规则、不改官方 C9、不连接 CTP、不调用订单 API。覆盖：basis `94.3667%`，warehouse `48.5468%`。稳定 OOS 候选为 `external_warehouse_build_20d` 与 `external_warehouse_high_p80`：前者样本 `764`、`16` 个 source、`6` 年、`14` 产品、total PnL `72,992,456.6`、mean PnL lift `4.2370`、`4/4` OOS fold 正；后者样本 `481`、`17` 个 source、`7` 年、`12` 产品、total PnL `35,785,955.0`、mean PnL lift `3.2995`、`4/4` OOS fold 正。结论：仓单外生状态有候选级信息量，但还不是组合目标证明；下一步只能冻结一个仓单条件做 proxy/真实路径验证，不得扫分位、品种、年份或方向。
- Stage046 已完成 `external_warehouse_build_20d + 25%` 非挤占风险 closed-lot proxy，决策 `stage046_warehouse_build_proxy_partially_improves_not_goal`。本阶段冻结 Stage045 第一候选，不扫 `20d`、分位、品种、年份、方向或倍率；不是真实组合引擎，不改官方 C9、不连接 CTP、不调用订单 API。选中 `863` lots，selected realized PnL `65,484,087.00`，proxy delta `16,371,021.75`；17 个半年起点收益改善/不变/变差为 `16/1/0`，最大回撤改善/不变/变差为 `13/2/2`，80% 收益保留 vs Stage006/Stage013 均 `17/17`。但严格任意 `>1` 年负窗口仍有 `305,804/7,215,647`，只从 Stage013 的 `330,947` 部分下降，最差收益为 `-44.4543%`，`2022-07` 起点最大回撤也较 Stage013 略恶化到 `-44.4543%`。结论：仓单 build 条件有收益和部分左尾信息量，但不能单独达成目标，不晋级真实规则、不触发 A/B；下一步只能做日级冷启动探针或真引擎验真，不得继续救仓单阈值/分位/产品/年份/方向。
- Stage047 已完成 `external_warehouse_build_20d +25%` 在真实日级冷启动探针上的验证，决策 `stage047_warehouse_daily_probe_not_left_tail_solution_no_param_rescue`。本阶段从 Stage046 worst 与 Stage013 worst 中固定抽取 `32` 个压力日级起点，真实跑 Stage013 到 `2026-06-30`，再叠加同一个仓单 build closed-lot proxy；不改官方 C9、不连接 CTP、不调用订单 API。结果：Stage013 和 Stage047 均 `32/32` 起点存在一年以上负结束日；负结束窗口从 `7,864` 小降到 `7,780`，但最差收益从 `-36.5967%` 恶化到 `-40.7967%`，最差最大回撤从 `-37.7002%` 恶化到 `-40.7967%`。到终点最差收益从 `55.0954%` 提高到 `75.1229%`，说明它仍是终点/右尾收益辅助，但不是左尾主解。结论：停止围绕仓单 build 的窗口、分位、产品、年份、方向和倍率救参，不进入真实引擎。
- Stage048 已完成 CFTC COT 跨市场映射资格审计，决策 `stage048_cftc_cot_low_coverage_no_stable_oos_keep_readonly`。本阶段沿用旧线冻结映射 `CF/OI/lh/lc/au/cu/fu/hc/rb`、报告日后第 4 天 08:00 中国时间可见、`45` 天最大滞后、`156/52` 周 managed-money 方向一致公式；只读复用 Stage038 的 `2,787` 个 opened flat-entry 样本，不改官方 C9、不连接 CTP、不调用订单 API。结果：COT 命中 `1,117/2,787=40.0789%`，supportive 样本 `209`，稳定 OOS 候选为空；`cot_supportive` mean PnL lift 仅 `0.4552` 且 `4` 个 OOS fold 中 `3` 个为正但最小 fold PnL `-214,870`；`cot_direct_mapping` total PnL 为 `-1,436,890`。`MA/jm/ru/SM/FG/AP/SA/sp/SH/si` 等重要品种完全无 COT 命中。结论：COT 只能作为外盘温度背景，不能进入 AI 选品、开仓过滤、加减仓或真实引擎；停止 COT 规则搜索。
- Stage049 已完成逐合约 OI 迁移与主力映射 PIT 审计，决策 `stage049_contract_oi_migration_candidate_but_source_gap_keep_readonly`。本阶段用仓库内 `.vntrader/database.db` 的逐合约日线 `open_interest` 与 `tqsdk_all_futures_main_contract_mapping_2010_2026_04.csv`，按 `feature_date+1` 才可见的 T+1 规则，只读复用 Stage038 的 `2,787` 个 opened flat-entry 样本；不改官方 C9、不连接 CTP、不调用订单 API。结果：逐合约 OI 命中 `2,787/2,787=100.0000%`，但 Stage038 实际开仓合约 `100%` 与主力映射一致，所以“是否主力”本身没有增量；稳定 OOS 候选为 `contract_oi_share_ge50`、`contract_oi_top2_concentration_ge70`、`mapping_main_oi_share_ge40`，其中 `contract_oi_share_ge50` 样本 `2,051`、total PnL `77,671,146.2`、mean PnL lift `1.6795`、`4/4` OOS fold 正、最小 fold PnL `3,440,471.6`。但目标扩展品种 `jd.DCE` 的逐合约 OI 只到 `2026-03-26`、主力映射只到 `2026-04-30`，未覆盖 `2026-06-30` 目标终点；因此 Stage049 只能保留为候选信息源，不能直接接入 AI 选品、开仓过滤或加风险。
- Stage050 已完成 `jd.DCE` 逐合约 OI/主力映射缺口的线内数据修复，决策 `stage050_jd_contract_oi_source_gap_repaired_line_local`。本阶段通过 TqSdk 只拉取并合并本研究线内的 `KQ.m@DCE.jd` 主力映射与 `jd` 候选合约日线/OI，不修改共享 mapping CSV、不写 `.vntrader/database.db`、不改官方 C9、不连接 CTP、不调用订单 API。结果：修复后 combined mapping 覆盖到 `2026-06-30`，逐合约 bar 覆盖到 `2026-06-30`；`jd2604` 到 `jd2706` 等 `15` 个合约有有效拉取，过期的 `jd2601/2602/2603` 在修复窗口为空属预期。
- Stage051 已完成修复 `jd` 源后的逐合约 OI 迁移重跑，决策 `stage051_contract_oi_migration_source_gap_cleared_ready_for_proxy`。本阶段复用 Stage049 固定条件和 Stage050 线内修复包，不新增阈值、不写交易规则、不修改共享源。结果：entry 命中 `2,787/2,787=100.0000%`，`source_gap_products=[]`；`jd.DCE` 逐合约 OI 快照覆盖 `2019-12-02 -> 2026-06-30`，主力映射覆盖 `2010-01-04 -> 2026-06-30`；稳定 OOS 候选为 `contract_oi_share_ge50` 和 `contract_oi_top2_concentration_ge70`，其中 `contract_oi_share_ge50` 样本 `2,051`、total PnL `77,671,146.2`、mean PnL lift `1.6795`、`4/4` OOS fold 正、最小 fold PnL `3,440,471.6`。下一步只允许冻结一个低自由度 OI 集中度条件做 proxy/true-engine 验证，不能继续扫 OI 阈值。
- Stage052 已完成 `contract_oi_share_ge50 +25%` 非挤占风险 closed-lot proxy，决策 `stage052_contract_oi_share_proxy_partially_improves_not_goal`。本阶段冻结 Stage051 第一稳定条件和固定加风险比例，不扫 `0.33/0.50/0.70` 阈值，不改官方 C9、不连接 CTP、不调用订单 API。结果：选中 `1,728` lots，selected realized PnL `42,941,026.70`，proxy delta `10,735,256.68`；17 个半年起点收益改善/不变/变差为 `17/0/0`，最大回撤改善/不变/变差为 `14/0/3`，80% 收益保留 vs Stage006/Stage013 均 `17/17`。严格任意 `>1` 年负窗口从 Stage013 的 `330,947/7,215,647` 降到 `252,134/7,215,647`，最差收益从 `-43.7940%` 改善到 `-40.3699%`，但仍未清零；到 `2026-06-30` 口径负窗口 `0`，最差 `24.7554%`。结论：逐合约 OI 集中度有组合路径增益，但不是用户目标主解，不能接实盘；下一步只能做日级冷启动探针或真实引擎验真，不允许继续救 OI 阈值、品种、年份或方向。
- Stage053 已完成 `contract_oi_share_ge50 +25%` 的真实日级冷启动探针，决策 `stage053_contract_oi_daily_probe_not_left_tail_solution_no_param_rescue`。本阶段从 Stage052 worst 与 Stage013 worst 固定抽取 `32` 个压力日级起点，真实跑 Stage013 到 `2026-06-30`，再按开仓日/品种/具体合约重新绑定 Stage051 OI 快照并叠加 Stage052 固定 proxy；不改官方 C9、不连接 CTP、不调用订单 API。结果：Stage013 与 Stage053 均 `32/32` 个探针起点存在一年以上负结束日；Stage053 负结束窗口仅从 `8,191` 小降到 `8,162`，但最差收益从 `-36.5967%` 恶化到 `-43.8208%`，最差最大回撤从 `-37.7002%` 恶化到 `-45.3616%`；到 `2026-06-30` 最差收益从 `55.0954%` 提高到 `70.9263%`，最低期末权益从 `232,643.10` 提高到 `256,389.45`。结论：OI 份额能抬最终权益和部分右尾，但会放大左尾路径，不是目标主解；停止逐合约 OI 阈值、品种、年份、方向和倍率救参。
- Stage054 已完成 Stage053 最差日级窗口的只读路径归因，决策 `stage054_left_tail_mainly_opened_after_start_risk_budget_problem`。本阶段选 Stage013 与 Stage053 各 `8` 个最差窗口，重跑 `9` 个唯一日级起点拿 positions；Stage013 原曲线 vs 重跑 positions 校验最大差异 `7.105e-15`。结果：`16` 个窗口的亏损归因中，opened_after_start 亏损占比 `100.00%`、existing_at_window_start 亏损占比 `0.00%`；最差产品方向聚合为 `fu.SHFE long`、`SM.CZCE long`、`MA.CZCE short` 等，但这些只用于风险路径诊断，不得做品种/方向黑名单。结论：当前左尾更像窗口后新增风险暴露的预算释放问题，下一步应做因果风险预算或信号质量过滤，而不是继续调已有持仓止损或最差品种。
- Stage055 已完成 Stage054 左尾窗口后新开仓的入场前信号预算只读审计，决策 `stage055_has_new_entry_negative_conditions_need_true_budget_engine`。本阶段重跑 Stage054 去重后的 `9` 个日级起点，保存 trades/entry_risk/entry_candidates/closed_lots，并构建 `246` 条窗口后新开 flat_entry 的 PIT feature matrix。结果：窗口 entry realized PnL `-474,365`、loss_abs `576,250`；`selected_volume_gt1` 条件 `100` 笔贡献 `-474,110`，`normal_release_not_full_market_ai_top8` 完全重合且同为 `-474,110`；相对地 `full_market_ai_top8` 只有 `18` 笔但合计 `+4,845`。结论：值得冻结验证“非 full-market AI top8 时不释放 1 手以上预算”的真实引擎，但本阶段仍是 closed-lot 归因，不是上线规则。
- Stage056 已完成 full-market AI Top8 新开仓预算 cap 的 A/C 真引擎压力验证，决策 `stage056_pressure_not_enough_stop_no_param_rescue`。本阶段用 Stage054/055 去重后的 `9` 个压力日级起点，对比 A=Stage013 与 C=Stage013 + 非 PIT full-market AI Top8 新 `flat_entry` 最多 `1` 手；不改官方 C9、不连接 CTP、不调用订单 API。结果：Stage056 cap 事件 `852`、减少手数 `5,016`；期末收益最小从 Stage013 `55.0954%` 提高到 Stage056 `73.7259%`，最差最大回撤从 `-37.7002%` 改善到 `-18.9994%`，但严格任意 `>1` 年负窗口从 `81,351` 增到 `183,423`，80% 收益保留仅 `1/9`。结论：这个规则把风险砍得过宽，左尾深度变浅但负区间变多、右尾被明显错杀；停止扫 `TopN/1手/2手/品种` 救参。
- Stage057 已完成 Stage056 失败归因。修正 cap event 到 baseline lot 的信号日/开仓日匹配后，cap 事件 `852` 个、匹配 baseline lot `842` 个、减少手数 `5,016`；被 cap 掉的 baseline PnL proxy 合计 `+516,346.37`，其中少赚 `1,597,899.58`、少亏 `-1,081,553.21`；右尾错杀 source `8` 个、减亏有帮助 source `0`。结论：Stage056 不是“少调一个 TopN/手数参数”，而是粗硬门槛把连续风险预算问题切成一刀切，停止 `TopN/1手/品种` 救参。
- Stage058 已完成连续风险预算 proxy 审计，决策 `stage058_continuous_budget_proxy_no_variant_passed_keep_readonly`。本阶段只读复用 Stage038 全样本 `2,787` 笔和 Stage055 压力样本 `246` 笔，固定审计 `quality_linear_floor25/50`、`quality_recovery_floor75`、`quality_top8_recovery_floor`，只作用于 `1` 手以上预算。结果：无 variant 同时通过压力样本减亏和全样本 `80%` 收益保留；最强减亏 `quality_linear_floor25` 压力样本 delta `+132,501.90`，但全样本收益保留仅 `64.9748%`；收益保留最高 `quality_linear_floor50` 也只有 `76.6499%`，未达 `80%`，压力样本 delta `+88,334.60`。结论：固定连续预算 proxy 仍会明显压掉全样本右尾，不进入真引擎，不继续调 floor/分层救参。
- Stage059 已完成 Stage055 压力样本交易路径 MAE/MFE 归因，决策 `stage059_pressure_losses_mixed_path_keep_readonly`。本阶段只读复用 `246` 笔窗口后新开 flat_entry，realized PnL `-474,365.00`、loss_abs `576,250.00`；亏损路径不是单一形态：`late_adverse_no_edge` loss_abs 占比 `40.7844%`，`early_adverse_no_edge` 占比 `34.8633%`，`gave_back_favorable_excursion` 占比 `24.3523%`。MAE 时点上 `day1_3` 与 `day4_10` 分别贡献 `50.3479%` 与 `49.4612%` 的 loss_abs。结论：不能用一个统一止损、统一开仓确认或统一预算 cap 解决，下一步必须拆成独立路径假设，不按最差品种/方向/日期写规则。
- Stage060 已完成 `late_adverse_no_edge` 前置信号与右尾冲突审计，决策 `stage060_late_adverse_has_preentry_oi_candidate_needs_proxy`。目标形态 `35` 笔、realized PnL `-235,020.00`、loss_abs `235,020.00`；入场后路径事实 `path_no_1r_mae_day4_10` 能 `100%` 精确覆盖该形态，但不能用于开仓前预算。入场前条件中，`oi_confirmed` 捕获 late-adverse loss_abs `100%`，Stage038 全样本 PnL `-10,862,524.00`；`oi_and_selected_volume_gt1` 捕获 `97.2173%`，全样本 PnL `-10,803,980.00`。其他高捕获条件如非 full-market AI top8、rank4-9、selected_volume_gt1 在全样本均为正 PnL，属于右尾冲突。结论：下一步只允许冻结一个 OI-confirmed 风险预算 proxy 验证，不能直接改线上，也不能回到传统 OI 作为高质量加风险。
- Stage061 已完成 `oi_confirmed_cap_to_one` 反向风险预算 closed-lot proxy，决策 `stage061_oi_confirmed_reverse_budget_proxy_candidate_needs_daily_probe`。本阶段只读复用 Stage038 全样本、Stage055 压力样本和 Stage059 late-adverse lot paths；冻结规则为 `oi_confirmed=True 且 selected_volume>1` 时代理压到 `1` 手，不扫 OI 阈值、不改官方 C9、不连接 CTP、不调用订单 API。结果：全样本 original PnL `62,843,641.40`、candidate PnL `74,077,008.74`，收益保留 `117.8751%`；压力样本从 `-474,365.00` 改到 `-163,175.00`，delta `+311,190.00`、loss reduction `65.6014%`；late-adverse 目标从 `-235,020.00` 改到 `-53,420.00`，delta `+181,600.00`、loss reduction `77.2700%`。但这是线性代理，不是真实组合引擎，也未验证严格任意 `>1` 年窗口。
- Stage062 已完成 `oi_confirmed_cap_to_one` 反向预算的真实引擎压力验证，决策 `stage062_pressure_not_enough_stop_no_param_rescue`。A=`stage013_pressure_baseline`，C=`stage062_oi_confirmed_reverse_budget_cap`，样本为 Stage054/055 去重后的 `9` 个压力日级起点，结束 `2026-06-30`；规则冻结为真实计划中 `oi_price_confirm_passed/oi_confirmed=True` 且 `selected_volume>1` 的新 `flat_entry` 降到 `1` 手。结果：Stage013/Stage062 正收益起点均 `9/9`，Stage013 期末收益最小/中位 `55.0954%/112.1421%`，Stage062 提高到 `194.5521%/229.9321%`，80% 收益保留 `9/9`；但严格任意 `>1` 年负窗口从 `81,351` 恶化到 `214,406`，严格最差收益/最大回撤从 `-37.7002%` 恶化到 `-39.1198%`。cap 事件 `257`、减少手数 `1,441`。结论：Stage061 proxy 被真实引擎反证；终点收益更高不等于符合用户“任意一年以上周期正收益”的目标，停止 OI-confirmed 一手 cap 救参。
- Stage063 已完成 `early_adverse_no_edge` 前置信号与右尾冲突审计，决策 `stage063_early_adverse_no_clean_preentry_candidate_keep_readonly`。目标形态 `54` 笔、realized PnL `-200,900.00`、loss_abs `200,900.00`；入场后路径事实 `path_mfe_lt_1r_and_mae_day0_3` 能覆盖 `100%`，但不能用于开仓前预算。高捕获入场前条件均撞全样本右尾：`not_full_market_consensus_top8` 捕获 `100%` 但全样本 PnL `+50,248,642.20`，`not_full_market_ai_top8` 捕获 `99.1787%` 但全样本 PnL `+43,318,222.20`，`selected_volume_gt1` 捕获 `82.7725%` 但全样本 PnL `+62,650,005.40`。全样本为负的 `oi_confirmed/risk_multiplier_gt1` 只捕获 `49.8756%`。结论：early-adverse 没有干净入场前预算候选，不进入 proxy 或真引擎。
- Stage064 已完成 `gave_back_favorable_excursion` 前置信号与退出路径审计，决策 `stage064_giveback_no_clean_preentry_candidate_keep_exit_diagnostic`。目标形态 `100` 笔、realized PnL `-140,330.00`、loss_abs `140,330.00`；入场后路径事实 `path_mfe_ge1` 捕获 `100%`，`path_mfe_ge1_and_mae_ge1` 捕获 `98.7672%`，`path_mfe_before_mae` 捕获 `97.1852%`，但这些都是入场后 MFE/MAE 路径事实，不能提前用于开仓预算。高捕获入场前条件仍撞全样本右尾：`not_full_market_consensus_top8` 捕获 `100%` 但全样本 PnL `+50,248,642.20`，`ai_rank_1_9/risk_multiplier_ge1` 捕获 `100%` 且全样本 PnL 为正；全样本为负的 `oi_confirmed/risk_multiplier_gt1` 只捕获 `66.5146%`，`ai_rank_gt9` 捕获 `0%`。结论：giveback 没有干净入场前预算候选，只能保留为退出路径诊断或账户外层设计入口。
- Stage065 已完成全样本退出路径 proxy 审计，决策 `stage065_exit_proxy_has_candidate_needs_true_engine`。修复 Stage059 压力路径只有 `r_multiple_agg` 导致压力样本曾被判 invalid 的口径问题后，`optimistic_breakeven_after_1r` 成为优先候选：全样本收益保留 `146.7293%`，pressure delta `+140,330`，winner cut `0`，只命中 `gave_back_favorable_excursion` `100/100` 笔，不命中 early/late adverse；硬止盈 `1/2/8R` 虽可改善压力样本，但砍掉 `242-248` 个 big winner 或收益保留失败。结论：固定硬止盈方向停止；保本 proxy 有价值但只是 closed-lot 乐观上界，下一步只能做真实引擎验真，不允许扫 `1R/2R/4R`、锁盈档位、品种、年份或方向。
- Stage066 已完成 `+1R` 后保本 stop 的真实引擎压力验证，决策 `stage066_pressure_improves_left_tail_expand_validation`。A=`stage013_pressure_baseline`，C=`stage066_breakeven_after_1r`，样本为 Stage054/055 去重后的 `9` 个压力起点，结束日 `2026-06-30`；规则冻结为 layer 到达 `+1R` 后 stop 抬到原入场价，同日同时触发 `+1R` 与回踩入场价时不假设有利先后，延迟到下一根日 K。结果：Stage013/Stage066 正收益起点均 `9/9`，Stage013 期末收益最小/中位 `55.0954%/112.1421%`，Stage066 `102.4221%/104.3021%`；最差最大回撤从 `-37.7002%` 改到 `-36.5167%`；严格任意 `>1` 年负窗口从 `81,351/2,681,648` 降到 `69,937/2,681,648`，严格最差收益从 `-37.7002%` 改到 `-36.5167%`；80% 收益保留 `9/9`；保本事件 `1,124`，实际应用 `550`，同日歧义延迟 `574`。结论：有扩样本价值，但目标仍未达成，不能上线，也不能扫 `1R/2R/4R` 或锁盈档位救参。
- Stage067 已完成 Stage066 保本 stop 的扩展日级压力起点验证，决策 `stage067_expanded_not_enough_stop_no_param_rescue`。A=`stage013_expanded_baseline`，C=`stage067_breakeven_after_1r`，样本为 Stage042 的 `32` 个扩展日级起点，结束日 `2026-06-30`。结果：A/C 终点正收益均 `32/32`，但严格任意 `>1` 年负窗口从 A `682,970/9,468,577` 恶化到 C `769,537/9,468,577`，严格最差收益从 `-45.8976%` 恶化到 `-46.0359%`，80% 收益保留仅 `26/32`；2021-11-05 到 2021-11-12 六个起点收益保留仅 `69.1408%`。AI 审计正常，C 组 `2021-10` 到 `2026-06` 共 `57` 个月均启用 AI 池，候选 `18,113` 行，未启用月份 `0`。结论：Stage066 小样本改善不能外推，保本 stop 不能继续交易化。
- Stage068 已完成 AI 超高质量信号组合只读审计，决策 `stage068_has_new_composite_super_quality_candidate_needs_proxy`。本阶段复用 Stage038 的 `2,787` 个 opened flat-entry PIT 特征矩阵与 OOS fold，不改正式策略、不连接 CTP、不调用订单 API、不触发 A/B。通过门槛的 new composite 候选为 `full_market_ai_top8_and_account_injured`、`full_market_ai_top8_and_ai_rank_1_6`、`full_market_ai_top8_and_active_positions_lt3`；最佳候选 `full_market_ai_top8_and_account_injured` 样本 `213`、`17` source、`5` 年、`12` 产品，total PnL `19,224,379.20`，mean PnL lift `4.0027`，OOS `3/3` fold 全正、最小 fold PnL `603,400`，观察年份 `5/5` 为正，最差年 PnL `16,200`。但正收益 top10% 贡献占比 `72.2621%`，右尾集中度高；结论是有资格进入 Stage069 冻结 proxy/真引擎验真，不能直接加风险上线。
- Stage069 已完成 `full_market_ai_top8_and_account_injured +25%` 非挤占风险 closed-lot proxy，决策 `stage069_super_quality_proxy_partially_improves_not_goal`。A=`stage013_engine`，C=`stage013_engine + full_market_ai_top8_and_account_injured +25% proxy`；选中 `161` open trades/lots，selected realized PnL `2,150,098.20`，proxy delta `537,524.55`。17 个半年起点收益保留 vs Stage006/Stage013 均 `17/17`，收益改善/不变/变差 vs Stage013 为 `16/0/1`；但严格任意 `>1` 年负窗口只从 Stage013 `330,947` 降到 `330,030`，严格最差收益从 `-43.7940%` 恶化到 `-44.1402%`，最差窗口仍是 `2022-07-15 -> 2023-07-17`。结论：该最强均值组合有收益信息但不能作为目标主解，不进入真引擎。
- Stage070 已完成 Stage068 sibling composite 固定 `+25%` 非挤占风险面板，决策 `stage070_sibling_panel_partial_improvement_no_goal`。只比较 Stage068 已通过的 3 个 new composite，不新增阈值、不改风险比例、不加入 OI/selected_volume 诊断项。最佳为 `full_market_ai_top8_and_active_positions_lt3`：选中 `246` lots，selected realized PnL `3,351,448.20`，proxy delta `837,862.05`，收益改善 `17/17`，回撤改善 `14/17`，收益保留 `17/17`；严格任意 `>1` 年负窗口降至 `315,429`，但严格最差收益仍 `-44.1402%`，未达任意一年以上正收益目标。`full_market_ai_top8_and_ai_rank_1_6` 最差更差为 `-44.2073%`。结论：Stage068 这组三个 AI/account 加风险组合都不能继续调参救援；下一步转失败归因、账户外层或新 PIT 信息源。
- Stage071 已完成 Stage070 最佳 variant `full_market_ai_top8_and_active_positions_lt3` 剩余左尾只读归因，决策 `stage071_stage070_add_risk_low_coverage_remaining_tail_requires_defensive_or_new_pit`。固定取 Stage070 目标 variant 的 `32` 个最差窗口，不新增交易参数、不改 C9；窗口 entry `1,379` 行，base loss_abs `10,476,750.00`，其中 Stage070 selected loss_abs 仅 `846,940.00`、占 `8.0840%`，unselected loss_abs `9,629,810.00`、占 `91.9160%`；selected delta 为 `-101,561.25`，只相当于 base loss_abs 的 `0.9694%` 且方向为负。结论：Stage070 的高质量加风险形状没有覆盖剩余左尾主损失源，继续救 Stage068/070 composite 属于低价值调参；下一步转账户外层、防守型 PIT 预算或新信息源。
- Stage072 已完成 q90 realized-vol 账户 overlay proxy，决策 `stage072_q90_vol_overlay_partial_improvement_not_goal`。A=`stage013_engine`，B=`stage013_engine_q90_vol_overlay`，C0=`full_market_ai_top8_and_active_positions_lt3`，C=`full_market_ai_top8_and_active_positions_lt3_q90_vol_overlay`；固定 `63` 日波动率、expanding q90、floor `0.35`，不扫参数。C 严格任意 `>1` 年负窗口从 C0 `315,429` 降到 `304,177`，但严格最差收益仍 `-44.1402%`，到终点最小收益从 C0 `26.3113%` 降到 `23.9691%`，80% 收益保留 `17/17`。结论：账户外层 overlay 有轻微信息量，但不能触达最差窗口本体；不进入真引擎，不调 q80/q95/floor/lookback 救参。
- Stage073 已完成逐合约 front/next 期限结构 PIT 审计，决策 `stage073_term_structure_no_stable_oos_candidate_keep_readonly`。本地 SQLite concrete contract 日线可构造 `21,770` 条 T+1 快照，覆盖 `15` 个目标品种；Stage038 样本匹配 `2,133/2,787=76.5339%`，方向顺风样本 `1,015` 条、PnL `18,762,542.80`。但 stable OOS 候选数为 `0`，最佳诊断 `ai_oi_account_and_directional_carry_aligned` 样本仅 `52` 且 OOS `2/4` fold 为正。结论：期限结构数据资产可保留，但不进入 proxy/真引擎，不扫 front/next 阈值、percentile、month_gap 或品种方向救参。
- Stage074 已完成 cold-start capital ramp proxy 与绝对权益曲线补图，决策 `stage074_cold_start_ramp_proxy_not_goal_no_param_rescue`。A=`stage013_engine`，B=`stage013_engine_cold_start_ramp`，C0=`full_market_ai_top8_and_active_positions_lt3`，C=`full_market_ai_top8_and_active_positions_lt3_cold_start_ramp`；固定 `floor=0.35`、`252` 个交易日 ramp，不改交易信号。目标 C 严格任意 `>1` 年负窗口从 C0 `315,429` 小降到 `304,693`，但全周期期末收益最小从 `5.4611%` 降到 `0.6940%`，到终点最小收益从 `26.3113%` 降到 `13.5359%`，最差最大回撤从 `-44.1402%` 恶化到 `-46.5554%`。结论：线性冷启动 ramp 不是目标主解，不继续扫 floor/ramp_days；绝对权益图已输出到 Stage074 目录。
- Stage075 已完成 staggered sleeve deployment proxy 与绝对权益曲线，决策 `stage075_staggered_sleeve_proxy_not_goal_no_param_rescue`。A=`stage013_engine`，B=`stage013_engine_staggered_sleeve`，C0=`full_market_ai_top8_and_active_positions_lt3`，C=`full_market_ai_top8_and_active_positions_lt3_staggered_sleeve`；固定 `4` 个等权袖，按 `0/63/126/189` 交易日投入，不改交易信号。目标 C 严格任意 `>1` 年负窗口从 C0 `315,429` 恶化到 `325,602`，全周期期末收益最小从 `5.4611%` 降到 `-1.6714%`，到终点最小收益从 `26.3113%` 降到 `9.7091%`，最差最大回撤从 `-44.1402%` 恶化到 `-51.2562%`。结论：分批多袖冷启动部署比线性 ramp 更差，不继续扫 sleeve_count/offset。
- Stage076 已完成趋势广度/分散度 PIT 只读审计，决策 `stage076_trend_breadth_keep_readonly_no_trade_rule`。复用 Stage038 `2,787` 个 opened flat-entry 候选级样本与 Stage017 market daily summary，按 `date+1` T+1 asof、最大 `7` 天特征年龄拼接；market daily 覆盖 `2020-01-02 -> 2026-04-30`，样本命中 `2,719/2,787=97.5601%`，但 entry 到 `2026-06-24`，存在近期 market gap。raw OOS 条件仅 `breadth_low_or_narrow_chop` 通过 fold 门槛，样本 `759`、total PnL `34,008,085`、OOS `4/4` fold 正；但 robust 门显示最差年 PnL `-3,543,200`、负年份 `3` 个，最终 stable robust candidate `0`。结论：趋势广度/分散度有复盘信息，但不形成可交易化高质量加风险条件；不进入 proxy/真引擎，不按 `breadth_low/narrow_chop/broad_trend` 救参。下一步若继续该方向，应先补齐 market daily 到 `2026-06` 后复核，或转真正新 PIT 信息源。
- Stage077 已完成 `jd.DCE` 独立非挤占候选资格审计，决策 `stage077_jd_not_independent_candidate_keep_observe`。复用 Stage021 full-market 月度预测，不改共享 AI 池、不改线上、不连接 CTP、不调用订单 API；`jd` 月度样本 `50` 个，覆盖 `2022-01-28 -> 2026-02-27`，全部 60d future PnL 合计 `-4,620`。`jd_ai_top8_independent` 样本 `11`、total PnL `16,800`、最差年 `+2,380`，但 OOS fold 只有 `2/4` 为正且正收益 top5 贡献 `100%`；`jd_consensus_top8_independent` 只有 `2` 个样本且合计 `-1,520`。最终 independent candidate `0`。结论：当前不能把鸡蛋接入共享 AI rerank，也不应给独立历史预算；只保留数据资产和 forward watch，除非后续有新的 PIT 特征能证明材料性。
- Stage078 已完成防守型 PIT 字段族覆盖审计，决策 `stage078_no_unrefuted_defensive_pit_candidate_keep_searching`。复用 Stage038 全量候选级 PIT 特征矩阵和 Stage071 剩余左尾窗口 entry，不改共享 AI 池、不改线上、不连接 CTP、不调用订单 API。门槛为全样本 count/year、剩余左尾 loss_abs 覆盖、全样本 PnL 非正、OOS 正 fold 少、右尾碰撞低、且不是已反证条件。结果 candidate `0`。看似覆盖强的条件不能用：`drawdown_abs_ge10` 覆盖剩余左尾 `95.8678%`，但全样本 PnL `+31,050,600`、OOS `4/4` 正；`not_full_market_ai_top8` 覆盖 `91.9160%` 但全样本 `+43,318,222` 且 Stage056 已反证；`selected_volume_gt1` 覆盖 `91.8787%` 但全样本 `+62,650,005` 且 Stage056 已反证；`oi_confirmed/risk_multiplier_gt1` 全样本为负且覆盖约 `80%`，但 Stage062/058 已真引擎反证。结论：当前 Stage038/Stage071 已知字段族没有新的未反证防守候选；下一步应停止用这些字段救参，转真正新 PIT 信息源或结构不同的账户外层。
- Stage079 已完成国内会员持仓/成交排名 PIT 覆盖审计，决策 `stage079_member_rank_not_history_selector_missing_left_tail`。复用本地 `external_domestic_member_rank_cache/member_rank_sum_daily_20230101_20260417.csv`、Stage038 全候选和 Stage071 剩余左尾窗口，按会员排名 `T+1` 可见、最大旧值 `7` 天做 as-of 连接；不写交易规则、不改线上、不改 AI 池、不连接 CTP、不调用订单 API。源覆盖为 `2023-01-03 -> 2026-04-17`，可见日期 `2023-01-04 -> 2026-04-18`，`15` 个品种。Stage038 全样本覆盖 `1251/2787=44.8870%`；Stage071 左尾窗口 entry 覆盖 `652/1314=49.6195%`，但左尾亏损金额只覆盖 `1,042,730/10,154,100=10.2691%`。结论：会员排名这份历史源缺 2022 关键左尾，不能作为当前历史目标的 selector 或规则输入；只保留 forward monitor，除非后续能补齐覆盖 2022-2023 的 PIT 历史。
- Stage080 已完成国内会员持仓排名 2022 非 DCE 补数可行性审计，决策 `stage080_member_rank_backfill_coverage_ready_for_signal_audit`。按 Stage071 左尾实际需要，仅用 AKShare `get_rank_sum_daily` 补 `2022-01-01 -> 2022-12-31` 的 `AP/AU/CF/CU/FG/FU/HC/MA/OI/RB/SA/SM/SP`，DCE 的 `JM/LH` 因接口不稳定先排除；不写交易规则、不改线上、不改 AI 池、不连接 CTP、不调用订单 API。12 个 fetch chunk 全部成功，抓取 `19,792` 行，合并后原始会员排名 `88,649` 行，覆盖日期扩展为 `2022-01-04 -> 2026-04-17`。Stage071 左尾 entry 覆盖从 `652/1314=49.6195%` 提到 `1142/1314=86.9102%`，左尾亏损金额覆盖从 `1,042,730/10,154,100=10.2691%` 提到 `8,833,530/10,154,100=86.9947%`；Stage038 全样本覆盖从 `44.8870%` 提到 `56.1895%`。结论：会员排名历史源已重新具备“进入信号方向/OOS 审计”的数据资格，但这不是信号有效性证明；下一步只能冻结补数源哈希并审计会员排名特征方向，不得直接写 selector/proxy/真引擎。
- Stage081 已完成国内会员排名特征方向/OOS 审计，决策 `stage081_member_rank_no_stable_signal_candidate_keep_readonly`。复用 Stage080 after-joined `2,787` 个 Stage038 opened flat-entry 样本，会员排名 T+1 as-of 可用 `1,566/2,787=56.1895%`；输入哈希已冻结：joined features `9fca7f6b4937e02c32248ef05ce48fde899038067577f415f98c503d67972640`、combined raw `9b22de83f4530859bcb440e016ab89017d5e453f1660dd4b841aa792ce43afbf`、combined features `8d374e2fb86c66aaea241ca8128675da2ad85534ce92025cfbee0e4d6d2f58c3`。预声明审计净持仓方向、净持仓变化方向、两者组合、turnover 四分位、与 AI top8/AI rank/account injured 的低自由度组合；stable candidate `0`。最接近的 `account_injured_and_member_position_flow_aligned` 样本 `301`、total PnL `8,149,483.6`、mean lift `1.2007`、OOS `3/3` fold 正且最小 fold `248,644.4`，但 `worst_year_pnl=-1,243,365.6`，年份稳定性失败。结论：Stage080 补数是数据资产，但简单会员排名净持仓/净变化信号不应进入 selector/proxy/真引擎。
- 当前不直接修改任何官方实盘策略逻辑，不接实盘；Stage056、Stage062、Stage067 均已被真引擎或扩样本反证，不进入正式候选。
- 下一步不要继续在同类 regime 分位、手数、单年阈值、品种、方向、日期、horizon、AI rank、pairwise rank、账户回撤阈值、loss_streak 阈值、传统 OI 作为高质量确认、逐合约 OI 阈值、full-market topN/simple 组合、鸡蛋共享 rerank/独立历史预算、非 full-market AI top8 一手 cap、OI-confirmed 一手 cap、early-adverse/giveback 高捕获前置过滤、仓单窗口/分位/产品/年份/方向、期限结构 front/next 阈值/percentile/month_gap、趋势广度 breadth bucket/regime、当前 Stage038/071 已知防守字段族、连续预算 floor/分层或倍率、统一早停/统一确认、固定硬止盈、保本/锁盈阈值、Stage068 AI/account composite 加风险、q90 overlay、cold-start ramp floor/ramp_days、staggered sleeve_count/offset、会员排名净持仓/净变化方向或 turnover 分位上救参；后续转其他 PIT 源或结构不同、且不压低趋势右尾的账户外层。

## 后续规划

- Stage002：已完成当前重建版目标基准审计，确认年度正收益目标当前失败、`jd.DCE` 数据可用但未进入基准。
- Stage003：已完成年度负收益行归因，重点 `2022/2023/2026`，并确认鸡蛋共享 rerank/topN 方向历史上有强反证。
- Stage004：已完成历史旧 C9 minrisk/highquality 线与鸡蛋 AI 池历史中已经反证的形状，建立“禁止重复尝试清单”。
- Stage005：已完成冻结代理数据可行性审计，确认当前不能直接写 A/C 候选；旧 Stage016 质量标签只能作为 Stage006 特征蓝图。
- Stage006：已完成当前重建版逐笔结果 + entry/first-minute 标签绑定，确认复跑与 Stage167 基准一致；但单用 Stage861 分钟源覆盖不足。
- Stage007：已完成分钟源覆盖修复，确认 `stage152_complete` 可把首分钟覆盖修到 `97.3537%`，并得到更稳定的只读高质量标签候选。
- Stage008：已完成冻结只读代理，确认 `ai4_6_entry_or_first_aligned + 25%` 非挤占加风险上界有价值，但年度负收益行未改善且缺真实成交/保证金路径。
- Stage009：已完成新目标密集起点审计，确认“到 `2026-06-30`”口径运行曲线子周期无负收益，但“任意结束日、任意 `>1` 年周期”口径仍未达标。
- Stage010：已完成最差窗口归因，确认 Stage008 高质量加风险不是左尾主因；窗口已平仓 lot 净额无法解释账户权益损失，主问题转向日级 holding_pnl/持仓浮亏和账户层风险暴露。
- Stage011：已完成焦点窗口日级持仓/保证金/holding_pnl 拆解；左尾更偏新增/交易仓位主导，而不是纯已有仓位拖累。
- Stage012：已完成只读诊断新增/交易仓位入场前账户状态；确认坏窗口中正常风险入场整体失效，高质量 AI 标签也承压，下一步应研究账户状态下的风险释放纪律。
- Stage013：已完成账户状态小风险试探真实引擎候选；左尾显著改善但严格任意结束日 `>1` 年目标仍失败，保留为 Stage014 的归因基底，不接实盘。
- Stage014：已完成 Stage013 剩余负窗口失败地图；确认剩余失败集中在 `2022-2023` 的 `1-2` 年恢复段，且 Stage013 pilot gate 已触发但不足。
- Stage015：已完成 Stage013 触发后确认信号只读归因与 `jd.DCE` 非挤占候选可行性；确认后加仓和 jd 直接入池均不晋级，下一步转账户压力治理或 jd 非挤占小预算真实引擎验证。
- Stage016：已完成账户压力只读归因；高 broker10、active4 高位拥挤、同向相关均不能直接交易化，下一步转低自由度外生 regime/volatility 信号或非交易层生存线。
- Stage017：已完成外生 regime/volatility 只读归因；`high_vol_low_eff` 在曲线 forward、最差窗口前段、entry cash PnL、AI top8 未来 PnL 上同向偏弱，值得做 Stage018 冻结真实引擎验证，但不能直接作为上线规则。
- Stage018：已完成 high-vol/low-efficiency causal 小风险试探真实引擎；负窗口减少但收益保留失败，最差窗口未修复，不晋级，停止同类阈值/手数/窗口救参。
- Stage019：已完成 Stage018 触发事件右尾错杀归因；确认该路线应关闭，下一步转向不打断强趋势右尾的新信息源或选择器。
- Stage020：已完成 Stage013 + 高质量标签加风险只读代理；确认高质量标签可以抬收益和部分左尾，但严格 `>1` 年负窗口仍未清零，不晋级，停止通过继续加倍率或换标签组合救参。
- Stage021：已完成 full-market AI/simple 共识选择器与 jd 非挤占只读代理；确认共识选择器有收益增益但不能修复严格左尾，jd 共识月份不稳定且 2022H2 误判，不晋级，停止通过调 topN/比例或强行 jd 入池救参。
- Stage022：已完成账户层利润锁定/备用桶只读审计；确认该方向能减少部分负窗口和绝对缺口，但无法修复最差 `-42.0358%` 且收益保留失败，不晋级，停止通过锁盈阈值/比例救参。
- Stage023：已完成剩余负窗口前置信号归因；`high_vol_high_eff` 和 `loss_and_high_vol_low_eff` 是有信息量的坏环境线索，但集中在 `2021-2022`，不能直接上线。下一步若继续，应做真实引擎级暂停/恢复候选的 A/C 验证，而不是继续扫标签。
- Stage024：已完成因果 `high_vol_high_eff` 暂停新开仓真实引擎；严格负窗口从 Stage013 的 `330,947` 降到 `298,012`，且收益保留 `17/17`，但最差回撤/严格最差收益仍为 `-44.0955%`，目标失败，不晋级。下一步先归因右尾错杀和剩余左尾，不继续调 regime 分位或手数。
- Stage025：已完成 Stage024 暂停失败归因；确认 hard regime gate 的问题不是平均方向，而是混合了右尾恢复交易与假趋势，后续只允许先做入场前可见的恢复段右尾识别或 remaining worst-window holding_pnl 归因，不允许继续救参。
- Stage026：已完成恢复段右尾 episode 入场前识别只读归因；`ai_consensus_any` 虽然 lift 高，但只有 `18` 个 episode 且集中在 `2` 个日期，不满足跨 source/跨日期稳定性。hard regime gate 方向关闭，下一步转 Stage024 remaining worst-window 的 holding_pnl/positions 拆解或真正外生信息源。
- Stage027：已完成 Stage024 remaining worst-window 持仓路径归因；`50` 个代表失败窗口的 positions 到曲线误差 `5.8208e-11`，聚合净 PnL `-6,468,110`，holding PnL 占净损失 `94.8614%`，窗口后新开/交易仓位亏损占比 `88.9707%`。下一步转 Stage028 入场前账户状态/候选排序归因，不能把 `fu.SHFE long` 等最差品种方向黑名单化。
- Stage028：已完成入场前账户状态/候选排序只读归因；`250` 笔同 source opened flat_entry 中 `137` 笔落入代表坏窗口，基准率 `54.8000%`。稳定线索为 `drawdown_abs_ge30/ge20`、`loss_streak_ge3/ge2` 和 `drawdown_ge20_and_volume_gt1`，AI rank/pairwise rank 不构成强解释。下一步只允许一个冻结真实引擎版本验证账户受伤状态下的新开仓释放纪律，不允许扫阈值、品种、方向、日期或 AI topN。
- Stage029：已完成账户受伤状态暂停新 `flat_entry` 的冻结真实引擎；回撤压到 `-27.4723%`，但正收益起点只有 `9/17`、80% 收益保留 `1/17`、严格任意 `>1` 年负窗口仍 `230,729` 个，决策 `stage029_goal_not_met_not_promoted`。下一步只能先归因失败，不继续扫 DD/loss_streak 阈值或把暂停规则接线上。
- Stage030：已完成 Stage029 失败归因；暂停事件 `97.3858%` 匹配 Stage006 同日候选、`90.1708%` 匹配 Stage006 实际打开候选，说明直接暂停不是过滤无效候选，而是大面积关闭原 C9 机会。下一步停止账户受伤 flat-entry 暂停/小手数化路线，转不切断右尾的信息源或账户外层资金安排。
- Stage031：已完成 Stage029 信号日/成交日滞后匹配归因；0-7 日滞后口径下，Stage006 实际打开的暂停事件 `2,587` 个中 `2,517` 个匹配 closed-lot，Stage006 滞后 realized PnL `60,296,707.20`，`loss_streak_only` 贡献 `58,285,212.40`，candidate AI rank `1-9` 贡献 `60,903,256.60`，rank `>9` 为 `-838,450.20`。下一步只允许预声明高质量恢复标签只读验证，不能机械暂停账户受伤状态，也不能按产品/日期/局部右尾写豁免。
- Stage032：已完成恢复段高质量标签只读审计；`tag_entry_or_first_aligned` 覆盖 `1,150/2,606` 笔却贡献 `60,459,052.40`，`rank_1_9_and_entry_or_first` 贡献 `58,108,480.00`，未选中组仅 `2,188,227.20`；但纯入场前 `preentry_core` 和 `OI confirm` 不支持交易化，full-market consensus 样本太小。下一步只允许冻结验证开仓日早段确认后加风险真实引擎，不允许提前加仓、产品/日期豁免或继续扫标签组合。
- Stage033：已完成 `rank1-9 + entry_or_first_aligned + 25%` 非挤占风险释放只读代理；选中 `796` 笔、代理增量 PnL `3,560,573.98`，17 个起点收益全改善、80% 收益保留全通过，但严格任意 `>1` 年负窗口仍 `324,091/7,215,647`，最差 `-42.3664%`。下一步不要进入真实加仓引擎，转账户级 selector、可交易早段执行约束或外生信息源。
- Stage034：已完成 Stage033 remaining negative precursor attribution；`13,267` 个可审计起点中 `2,122` 个未来任意 `>1` 年曾为负，最强条件 `market_high_vol_high_eff` 负率 `66.2791%`、lift `4.1438`。结论是旧坏环境前兆仍然存在，但 hard gate 已经在 Stage024/025/026 失败，下一步只能拆分该 regime 内右尾错杀与真坏窗口。
- Stage035：已完成 `high_vol_high_eff` 内部拆解；负窗口更像过热回吐，典型条件 `63d>20% 且 consensus_1_3` 样本 `124`、负率 `100%`，而深回撤恢复条件 `dd<=-30` 样本 `81`、负率 `0%`。下一步只允许冻结真实引擎验证“过热抑制 + 恢复右尾保护”，不允许按 `2022`、月份、warmup、品种、方向或局部阈值继续拟合。
- Stage036：已完成“过热抑制 + 恢复右尾保护”冻结真实引擎验证；严格负窗口 `515,884`、严格最差 `-44.3589%`、80% 收益保留 `16/17`，事件仅 `10` 个且全部为 `SA.CZCE` long。该规则短期减少一笔 SA 亏损，但长期扰动账户路径并伤害 `2021-01` 起点，不晋级，停止过热 cap 阈值/手数救参。
- Stage037：已完成外生/AI 特征资格审计；可历史只读预测力审计的字段收缩为当前 C9 候选流中的 AI rank/score、OI确认、账户状态，pairwise 只观察，post-entry schema 不可用于入场前，外生 forward ledger 与 `jd.DCE` 均未达可交易化资格。下一步 Stage038 做候选级 PIT feature matrix，不写交易规则。
- Stage038：已完成候选级 PIT 特征矩阵与 OOS 审计；`full_market_ai_top8` 是当前最干净的入场前高质量候选，`ai_rank_1_6` 是宽 AI 质量层，`account_injured` 只作为恢复状态线索，OI 确认被反证。下一步 Stage039 只能冻结一个低自由度 proxy 做真实目标审计，不扫 topN/阈值。
- Stage039：已完成 `full_market_ai_top8 + 25%` 非挤占加风险 closed-lot proxy；终点收益和全起点收益都改善，但严格任意 `>1` 年负窗口增加、最差收益恶化，不达标且不继续救参。下一步只能归因为什么右尾增强不能修复左尾，或转新外生/账户外层。
- Stage040：已完成 Stage039 负窗口迁移与 delta 归因；净增负窗口 `1,499`，其中新增负窗口 `91.9919%` 是绝对权益不低于 Stage013 的分母效应，但 common-negative 左尾仍存在。下一步不能按 Stage039 top8 的亏损产品/日期救参。
- Stage041：已完成 `8` 个关键日期真实日级冷启动探针；Stage013 和 Stage041 proxy 均 `8/8` 起点存在一年以上负结束日，证明严格左尾不是纯子窗口审计假象。下一步应扩大真实日级冷启动样本，或转账户外层/新外生源。
- Stage042：已完成 `32` 个分层关键日期真实日级冷启动扩展探针；Stage013 和 Stage042 proxy 均 `32/32` 起点存在一年以上负结束日，Stage042 proxy 仅把探针最差收益从 `-36.5967%` 小幅缓冲到 `-36.4775%`，到终点最差从 `55.0954%` 抬到 `64.2809%`，但未减少负结束日且最差回撤略恶化。下一步应停止救 `full_market_ai_top8`，转账户外层/新外生源或更宽日级网格确认分布。
- Stage043：已完成现金收益账户外层可行性审计；Stage042 proxy 需要 `34.2296%` 简单年化现金收益才可清零全部一年以上负结束日，`8%` 年化仍有 `4,847` 个负结束日、`20%` 仍有 `634` 个。下一步停止把普通现金收益/备用金利息当主解，转真正新外生信息源或更宽日级网格确认坏窗口分布。
- Stage044：已完成外生数据源资格库存审计；`basis` 与 `warehouse_receipt` 覆盖 `2020-01-02` 到 `2026-04-17` 且覆盖 `2022-2023` 左尾，但还缺 PIT/T+1 证明；`member_rank` 缺 2022 左尾，`forward_ledger` 历史太短，`CFTC` 需要跨市场映射。下一步只允许先做 `basis/warehouse` 点时化与坏窗口覆盖归因，不直接写交易规则。
- Stage045：已完成 `basis/warehouse` T+1/PIT 候选级外生特征审计；basis 覆盖 `94.3667%`，warehouse 覆盖 `48.5468%`，固定仓单条件 `external_warehouse_build_20d` 与 `external_warehouse_high_p80` 通过当前 OOS 稳定候选门槛。下一步只允许冻结一个仓单条件做 proxy/真实路径验证，不扫 `20/80` 分位、产品、年份或方向。
- Stage046：已完成 `external_warehouse_build_20d +25%` closed-lot proxy；选中 `863` lots、proxy delta `16,371,021.75`，收益改善 `16/17`、80% 收益保留 `17/17`，但严格任意 `>1` 年负窗口仍 `305,804/7,215,647`，最差 `-44.4543%`，决策 `stage046_warehouse_build_proxy_partially_improves_not_goal`。下一步只能做日级冷启动探针或真引擎验真，不继续救仓单参数。
- Stage047：已完成仓单 build 条件日级冷启动探针；Stage013/Stage047 均 `32/32` 起点存在一年以上负结束日，Stage047 负窗口小降但最差收益/回撤恶化，决策 `stage047_warehouse_daily_probe_not_left_tail_solution_no_param_rescue`。下一步停止仓单 build 参数路线，转新外生源或真实路径归因。
- Stage048：已完成 CFTC COT 跨市场映射资格审计；COT 命中率 `40.0789%`、supportive 样本 `209`、稳定 OOS 候选为空，`MA/jm/ru/SM/FG/AP/SA/sp/SH/si` 等重建线重要品种完全无覆盖，决策 `stage048_cftc_cot_low_coverage_no_stable_oos_keep_readonly`。下一步停止 COT 规则搜索，转国内点时化外生源或真实路径归因。
- Stage049：已完成逐合约 OI 迁移与主力映射 PIT 审计；逐合约 OI 命中 `100%`，但实际开仓合约 `100%` 已是映射主力，“是否主力”无增量。稳定 OOS 候选为 `contract_oi_share_ge50`、`contract_oi_top2_concentration_ge70`、`mapping_main_oi_share_ge40`，决策 `stage049_contract_oi_migration_candidate_but_source_gap_keep_readonly`；因 `jd.DCE` 逐合约 OI 和主力映射未覆盖到 `2026-06-30`，下一步先修数据源缺口，再冻结一个低自由度 proxy/true-engine 验证。
- Stage050：已完成 `jd.DCE` 逐合约 OI/主力映射线内修复；TqSdk 修复包覆盖到 `2026-06-30`，共享 mapping CSV 与 SQLite 均未改，决策 `stage050_jd_contract_oi_source_gap_repaired_line_local`。
- Stage051：已完成修复源后的 Stage049 重跑；`source_gap_products=[]`、命中 `2,787/2,787=100%`，稳定 OOS 候选为 `contract_oi_share_ge50` 与 `contract_oi_top2_concentration_ge70`，决策 `stage051_contract_oi_migration_source_gap_cleared_ready_for_proxy`。下一步冻结一个低自由度 OI 集中度条件做 proxy/true-engine 验证，不扫阈值。
- Stage052：已完成 `contract_oi_share_ge50 +25%` closed-lot proxy；选中 `1,728` lots、proxy delta `10,735,256.68`，收益改善 `17/17`、80% 收益保留 `17/17`，严格 `>1` 年负窗口从 `330,947` 降到 `252,134`，最差从 `-43.7940%` 改善到 `-40.3699%`，但目标未达成，决策 `stage052_contract_oi_share_proxy_partially_improves_not_goal`。下一步只能做日级冷启动探针或真实引擎验真，不扫 OI 阈值。
- Stage053：已完成 `contract_oi_share_ge50 +25%` 日级冷启动探针；Stage013/Stage053 均 `32/32` 起点存在一年以上负结束日，负结束窗口从 `8,191` 小降到 `8,162`，但最差收益恶化到 `-43.8208%`、最差最大回撤恶化到 `-45.3616%`。结论：OI 份额只能作为右尾/终点收益辅助信息，不能作为左尾主解；停止 OI 阈值救参，下一步转真实路径归因或更强 PIT 信息源。
- Stage054：已完成 Stage053/Stage013 最差窗口只读路径归因；`16` 个最差窗口中 opened_after_start 亏损占比 `100.00%`，existing_at_window_start `0.00%`，positions 校验最大差异 `7.105e-15`，决策 `stage054_left_tail_mainly_opened_after_start_risk_budget_problem`。下一步转新增风险暴露的因果预算/信号质量过滤，不按最差品种、方向、日期做黑名单。
- Stage055：已完成 Stage054 新开仓入场前信号预算只读审计；窗口后新开 flat_entry `246` 条，realized PnL `-474,365`，`selected_volume_gt1` 与 `normal_release_not_full_market_ai_top8` 均贡献 `-474,110`，`full_market_ai_top8` 小样本为 `+4,845`。下一步可冻结一个真引擎验证“非 full-market AI top8 只给 1 手预算”，并严查收益保留和严格任意 `>1` 年窗口。
- Stage056：已完成“非 full-market AI top8 新 flat_entry 最多 1 手”的 A/C 真引擎压力验证；Stage056 期末收益最小和最大回撤改善，但严格负窗口从 Stage013 `81,351` 增到 `183,423`，80% 收益保留仅 `1/9`，决策 `stage056_pressure_not_enough_stop_no_param_rescue`。下一步停止 TopN/手数/品种救参，做失败归因或换更稳定的因果预算信号。
- Stage057：已完成 Stage056 失败归因；cap 事件 `852`、匹配 baseline lot `842`，被 cap 掉的 PnL proxy 净额 `+516,346.37`，其中少赚 `1,597,899.58`、少亏 `-1,081,553.21`，右尾错杀 source `8`、减亏有帮助 source `0`。结论是 hard cap 粗糙切掉右尾，不是 TopN 或一手参数可救；下一步只考虑连续预算/状态条件。
- Stage058：已完成连续风险预算 proxy 审计；固定四个预算形状均未同时满足压力减亏和全样本 `80%` 收益保留，最佳减亏版本全样本收益保留仅 `64.9748%`，最高收益保留版本也只有 `76.6499%`。结论：固定连续预算 proxy 不进入真引擎，停止 floor/分层救参；下一步转更强 PIT 信息源、真实路径归因或账户外层。
- Stage059：已完成 Stage055 压力样本交易路径 MAE/MFE 归因；`246` 笔窗口新开仓 realized PnL `-474,365`、loss_abs `576,250`，亏损由 late adverse `40.7844%`、early adverse `34.8633%`、giveback `24.3523%` 混合构成。结论：停止把压力窗口当成单一问题处理，下一步按路径形态拆假设，不直接写统一止损/确认/预算规则。
- Stage060：已完成 late-adverse 前置信号与右尾冲突审计；`oi_confirmed` 捕获 late-adverse loss_abs `100%` 且 Stage038 全样本 PnL `-10,862,524`，`oi_and_selected_volume_gt1` 捕获 `97.2173%` 且全样本 PnL `-10,803,980`；其他高捕获条件均正 PnL、撞右尾。结论：下一步可做冻结 OI-confirmed 反向预算 proxy，不直接上线。
- Stage061：已完成 `oi_confirmed_cap_to_one` 反向风险预算 proxy；全样本收益保留 `117.8751%`，压力样本 delta `+311,190`，late-adverse delta `+181,600`，proxy gate 通过。结论：该形状值得进入日级冷启动/真引擎探针，但当前仍不是上线规则；下一步必须验证严格任意 `>1` 年窗口、真实成交/保证金路径、成本压力和右尾错杀。
- Stage062：已完成 `oi_confirmed_cap_to_one` 真实引擎压力验证；终点收益和 80% 收益保留都改善，但严格任意 `>1` 年负窗口从 `81,351` 恶化到 `214,406`，最差收益/回撤恶化到 `-39.1198%`，决策 `stage062_pressure_not_enough_stop_no_param_rescue`。结论：Stage061 proxy 不能交易化，停止 OI-confirmed 一手 cap、阈值、手数、品种或方向救参。
- Stage063：已完成 early-adverse 前置信号与右尾冲突审计；目标 `54` 笔、loss_abs `200,900`，高捕获入场前条件均撞全样本右尾，负全样本 OI 条件捕获不足。结论：early-adverse 没有干净入场前预算候选，不进入 proxy/真引擎；下一步转 giveback 路径审计、强 PIT 信息源或账户外层设计。
- Stage064：已完成 giveback 前置信号与退出路径审计；目标 `100` 笔、loss_abs `140,330`，高捕获入场前条件仍撞全样本右尾，负全样本 OI 条件捕获 `66.5146%` 不足。结论：giveback 没有干净入场前预算候选，路径事实只能作为低自由度退出审计入口，不能直接止盈化。
- Stage065：已完成全样本退出路径 proxy 审计；`optimistic_breakeven_after_1r` 的 closed-lot 上界通过压力改善与右尾保留门槛，全样本收益保留 `146.7293%`、pressure delta `+140,330`、winner cut `0`，但它只解决 giveback，不解决 early/late adverse。下一步只能做真实引擎验真，确认真实分钟事件顺序、成本、保证金、严格任意 `>1` 年窗口和右尾错杀，不允许继续扫保本/锁盈阈值。
- Stage066：已完成 `+1R` 后保本 stop 的真实引擎压力验证；严格任意 `>1` 年负窗口从 Stage013 `81,351` 降到 Stage066 `69,937`，80% 收益保留 `9/9`，但负窗口未清零，决策 `stage066_pressure_improves_left_tail_expand_validation`。下一步只能扩样本验证，不允许把它当成已达标版本。
- Stage067：已完成 Stage066 保本 stop 的扩展日级压力起点验证；严格任意 `>1` 年负窗口从基准 `682,970` 恶化到 `769,537`，80% 收益保留仅 `26/32`，决策 `stage067_expanded_not_enough_stop_no_param_rescue`。结论：停止保本/锁盈阈值路线，转更强 PIT 信息源、账户外层或高质量信号识别。
- Stage068：已完成 AI 超高质量信号组合只读审计；new composite 通过 `3` 个，最佳 `full_market_ai_top8_and_account_injured` 为 `213` 笔、total PnL `19,224,379.20`、mean PnL lift `4.0027`、OOS `3/3` fold 全正、最差年 `+16,200`，但正收益 top10% 贡献 `72.2621%`，右尾集中度高。下一步只允许冻结一个 composite 做 Stage069 proxy/真引擎验真，不能直接加风险上线。
- Stage069：已完成最佳均值 composite `full_market_ai_top8_and_account_injured +25%` proxy；负窗口 `330,947 -> 330,030`，收益保留 `17/17`，但最差收益恶化到 `-44.1402%`，不进入真引擎。
- Stage070：已完成 Stage068 sibling composite 面板；最佳 `full_market_ai_top8_and_active_positions_lt3` 负窗口 `315,429`，收益/回撤路径好于 Stage069，但最差仍 `-44.1402%`，不达目标。结论：停止 Stage068 AI/account composite 加风险救参，转失败归因、账户外层或新 PIT 信息源。
- Stage071：已完成 Stage070 最佳 variant 剩余左尾归因；`32` 个最差窗口内 unselected loss_abs 占 `91.9160%`，Stage070 selected delta 为 `-101,561.25`，覆盖量仅 `0.9694%`。结论：继续调 Stage068/070 AI/account 加风险无价值，下一步转账户外层、防守型 PIT 预算或新信息源。
- Stage072：已完成 q90 realized-vol 账户 overlay proxy；C=`Stage070 最佳 + q90 overlay` 负窗口 `315,429 -> 304,177`，但最差仍 `-44.1402%`，到终点最小收益降到 `23.9691%`。结论：不达标，不继续调 overlay 分位/floor/lookback。
- Stage073：已完成逐合约 front/next 期限结构 PIT 审计；T+1 快照覆盖 `15` 个目标品种、Stage038 样本匹配 `76.5339%`，但 stable OOS 候选数为 `0`，最佳小样本诊断 OOS 仅 `2/4` fold 为正。结论：保留期限结构数据资产，不进入 proxy/真引擎，不继续扫期限结构参数。
- Stage074：已完成冷启动资本 ramp proxy 和绝对权益补图；C=`Stage070 最佳 + cold-start ramp` 负窗口 `315,429 -> 304,693`，但终点最小收益 `26.3113% -> 13.5359%`、最差最大回撤 `-44.1402% -> -46.5554%`。结论：线性 ramp 不是主解，不继续扫 floor/ramp_days；已输出全量绝对权益图和目标重点对比图。
- Stage075：已完成分批多袖部署 proxy 和绝对权益补图；C=`Stage070 最佳 + staggered sleeves` 负窗口 `315,429 -> 325,602`，终点最小收益 `26.3113% -> 9.7091%`、最差最大回撤 `-44.1402% -> -51.2562%`。结论：分批冷启动账户外层更差，停止 sleeve_count/offset 救参。
- Stage076：已完成趋势广度/分散度 PIT 只读审计；T+1 market breadth 命中 `97.5601%`，但 market daily 只到 `2026-04-30`，存在 `2026-05/06` 近期缺口。raw OOS 条件 `breadth_low_or_narrow_chop` 为正，但分年稳健性失败：最差年 PnL `-3,543,200`、负年份 `3` 个，robust candidate `0`。结论：保留广度审计资产，不进入 proxy/真引擎，不扫 breadth bucket/regime 救参。
- Stage077：已完成 `jd.DCE` 独立非挤占候选资格审计；`jd` full-market 月度样本 `50` 个，全部 60d future PnL 合计 `-4,620`，`jd_ai_top8_independent` 虽为正但 OOS fold 仅 `2/4`、右尾高度集中，`jd_consensus_top8_independent` 样本 `2` 且为负，independent candidate `0`。结论：鸡蛋当前不进共享 AI 池、不配独立历史预算，只保留 forward watch 或等待新 PIT 特征。
- Stage078：已完成防守型 PIT 字段族覆盖审计；当前 Stage038/071 已知字段族 candidate `0`。`drawdown_abs_ge10`、`not_full_market_simple_top8`、`full_market_probability_lt55` 等未反证条件覆盖左尾但全样本强正/右尾碰撞高；`not_full_market_ai_top8`、`selected_volume_gt1`、`oi_confirmed/risk_multiplier_gt1` 等覆盖强但已被 Stage056/062/058 反证。结论：停止用当前已知字段族救参，转新 PIT 信息源或结构不同的账户外层。
- Stage079：已完成国内会员持仓/成交排名 PIT 覆盖审计；源从 `2023-01-03` 开始，T+1 as-of 后 Stage038 全样本覆盖 `44.8870%`，Stage071 左尾 entry 覆盖 `49.6195%`，但左尾亏损金额覆盖仅 `10.2691%`。结论：这份会员排名历史源缺 2022 关键左尾，不能作为当前历史目标 selector，只保留 forward monitor 或等待补齐 PIT 历史。
- Stage080：已完成国内会员持仓排名 2022 非 DCE 补数可行性审计；12 个 AKShare chunk 全成功，补 `AP/AU/CF/CU/FG/FU/HC/MA/OI/RB/SA/SM/SP` 共 `19,792` 行，DCE `JM/LH` 暂排除。Stage071 左尾亏损金额覆盖从 `10.2691%` 提到 `86.9947%`，entry 覆盖从 `49.6195%` 提到 `86.9102%`，决策 `stage080_member_rank_backfill_coverage_ready_for_signal_audit`。结论：会员排名重新具备历史信号审计资格，但不能直接交易化；下一步只允许冻结补数源并做特征方向/OOS 审计。
- Stage081：已完成会员排名特征方向/OOS 审计；可用覆盖 `56.1895%`，stable candidate `0`。最接近条件 `account_injured_and_member_position_flow_aligned` 虽 OOS `3/3` fold 正、mean lift `1.2007`，但最差年 `-1,243,365.6`，不通过年份稳定性。结论：会员排名补数保留为数据资产和 forward monitor，不进入 proxy/真引擎，停止净持仓/净变化方向与 turnover 分位救参。
