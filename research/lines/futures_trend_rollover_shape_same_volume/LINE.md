# 主力合约换月形态确认与风控容量重开研究线

- line_id：`futures_trend_rollover_shape_same_volume`
- 当前状态：Stage018 已完成 N（多空对称严格三倍量 `×1.5`、严格半量以下 `×0.5`）完整周期门；N 相对 M 通过增量诊断，但相对正式 A/换月 C 的滑点为 `109.5635%/110.1694%`，超过105%，不进入多周期、不晋级
- 正式基线：`origin/master@8b3e534ae4336240157fdf69abae98599f0dbfd4`
- 正式策略：`official_live_stage847_c9_15w_stage819_05r_stop_retry_once`
- 工作区：`/Users/bytedance/Desktop/person/vnpy/.worktrees/rollover-shape-same-volume`
- 分支：`codex/rollover-shape-same-volume`

## 结构性假设

主力合约切换属于同一产品趋势仓位的合约迁移。可交易合约会变，但同一产品的趋势历史不应在换月时清零。旧主力平仓后，新主力只要在当前行情批次有可交易日 K 和完整合约元数据，就用截至当日的点时连续历史判断 MA5/10/20/40 与 MACD；形态同向时优先延续旧仓手数，硬风控容量不足则缩至最大允许整数手数，容量为零才保持空仓。

## 冻结合同

- 多头延续：`MA5 > MA10 > MA20 > MA40` 且 MACD histogram `> 0`。
- 空头延续：`MA5 < MA10 < MA20 < MA40` 且 MACD histogram `< 0`。
- Stage003 C 不要求新主力自身达到普通开仓的 `ArrayManager.inited=41`，也不要求任何目标合约 K 线根数；只要求新主力出现在当前行情批次、当日 OHLC 为正且自洽、成交量大于 0，并且 size/pricetick/margin ratio 完整为正。
- 指标历史使用旧合约截至换月时可见的真实历史，按 `新主力当日收盘/旧合约最近可见收盘` 做 backward-ratio 平移；旧合约同日有 bar 时替换同日末行，旧合约已提前停止时追加新主力当日真实 OHLCV/OI；连续来源仍须覆盖 MA40，不能读取未来数据。
- 当日可交易以新主力是否在当前 `bars` 批次且日期为批次最新日期判断，不要求旧合约最后日 K 同日存在。
- Stage003 当前候选手数采用 `shrink_to_allowed`：`min(旧仓实际剩余手数, 当前全部硬风控允许手数)`；不得超过旧仓，容量为零才不开仓。
- Stage001 历史对照仍可通过 `rollover_shape_volume_policy=exact_or_skip` 复现，不删除旧实验合同。
- Stage002 的 `target_contract_only` 仍作为可复现 B 对照；禁止直接拼接未复权旧新价格，避免换月价差伪造 MA/MACD。
- 只研究同方向换月延续，不新增反向开仓，不修改正式配置、实盘目录、CTP 或订单提交链路。

## Stage003 实验

- A：当前正式 C9/15万策略原样。
- B：A + 新主力自身至少 40 根 + `shrink_to_allowed`。
- C：A + 新主力仅当日可交易行情/元数据 + `backwards_ratio_continuous` + `shrink_to_allowed`。

## 预声明晋级门

- 必须无未来数据、无未复权跨合约拼接、无同日重复重开、无旧新合约同时持仓。
- C 的候选诊断数必须等于换月平仓数，必须真实存在 `target_count<40/source_count>=40` 样本，且这些样本不得再因目标合约历史不足跳过。
- B/C 必须满足 `final=min(previous, selected)`，且实际成交等于最终目标；当日行情或元数据缺失时 fail closed。
- 完整回测若启动，比较期末权益、总收益、最大回撤、Sharpe、滑点、交易次数、胜率、多起点和弱窗口；收益改善不能以明显恶化回撤或成本为代价。
- 失败后不扫描 MA 周期、MACD 参数、方向、品种、年份或手数比例救参。

## 当前判断

- Stage003 A/B/C：A `13,071,214.10/8614.1427%/-56.2069%/Sharpe1.3622`；B `13,492,951.90/8895.3013%/-57.2674%/Sharpe1.3631`；C `13,338,365.80/8792.2439%/-56.9876%/Sharpe1.3627`。
- C 相对 B：期末权益 `-154,586.10`、收益 `-103.0574pp`、最大回撤改善 `0.2797pp`、Sharpe `-0.0004`、滑点减少 `56,150`、交易增加 `14`；结构语义通过，但没有性能晋级证据。
- 换月事件：C `23` 次全部完成形态评估并成交，原手数 `20`、缩手 `3`、不开仓 `0`；其中 `5` 次目标合约只有 `1` 根而连续来源为 `41` 根，目标历史门槛绕过通过。
- 近期专项：`2026-08-18 si2609->si2611`、`2026-08-19 jm2609->jm2701` 的新主力都只有 `2` 根，当日行情/元数据完整，连续历史多头排布且 MACD 为正，均会进入续接路径。
- 过拟合风险：规则层面低、绩效结论层面中等。数据语义是结构修复，但只有 `23` 次换月和 `5` 次历史短样本，不能按当前收益继续挑复权方式或指标参数。
- 继续价值：工程与固定规则 forward shadow 有，主动参数优化价值低；不据当前结果扫描 MA、MACD、复权方式、品种、日期或手数比例。

## Stage004 正式晋级审计

- 用户明确授权直接晋升后，冻结 `2019-2025` 七个独立 start-year 和四个一年弱/近期窗口，以真引擎逐窗独立冷启动比较 A/C。
- C 的 start-year 收益胜率 `7/7`、收益差中位 `+27.2869pp`，但 `2022` 起点只多赚 `4.4100pp`，最大回撤从 `-39.9820%` 恶化到 `-46.0696%`，恶化 `6.0876pp`。
- `2018-01` 一年窗口收益从 `-3.9197%` 恶化到 `-10.0331%`，Sharpe 从 `-0.1592` 恶化到 `-0.5063`。
- 十一个窗口聚合滑点 C/A=`106.6559%`，超过预声明 `105%` 上限；DD、Sharpe、成本三项 gate 失败，最终 `promotion_pass=false`。
- 结论：用户授权不替代风险证据；不修改正式配置、不发布正式物料、不推送 master、不激活生产。

## Stage005 多周期资金曲线审计

- 在结果产生前提交冻结 1/2/3 年、每年 1 月和 6 月独立冷启动窗口及全部 gate；共 `43` 窗口、`86` 个 A/C 真引擎运行，临近完整窗口只观察不投票。
- 1 年 `15` 个完整窗口：收益胜率 `60.00%`、收益差中位 `+1.6267pp`、DD 非劣率 `93.33%`，但 Sharpe 非劣率仅 `66.67%`，失败。
- 2 年 `13` 个完整窗口：收益胜率 `53.85%`、收益差中位 `+0.7867pp`，但 DD/Sharpe 非劣率均 `76.92%`；`2020-06` 收益差 `-204.4657pp`、回撤恶化 `9.3533pp` 并新增 DD50 失败，失败。
- 3 年 `11` 个完整窗口全部聚合门通过，但不能覆盖 1/2 年失败；完整周期 C 虽多赚 `178.1011pp`，仍出现正式 A 没有的 broker100 失败。
- 决策：`confirm_do_not_promote_after_multicycle`；五张完整/逐周期/汇总资金曲线与原始曲线 CSV 一并保留，不改正式配置、物料、master 或生产。

## Stage006 30日方向一致风险1.2倍反证

- 在结果生成前以 `5da97a6a4` 冻结 A 正式、C 换月续仓、D=C+30日方向一致风险1.2倍；定义为信号日收盘相对向前第30个交易日收盘，多头 `>0`、空头 `<0`。
- 默认参数关闭；普通开仓、反手、换月续仓在风险手数取整前增强，三类加仓按隐含止损风险增强后向下取整，再经过原有全部硬闸门。
- D 的方向一致事件 `371/377=98.4085%`，说明规则与原趋势信号高度重叠，缺少风险分层选择力。
- D 相对 C 虽增加期末权益 `3,252,655.50`、收益 `2168.4370pp`、Sharpe `0.0116`，但最大回撤恶化 `4.9436pp`，滑点增至 C 的 `151.2563%`，broker10 峰值从 `100.4112%` 恶化到 `114.6031%`，超过100%天数从 `1` 增至 `4`。
- 回撤、滑点、broker 三个预声明门失败，决策 `stop_directional_30d_risk_boost_after_full_period`；不运行多周期，不扫回看周期和倍率救参。

## Stage007 A/C/D 多周期诊断

- 用户在 Stage006 最小门失败后明确要求补跑多周期；结果前以 `a9150bba5` 冻结 Stage005 固定的 1/2/3 年、每年 1 月和 6 月起点、临近完整窗口只观察以及 A/C/D 全部门槛。
- 共 `43` 个窗口、`129` 次真引擎独立运行；1/2/3 年完整窗口分别 `15/13/11` 个，并保留每档 `1` 个临近完整终端窗口只观察。
- D 相对 C 的 1/2/3 年收益胜率 `66.67%/61.54%/72.73%`，收益差中位 `+4.3133/+5.5733/+66.5117pp`；说明收益放大跨多个窗口存在。
- 代价同样跨周期：D 相对 C 的聚合滑点比 `113.11%/118.07%/121.89%`，3年 DD 非劣率仅 `45.45%`；D 相对 A 的对应滑点比 `115.13%/118.03%/123.42%`，3年 DD 非劣率仅 `36.36%`。
- 完整周期 D 为 `16,591,021.30/10960.6809%/-61.9312%/Sharpe1.3743`，滑点 `2,294,860`，broker10 峰值 `114.6031%`、超100% `4` 天；DD、成本、broker 三门失败。
- 决策 `confirm_directional_boost_not_promotable_after_multicycle`；五张完整/1年/2年/3年/汇总资金曲线和全部原始 CSV 固化，不修改正式配置、物料、master 或生产。

## Stage008 方向与量能联合确认多周期反证

- 结果前以 `ab4d427ec6` 固定 F：30 日价格方向同向，且含信号日的 `T-9..T` 成交量总和严格大于 `T-19..T-10` 时风险金额乘 `1.2`；其他为 `1.0`，无反向 `0.8`。
- 共 `43` 个窗口、A/C/D/F `172` 次真引擎；A/C/D 的全部同窗关键指标和资金曲线与 Stage007 逐值一致。
- F 的 `377` 条诊断中价格同向 `371`，量能确认增强 `214`；排除 `30` 笔日内 retry 后，真实初始 OPEN `375` 笔，价格同向 `369=98.40%`、量能增强 `213=56.80%`。换月重开仅 `6/23` 增强，没有换月量迁移触发集中。
- 完整周期 F 为 `16,361,436.10/10807.6241%/-61.9911%/Sharpe1.3893`，滑点 `2,053,480`，broker10 峰值 `102.5885%`、超100% `2` 天；相对 D 成本与 broker 明显改善，但相对 C 回撤恶化 `5.0035pp`、滑点为 `135.35%`，broker 仍恶化。
- F 相对 C 的 1/2/3 年收益胜率 `60.00%/53.85%/63.64%`，但滑点比 `109.89%/114.44%/117.28%`，2/3 年 DD 非劣率仅 `61.54%/54.55%`；相对 A 的 3 年 DD 非劣率仅 `45.45%`。
- 决策 `volume_confirmed_boost_not_promotable`；关闭 `30日同向 + 10/10日量增 + 1.2倍` 历史优化，不扫成交量窗、阈值、倍率、品种、方向、年份或起点。

## Stage010 成交量翻倍确认全周期反证

- 结果前以 `7492d06ac` 冻结 H：30日价格方向同向，且含信号日最近10日成交量总和严格大于再前10日两倍时风险金额乘 `1.2`；默认阈值仍为 `1.0`，不改变正式策略或既有 A/C/D/F。
- A/C/F 的全周期指标和曲线与 Stage008 逐值一致；H 为 `15,553,660.10/10269.1067%/-60.3470%/Sharpe1.4102`，滑点 `1,701,240`、交易 `823`、胜率 `52.2312%`、broker10 `95.2240%`、超100% `0` 天。
- H 诊断 `375` 条，价格同向 `369`、量能翻倍增强 `47=12.7371%`；实际初始 OPEN `373` 笔，增强 `47=12.6005%`。普通开仓增强 `47/352`，换月重开 `0/23`。
- H 相对 A/C 收益与 Sharpe 提高，但回撤分别恶化 `4.1400pp/3.3593pp`，滑点分别为 `111.5136%/112.1302%`；A_vs_H 另有 broker 峰值恶化，两个预声明晋级对照均失败。
- 决策 `stop_double_volume_boost_after_full_period`；不运行多周期、不扫其他成交量比例救参、不修改正式配置、物料、master 或生产。

## Stage013 仅多头非对称风险多周期反证

- 结果前以 `aac9aed1c` 冻结 J：仅多头参与双倍量能确认的 `1.5/0.5` 风险分配，空头在历史检查前旁路为 `1.0`；A/C/H复用Stage011的129次真引擎，I/J各新跑43窗，共215臂窗。
- J 完整周期为 `6,948,829.10/4532.5527%/-46.1710%/Sharpe1.3130`，滑点 `734,090`、交易 `796`、broker10峰值 `82.3796%`；相对I收益增加 `2100.1813pp`，证明空头旁路能恢复部分复利。
- 但J相对A/C收益只保留 `52.6176%/51.5517%`，Sharpe分别下降 `0.049254/0.049693`；A_vs_J与C_vs_J的9个1/2/3年×combined/1月/6月门全部失败。
- J的364条诊断为多头确认40、多头未确认251、空头旁路73；实际初始OPEN也是364，全部可确定性绑定。39/39完整滚动窗口生存，最低权益 `103,450`。
- 决策 `reject_long_only_asymmetric_after_multicycle`；方向归因有价值，但不是可晋级的新信息源，关闭成交量/方向风险倍率路线，不扫方向、倍率、品种、年份或起点。

## Stage014 仅多头命中加风险全周期反证

- 结果前以 `193728cfd` 冻结K：多头30日同向且最近10日成交量严格大于前10日两倍时风险 `×1.5`，其他多头和全部空头 `×1.0`；A/C/H/J复用Stage013，仅K新跑一次全周期。
- K为 `13,164,443.80/8676.2959%/-60.5299%/Sharpe1.3629`，滑点 `1,665,810`、交易 `825`、broker10峰值 `98.3590%`。
- K相对A只多赚 `62.1531pp`，但回撤恶化 `4.3230pp`、滑点为 `109.1912%`、broker峰值恶化 `6.8640pp`；相对C少赚 `115.9480pp`、回撤恶化 `3.5423pp`、滑点为 `109.7950%`。
- 375条诊断为多头命中39、多头未命中262、空头旁路74；实际初始OPEN 373，39条增强全部成交。J的收益损失主要来自未命中半风险，但恢复仓位只是恢复风险暴露。
- 决策 `stop_long_only_confirmation_only_after_full_period`；A/C双晋级门失败，不跑多周期，关闭方向/量能倍率历史优化。

## Stage015 仅多头三倍量能命中加风险全周期反证

- 结果前以 `369cb650a` 冻结L：仅多头30日同向且最近10日成交量严格大于前10日三倍时风险 `×1.5`，其他多头和全部空头 `×1.0`；A/C/H/K复用Stage014，仅L新跑一次全周期。
- L为 `14,442,341.80/9528.2279%/-56.5393%/Sharpe1.3743`，滑点 `1,684,490`、交易 `827`、broker10峰值 `98.8823%`。
- L相对A/C收益分别增加 `914.0851/735.9840pp`，回撤只恶化A `0.3324pp`并改善C `0.4483pp`，Sharpe也均提高；但滑点仍为 `110.4156%/111.0262%`，A侧broker峰值另恶化 `7.3873pp`。
- 376条诊断为多头命中17、多头未命中285、空头旁路74；实际初始OPEN 374，17条增强全部成交。严格三倍只留下小样本，改善不能覆盖双基线成本门失败。
- 决策 `stop_long_only_triple_volume_after_full_period`；不跑多周期，不扫 `2.5/3.5/4.0`、风险倍率、方向、品种、年份或起点，方向/量能风险倍率历史优化继续关闭。

## Stage016 仅多头三倍量能多周期诊断

- 用户在 Stage015 全周期失败后明确要求补跑；结果前以 `62ba833fa` 冻结43窗口、A/C/K/L、1/2/3年每年1月与6月起点和固定五图。A/C复用Stage013的86个臂窗，K/L新跑86次真引擎。
- L相对正式A的1/2/3年combined收益胜率 `66.67%/61.54%/72.73%`，收益差中位 `+2.2167/+1.0100/+24.9171pp`；但Sharpe非劣率 `73.33%/76.92%/81.82%`，DD非劣率 `93.33%/76.92%/63.64%`，3年滑点比 `105.96%`，9个周期×起点门只通过1个。
- L相对C的9个门通过7个，但2年/3年1月起点滑点比 `106.03%/107.79%` 失败，完整周期滑点 `111.03%` 仍失败；大量1年窗口与C相同也说明严格三倍增强样本稀少。
- L相对A最差收益差为 `3年2020-06=-160.1315pp`；相对C最差为 `3年2019-06=-153.9590pp`，同时回撤恶化 `5.8088pp`。39个完整滚动L窗口全部生存，但尾部和起点依赖明确存在。
- 决策 `confirm_long_only_triple_volume_not_promotable_after_multicycle`；局部相对C/K优势不能覆盖正式A与完整周期成本失败，不扫阈值、倍率、方向、品种、年份或起点，不发布正式物料、不改master或生产。

## Stage017 多头三倍放大与半量缩减风险全周期反证

- 结果前以 `ebe805b86` 冻结 M：多头30日同向且最近10日量严格大于前10日三倍时风险 `×1.5`；多头最近10日量严格小于前10日半量时风险 `×0.5`，低量不要求30日同向；其他多头和全部空头 `×1.0`。A/C/L复用Stage015，仅M新跑一次。
- M 为 `14,293,257.00/9428.8380%/-54.2470%/Sharpe1.406198`，滑点 `1,634,290`、交易 `826`、胜率 `52.7496%`、broker10峰值 `91.0591%`、超100% `0` 天。
- M 相对A/C收益分别增加 `814.6953/636.5941pp`、最大回撤改善 `1.9599/2.7406pp`、Sharpe增加 `0.043967/0.043529`；但滑点分别为 `107.1251%/107.7175%`，两个晋级对照均仅成本门失败。
- 376条诊断为多头高量17、低量4、基准281、空头旁路74；4条低量风险均精确减半并形成后续OPEN，但自然样本恰好全部30日同向，未提供非同向低量真实样本证据。
- M 相对L少赚 `99.3899pp`，但回撤改善 `2.2923pp`、Sharpe提高 `0.031891`、滑点下降 `2.9801%`、broker峰值改善 `7.8232pp`；这是4次小样本路径重分配，不能替代A/C固定门。
- 决策 `stop_long_triple_volume_with_low_volume_discount_after_full_period`；不跑多周期、不扫低量阈值或倍率、不晋升、不改正式物料/master/production/CTP。

## Stage018 多空对称三倍放大与半量缩减风险全周期反证

- 结果前以 `f1ac2ef6b95a8e69a6d45c59a4186403079bafaf` 冻结 N：多头30日上涨/空头30日下跌且最近10日量严格大于前10日三倍时风险 `×1.5`；两方向最近10日量严格小于前10日半量时风险 `×0.5`且不要求方向一致；其他 `×1.0`。A/C/M复用Stage017，仅N新跑一次。
- N 为 `15,573,747.90/10282.4986%/-46.5442%/Sharpe1.468868`，滑点 `1,671,490`、交易 `827`、胜率 `52.4673%`、broker10峰值 `87.3320%`、超100% `0` 天。
- N 相对A/C收益增加 `1668.3559/1490.2547pp`、回撤改善 `9.6628/10.4435pp`、Sharpe增加 `0.106637/0.106198`；但滑点为 `109.5635%/110.1694%`，A/C双晋级门均仅成本失败。
- 377条诊断为多头高量/低量/基准 `17/4/281`，空头高量/低量/基准 `4/2/69`；严格阈值、倍率和目标风险合同通过。空头增量只有6次，历史路径改善不能证明可穿越周期。
- 决策 `stop_symmetric_triple_volume_with_low_volume_discount_after_full_period`；不跑多周期、不扫阈值或倍率、不晋升、不改正式物料/master/production/CTP。

## Stage019 M版多头信号日ATR冲击过滤全周期空命中

- 结果前以 `9b5beacbc808ca1376d1b13f2344e8ec10d1e1f6` 冻结O：仅多头 `flat_entry/reverse_entry/rollover_reopen` 在信号日跌幅严格大于此前5个完整交易日ATR简单平均的2倍时阻止开仓；等于允许，空头、加仓、retry排除，历史无效保持M。
- A/C/M逐值复用Stage017，仅O新跑一次。945条诊断中多头候选482、空头旁路463；实际拦截0，最大跌幅/ATR仅 `1.650246`，所以O与M的2037日资金曲线、826条trade及全部关键指标完全一致。
- O仍为 `14,293,257.00/9428.8380%/-54.2470%/Sharpe1.406198`，滑点 `1,634,290`、交易826、胜率 `52.7496%`；相对A/C滑点比 `107.1251%/107.7175%`，双晋级门失败。
- 独立 reviewer 对ATR计算、入口隔离、0命中、复用身份、门禁和生产边界复算后为 `blocker=0/non-blocker=0`。
- 决策 `stop_m_long_atr_shock_filter_after_full_period`；不跑多周期，不降低ATR倍数或扫描周期寻找命中，不扩展方向/入口，不改正式物料/master/production/CTP。

## Stage020 N版多头信号日1倍ATR冲击过滤全周期反证

- 结果前以 `b9578feb30545566b7c5caacc164c981dabaf027` 冻结P：完整继承N的多空对称三倍量能放大/半量缩减，只对多头普通/反转/换月重开增加 `信号日跌幅 > 前置ATR5` 禁开；空头、加仓、retry不变。
- A/C/N逐值复用Stage018，仅P新跑一次。946条诊断中多头483、空头旁路463；实际7条多头sizing候选置0，普通入口6、换月1，合同通过。
- P为 `15,135,800.10/9990.5334%/-44.9033%/Sharpe1.495411`，滑点 `1,571,580`、交易821、胜率 `52.8467%`、broker10峰值 `99.6724%`。
- P相对C全部门通过；相对A收益/回撤/Sharpe/成本均通过，但broker峰值比A恶化 `8.1774pp`，正式双门未同时通过。相对N少赚 `291.9652pp`、broker峰值恶化 `12.3404pp`。
- 独立 reviewer 复算实现、产物与门禁，最终 `blocker=0/non-blocker=0`。
- 决策 `stop_n_long_atr_shock_filter_after_full_period`；不自动跑多周期、不扫ATR倍数或周期救参，不改正式物料/master/production/CTP。

## Stage021 P版多空对称1倍ATR逆向冲击过滤全周期空增量

- 结果前以 `b907562db36d38ca5e07c9b1eba8e3e5dd9e88c5` 冻结Q：继承P与N的全部风险缩放，多头大跌、空头大涨严格超过前置ATR5时禁开；普通/反转/换月生效，加仓与retry排除。
- A/C/P逐值复用Stage020，仅Q新跑一次。946条诊断中Q拦截多头7、空头7；空头新增7条全部为普通入口，合同通过。
- 但7条空头候选在P中本来就没有形成对应实际OPEN，Q与P同为821条trade、2037日资金曲线逐点最大差0，全部关键指标完全一致：`15,135,800.10/9990.5334%/-44.9033%/Sharpe1.495411`。
- Q相对C通过、相对A仍因broker峰值 `99.6724% vs 91.4950%` 失败；P_vs_Q形式通过只是完全相等，不是新增alpha。
- 独立 reviewer 键级复算确认14/14候选均未进入entry_risk/OPEN/trade_events，最终 `blocker=0/non-blocker=0`。
- 决策 `stop_p_symmetric_atr_shock_filter_after_full_period`；不跑多周期、不调整后置门或ATR参数制造实际命中，不改正式物料/master/production/CTP。

## Stage022 Q多空对称ATR过滤多周期零增量复核

- 用户在 Stage021 全周期失败后明确要求补跑；结果前以 `85e9cb4d2` 冻结A/C/P/Q、43窗口、1/2/3年每年1月与6月起点及固定五图。A/C复用86臂窗，P/Q新跑86次真引擎，共172臂窗。
- Q与P在43/43窗口的九项核心指标和逐日资金曲线全部逐值相同；P_vs_Q 的9个周期×起点门形式全过只是零差平局，确认空头过滤在不同冷启动状态下仍没有改变实际组合路径。
- Q相对A的1/2/3年combined收益胜率 `33.33%/61.54%/54.55%`，9个门只通过3个；相对C通过6/9。全周期仍为 `15,135,800.10/9990.5334%/-44.9033%/Sharpe1.495411`，broker峰值 `99.6724%` 对A的 `91.4950%` 失败。
- 39个完整滚动Q窗口全部生存，但最低权益 `98,680`，最差收益 `1年2022-01=-19.0133%`、最深回撤 `2年2022-06=-52.9011%`；跨起点稳定性不足以覆盖全周期门失败。
- 独立 reviewer 逐值复算 A/C 身份、P/Q 43窗曲线、全周期和周期门禁、五图及生产边界，结论 `P0=0/P1=0/P2=0`。
- 决策 `confirm_q_not_promotable_after_multicycle`；不调ATR参数或后置门制造命中，不晋升、不发布正式物料、不改master/production/CTP。

## 下一步

1. 固定 `backwards_ratio_continuous + shrink_to_allowed` 只做 forward shadow，积累更多自然换月和容量不足 OOS 事件；不再增加历史窗口来重复证明同一失败。
2. 不扫复权方式、缩手比例、MA、MACD、品种、日期或方向救 `2018/2022`；当前不进入正式物料与激活流程。
3. 只有新增、未参与本次设计的 forward 样本改变风险判断，才重新开启新的正式晋级审计。
4. `30日+1.2倍` 风险增强路线经 Stage007 多周期再次确认关闭；不扫 `20/40/60` 日、`1.1/1.3/1.5` 倍、品种、方向、年份或起点。
5. Stage008 量能确认也未改变晋级结论；量能字段只保留为新增自然 OOS 的只读归因标签，不继续做历史救参。
6. Stage009 对用户提出的 `recent_10d_volume > 0.5 * prior_10d_volume` 先做覆盖率预检：会命中 `365/371=98.3827%` 的价格同向诊断，只比 D 少增强 `6` 次，已经退化为近普遍加风险；按预声明停止条件不实现 G、不跑全周期/多周期，也不继续扫描成交量比例阈值。
7. Stage010 的成交量翻倍 H 虽有选择性，但全周期 A/C 对照的回撤与成本门均失败；按冻结合同关闭该阈值，不运行多周期，后续只接受新增自然 OOS 或真正外生的信息源。
8. 用户要求补跑后，Stage011 新跑43个H独立窗口并复用Stage008已验证的129个A/C/F窗口：H相对C的9个周期×起点门通过8个，但相对正式A的9个全部失败，2/3年combined收益胜率仅 `46.15%/45.45%`，3年DD非劣率 `72.73%`；完整周期A/C风险失败仍有效，最终 `confirm_double_volume_not_promotable_after_multicycle`，不再扫描阈值。
9. Stage012 按用户确认冻结 I：双重确认命中风险 `×1.5`，否则 `×0.5`。I 全周期仅 `3,798,557.20/2432.3715%/-44.8870%/Sharpe1.200596`，虽然相对 A/C 回撤改善 `11.3200/12.1006pp`、滑点降至 `25.3463%/25.4864%`，但收益只保留 `28.2370%/27.6650%` 且 Sharpe 均显著下降；A_vs_I、C_vs_I 都失败，最终 `stop_asymmetric_double_volume_risk_after_full_period`，不跑多周期、不扫倍率。
10. Stage015 将仅多头增强阈值收紧到严格三倍后，L 的完整周期路径优于 K，但 A/C 滑点门仍失败；增强样本仅17次，不再做任何历史阈值或倍率迭代，等待真正外生的新 OOS 信息。
11. Stage016 按用户要求补完固定多周期后，L 对正式A的9个周期×1月/6月门只通过1个，且中长周期DD/成本和若干弱起点再次失败；多周期不能救回完整周期A/C成本门，方向/量能风险倍率路线继续关闭。
12. Stage017 的低量防守只命中4次，虽改善M的风险路径，但A/C成本门均失败；不因“只差成本门”继续扫 `0.4/0.6`、风险倍率或强行跑多周期，只保留为forward只读标签。
13. Stage018 将相同规则扩展到空头后，只有4次空头高量和2次空头低量改变风险；N虽显著改善全周期收益/回撤/Sharpe并通过M增量对照，但A/C成本仍超105%。按冻结合同停止，不把少数事件的复利路径当成新alpha。
14. Stage019 的固定 `多头信号日跌幅 > 2×前置ATR5` 在482条多头候选中0命中，O与M逐值相同；不为制造样本而降低倍数、改周期或扩入口，等待新增自然forward事件。
15. Stage020 按用户纠正基于N并使用1倍ATR，7次触发让P相对C通过但仍恶化正式A的broker峰值；小样本路径改善不能覆盖A门失败，不继续扫参。
16. Stage021 空头对称过滤命中7条sizing候选，但它们在P中原本就没有形成实际OPEN，Q与P逐值相同；该过滤与既有后置门重叠，不跑多周期或改门制造差异。
17. 用户要求补跑后，Stage022 新跑P/Q各43个独立窗口，Q与P在43/43窗逐日资金曲线完全相同；Q相对A只通过3/9个周期门且全周期broker门仍失败，最终 `confirm_q_not_promotable_after_multicycle`，不再增加历史窗口或调参。
