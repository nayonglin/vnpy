# Stage056 Stage037 AI Top14+fu 全周期 A/C

区间：`2018-01-01` 至 `2026-08-28`。唯一变量为 AI 月度池从 Top8 非fu + fu 扩为 Top14 非fu + fu。

> 身份警示：本报告的 A 是远端 master `m0016` Stage037 离线研究基线，不是当前稳定生产。稳定生产仍为 `09aa96a / m0015 / Stage021-Q`，与 master `a7d8599 / m0016 / Stage037` 不一致；因此本次数字只能作为离线诊断，不能作为合规正式基线A/C或晋升证据。

| 版本 | 期末权益 | 总收益 | 最大回撤 | Sharpe | 总滑点 | 总交易次数 | 非零交易日胜率 | broker10峰值 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| A: master m0016 Stage037离线基线 Top8+fu（9品种） | 16,859,940.60 | 11139.9604% | -39.9147% | 1.538821 | 1,659,555 | 734 | 53.2310% | 93.5807% |
| C: Stage037 Top14+fu（15品种） | 19,095,929.80 | 12630.6199% | -44.7340% | 1.500060 | 2,577,090 | 920 | 53.4286% | 111.8003% |

## 结果差异

- C 相对 A：期末权益 `+2,235,989.20`，总收益 `+1490.6595pp`，最大回撤 `-4.8193pp`，Sharpe `-0.038761`。
- 交易数 `+186`，滑点 `+917,535`，非零交易日胜率 `+0.1976pp`。

## 数据边界

- 共 `55` 个 AI 月度快照；每月均为 14 个非fu + 固定fu，master m0016 的 Top8 全部保留。
- `2026-03/04/05` 的原始第9–14名评分未被历史正式物料保存；这三个月采用 `membership_locked_score_fill`：锁住当时正式 Top8，再用同模型时点重放排名补6个。
- 2019-12-31 的 static18 前AI边界原样保留，未把模型选择逻辑倒灌到AI上线前。

## 新增品种开仓归因

- `OI.CZCE`：开仓成交 `13` 笔，合计 `2836` 手。
- `au.SHFE`：开仓成交 `10` 笔，合计 `320` 手。
- `MA.CZCE`：开仓成交 `8` 笔，合计 `2537` 手。
- `hc.SHFE`：开仓成交 `7` 笔，合计 `2628` 手。
- `AP.CZCE`：开仓成交 `6` 笔，合计 `811` 手。
- `SA.CZCE`：开仓成交 `5` 笔，合计 `2494` 手。
- `lh.DCE`：开仓成交 `5` 笔，合计 `272` 手。
- `CF.CZCE`：开仓成交 `4` 笔，合计 `1375` 手。
- `FG.CZCE`：开仓成交 `4` 笔，合计 `1196` 手。
- `SM.CZCE`：开仓成交 `4` 笔，合计 `1596` 手。
- `cu.SHFE`：开仓成交 `4` 笔，合计 `205` 手。
- `ru.SHFE`：开仓成交 `4` 笔，合计 `406` 手。
- `sp.SHFE`：开仓成交 `4` 笔，合计 `922` 手。
- `SH.CZCE`：开仓成交 `3` 笔，合计 `512` 手。
- `jm.DCE`：开仓成交 `3` 笔，合计 `513` 手。
- `rb.SHFE`：开仓成交 `3` 笔，合计 `959` 手。
- `si.GFEX`：开仓成交 `2` 笔，合计 `567` 手。
- `lc.GFEX`：开仓成交 `1` 笔，合计 `222` 手。

## 决策边界

- 选择合同：`True`；唯一变量合同：`True`。
- 风险结果：回撤不劣于正式版 `False`，Sharpe不低于正式版 `False`，broker10不超过100% `False`。
- 六身份一致门：`False`；本次决策为 `protocol_invalid_production_identity_drift_keep_offline_diagnostic_only_do_not_promote`。
- 本阶段是研究回测，不连接 CTP、不调用订单API、不自动晋升正式版。
