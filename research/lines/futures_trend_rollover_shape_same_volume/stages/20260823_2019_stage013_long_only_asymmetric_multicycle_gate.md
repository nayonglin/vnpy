# Stage013 多头限定非对称双倍量能多周期预声明

- line_id：`futures_trend_rollover_shape_same_volume`
- 冻结时间：2026-08-23 20:19 CST
- 阶段状态：结果前冻结；尚未运行 Stage013 回测
- 是否重要突破：否
- 是否触发 A/B：是

> 20:22 CST 运行前身份校验在第1个窗口前阻断：Stage011 日期含午夜时间、Stage010 仅日期，2037个H权益值逐位相同但原始字符串不同。只将日期身份校验改为ISO混合解析后比较日历日，并新增回归测试；未运行任何窗口、未看到任何Stage013结果，规则、窗口、闸门与指标均未改。

## 假设与调研判断

- 结构假设：Stage012 把约 `87%` 的普通趋势开仓统一降至半风险，切断右尾复利；如果这种损失主要来自空头，则只对多头使用双倍量能非对称分配、让空头保持原风险，可能保留更多收益并控制回撤。
- CME 说明成交量主要反映参与度、流动性和换月迁移，不能单独区分买卖或开平：<https://www.cmegroup.com/education/courses/introduction-to-futures/what-is-volume>。
- 判断：多空不对称有一次归因价值，但“Stage012 失败后只释放空头”是后验拆分，过拟合风险高；本阶段禁止根据结果扫描方向、`0.5/1.5`、成交量倍数、窗口、品种、年份或起点。

## 冻结规则

- A：正式 C9/15万；复用 Stage011 已验证的43窗口。
- C：A + 换月连续历史形态续仓；复用 Stage011。
- H：C + 30日方向同向且最近10日成交量严格大于再前10日两倍时风险 `×1.2`，否则 `×1.0`；复用 Stage011。
- I：C + 多头和空头均执行双重确认命中 `×1.5`、否则 `×0.5`；本阶段真跑43窗口，其中完整周期须与 Stage012 逐值一致。
- J：C + 仅多头执行双重确认命中 `×1.5`、否则 `×0.5`；所有空头无条件绕过此层并保持 `×1.0`；本阶段真跑43窗口。
- 双重确认：信号日收盘 `T` 相对第30个交易日前收盘方向同向，且 `T-9..T` 成交量总和严格大于 `T-19..T-10` 的两倍；恰好两倍仍为未确认。
- 多头历史不足、数据非法、方向不一致、成交量不足或未达阈值均使用 `×0.5`；空头不读取这些条件，直接 `×1.0`。
- 倍率位于现有风险预算之后、整数手向下取整之前；单笔、组合、相关性、保证金、broker、可用资金等硬门继续在后且只能缩减/拒绝。
- 覆盖 flat、reverse、rollover reopen、regular add、donchian add、post-quality add；默认开关关闭时 A/C/H/I 旧语义不变。

## 多周期窗口与输出合同

- 数据范围：2018-01-02 至 2026-05-29。
- 固定43窗口：完整周期1个；1年15个完整+1个 terminal near-complete；2年13+1；3年11+1。
- 每个1/2/3年周期均包含1月和6月独立起点；只有完整窗口参与晋级门，near-complete 只观察。
- A/C/H 复用 `129` 次独立真引擎；I/J 新跑 `86` 次；逻辑总计 `215` 个臂窗。
- 10组两两比较，每窗一行，共 `430` 行；1/2/3年 × combined/January/June 聚合共 `90` 行。
- 固定五图：完整周期、1年、2年、3年、聚合热力图；同代原子发布 summary/comparison/aggregate/curve/decision/诊断/成交/事件与图片。

## 晋级闸门

- 晋级对照只认 `A_vs_J` 和 `C_vs_J`；`I_vs_J` 仅用于多空释放归因。
- 完整周期：收益不低于左臂；回撤恶化不超过1个百分点；Sharpe不劣于0.01；滑点不超过105%；账户生存；broker100严重度不恶化。
- 每个1/2/3年 × combined/January/June：收益胜率至少50%；收益差中位数非负；回撤2个百分点非劣率至少80%；DD50失败数不增加；Sharpe 0.05非劣率至少80%；聚合滑点不超过105%；全部生存；broker100失败数不增加。
- J合同必须同时出现多头命中、多头未命中和空头绕过，并逐行验证 `1.5/0.5/1.0` 与目标风险金额。
- 任一晋级闸门失败即 `reject_long_only_asymmetric_after_multicycle`，不得选择性解释或自动晋级。

## 开始前反思

- 是否在过拟合：规则执行本身尚未看 Stage013 结果，且窗口、阈值、倍率、方向和闸门均先冻结，因此本次运行不是结果内调参；但假设来自 Stage012 失败后的方向拆分，研究序列的后验过拟合风险为高。
- 是否还有价值继续：有，但只值这一次完整多周期证伪；它能区分收益损失是否主要来自空头降风险。若失败，停止该方向，不继续扫多空、倍率或阈值。
- 生产边界：不改正式配置、正式物料、master、production、launchd；不连接 CTP，不调用报单或撤单 API。
