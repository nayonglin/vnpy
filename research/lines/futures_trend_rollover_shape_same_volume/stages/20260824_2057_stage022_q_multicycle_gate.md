# Stage022 / Q 多周期固定门禁（运行前）

- 改动时间：2026-08-24 20:57（Asia/Shanghai）
- 是否重要突破版本：否；这是用户在 Stage021 全周期失败后明确要求的固定多周期诊断，不修改 Q 规则、不做参数搜索。
- 简单总结：比较 A（正式基准）、C（正式换月延续）、P（仅多头 1 倍 ATR5 逆向冲击禁开）和 Q（P 加空头对称禁开）。
- Q 冻结规则：完整继承 N 的多空成交量风险缩放；多头 `前收-信号日收 > 前置ATR5` 禁开，空头 `信号日收-前收 > 前置ATR5` 禁开；严格大于，等于允许；仅普通、反转、换月重开生效，加仓和 C9 retry 排除。
- 候选来源提交：`b907562db36d38ca5e07c9b1eba8e3e5dd9e88c5`；Stage021 结果提交：`f64cccd77d7f144765a9e983bd3b2108cacfe1d6`。
- 新增参数：无。
- 修改参数：无。
- 删除参数：无。
- 回测合同：`2018-01-01 -> 2026-05-29` 全周期，另有独立冷启动 1/2/3 年窗口；每个周期同时覆盖 1 月和 6 月起点，共 43 个窗口。
- 运行计划：A/C 复用 Stage013 已验证的 86 个独立臂窗；P/Q 各新跑 43 次，共 86 次新引擎运行、172 个逻辑臂窗。P/Q 全周期必须与 Stage021 逐值一致。
- 固定产物：summary、comparison、aggregate、equity curves、decision JSON、Q 全周期诊断/交易明细，以及全周期、1年、2年、3年、聚合五张图；整代原子发布，若中断或磁盘满则从头重跑。
- 新增/修改/删除回测结果：运行前均无。
- 固定判定：Stage021 的 A_vs_Q broker 峰值不恶化门失败继续绑定；任何多周期局部优势都不能覆盖全周期失败。
- 过拟合反思（运行前）：是，高风险。Q 位于连续后验迭代链末端，且全周期新增空头过滤对实际 OPEN 为零增量；本次不调参并固定全部起点，只能降低选择性汇报，不能消除后验偏差。
- 继续价值（运行前）：有，但仅限一次固定跨起点诊断。价值在于确认空头过滤在不同冷启动持仓路径上是否仍与后置开仓门重叠；没有价值继续扫描 ATR 周期、倍数、方向、年份或起点。
- TODO：执行 86 次新窗口，重建与复验全部产物；跑出数据后交独立 reviewer；最终不触碰正式物料、master、production、CTP 或订单路径。
