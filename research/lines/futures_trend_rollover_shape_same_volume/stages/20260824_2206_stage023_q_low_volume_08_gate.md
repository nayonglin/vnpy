# Stage023 / Q低量阈值0.8防守版R（全周期反证）

- 改动时间：2026-08-24 22:06（Asia/Shanghai）
- 是否重要突破版本：否；这是用户确认的固定小数阈值防守实验，必须按高过拟合风险处理。
- 候选规则与执行器冻结提交：`0c832dba37c79c4cc2de7823fd96864838340682`。
- 候选假设：在趋势信号与Q的ATR逆向冲击过滤均保留时，近期成交量显著收缩可能意味着趋势参与度下降；把这类初始风险减半，观察能否改善正式A和换月C的路径风险而不显著损失收益。
- A：正式C9/15万；C：正式换月延续；Q：现有多空ATR过滤与 `<0.5倍量` 半风险；R：Q仅把低量阈值改为严格 `<0.8倍`。
- 新增参数：无底层参数，只新增R实验profile。
- 修改参数：`directional_30d_low_volume_ratio_threshold: 0.5 -> 0.8`。
- 保持参数：低量风险 `×0.5`；多空30日同向且量 `>3倍` 风险 `×1.5`；其他 `×1.0`；多空1倍前置ATR5逆向冲击禁开；等于阈值不触发。
- 删除参数：无。
- 结果前覆盖率预检：Q实际风险意图中有效量比 `374` 条；`<0.5` 为 `6=1.60%`，`<0.8` 为 `62=16.58%`，新增 `56` 条（多43/空13；普通44/换月12）。覆盖率有选择性但阈值无外部理论定值。
- 最小实验：A/C/Q复用Stage021已提交全周期，R只新跑一次 `2018-01-01 -> 2026-05-29`；比较收益、回撤、Sharpe、滑点、交易数、胜率、生存与broker峰值。
- 固定晋级门：A_vs_R和C_vs_R必须同时通过既有全周期门，量能合同与ATR合同必须通过，且R必须真实改变Q的组合指标/交易路径；否则停止，不跑多周期。
- 新增回测结果：R全周期期末权益 `12,694,469.70`、总收益 `8362.9798%`、最大回撤 `-44.0580%`、Sharpe `1.430950`、总滑点 `1,378,110`、总交易次数 `819`、胜率 `52.4088%`、broker10峰值 `87.6272%`、超100% `0` 天。
- 修改/删除回测结果：无；A/C/Q均从Stage021提交产物逐值复用，仅R新跑一次。
- 增量结果：R相对Q期末权益减少 `2,441,330.40`、总收益减少 `1627.5536pp`、Sharpe减少 `0.064461`、交易减少2笔；最大回撤改善 `0.8453pp`、滑点减少 `193,470`、broker10峰值改善 `12.0452pp`。
- 正式门结果：R相对A总收益低 `251.1629pp`，相对C低 `429.2641pp`，两组均仅“收益不低于基线”失败；相对Q同时失败收益与Sharpe门。决策 `stop_r_low_volume_08_after_full_period`，不进入多周期。
- 风险合同：374条诊断中多头高量/低量/基准 `17/47/234`，空头 `4/15/57`；严格 `<0.8`、等于不触发、目标风险 `×0.5` 合同全部通过。相对Q新增低量命中56条；由于权益和持仓路径反馈，最终组合交易路径发生真实变化。
- ATR合同：947条诊断中规则命中14条，多头7、空头7；13条正手数候选被置0，另1条在成交量/整数手数层已经为0但仍正确记录ATR规则命中 `0->0`。初版复用的旧合同错误要求所有blocked记录必须 `before>0`，修正Stage023校验语义后从已发布原始结果重建报告，未重跑回测引擎、未改变策略与指标。
- 外部调研判断：Campbell/Grossman/Wang的原始研究说明成交量与收益序列相关关系取决于流动性交易压力；大规模异常复现研究也提示成交量类信号缺少稳定普适性。这些资料支持谨慎验证，不支持0.8的精确值。
- 过拟合反思（运行前）：是，高风险。0.8是用户提出的精确小数阈值，且位于连续历史迭代之后；预检只能证明覆盖率，不证明预测力。
- 继续价值（运行前）：有，但仅限一次固定全周期证伪。若失败，停止阈值/倍率/方向/年份/品种/起点扫描；若双基线与增量门都通过，再讨论固定多周期。
- 生产边界：不改正式物料、master、production、CTP或订单链；订单/撤单API必须为0/0。
- 过拟合反思（运行后）：是，高风险。阈值从0.5改到0.8使低量覆盖从6条扩大到62条，56个新增事件及其复利反馈足以显著改写路径；收益下降说明这不是稳定的新信息源，而是更广泛地削减已有趋势暴露。
- 继续价值（运行后）：否，当前历史阈值优化不值得继续。R虽改善回撤、资金占用和成本，但低于A/C收益门且弱于Q；不扫描0.6/0.7/0.9、倍率、方向、品种、年份或起点。
- 最终生产边界：正式配置、正式物料、master、production、CTP均未修改；订单/撤单API `0/0`，CTP未连接。
- 独立reviewer：复算规则身份、A/C/Q曲线复用、R指标与门禁、374条量能合同及14条ATR合同；确认唯一 `2020-04-17 au2006.SHFE long flat_entry` 的 `0->0` 来自低量层先行归零，不掩盖ATR错误，结论 `P0=0/P1=0/P2=0`。
- TODO：封存负结果；只保留新增自然OOS的只读观察。
