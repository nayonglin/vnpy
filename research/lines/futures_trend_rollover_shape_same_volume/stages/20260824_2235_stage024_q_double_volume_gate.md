# Stage024 / Q高量阈值2倍版S（全周期反证）

- 改动时间：2026-08-24 22:35（Asia/Shanghai）
- 是否重要突破版本：否；这是用户明确要求的固定阈值迭代，按高过拟合风险处理。
- 候选规则与执行器冻结提交：`4d5f4dc24872ea2ad200d73bcc8c844bfe08bdda`。
- 上一版本处置：R/Stage023由用户明确放弃；历史代码和产物保留为负证据，但S不继承R。
- 候选假设：在Q的双向ATR逆向冲击过滤和低量防守不变时，把高量确认从三倍放宽到两倍，可能用更多“方向同向且参与度明显扩张”的事件增加趋势风险，但必须证明不是广泛加杠杆。
- A：正式C9/15万；C：正式换月延续；Q：多空高量 `>3倍 ×1.5`、低量 `<0.5倍 ×0.5`、双向1倍ATR5拦截；S：Q仅把高量阈值改为严格 `>2倍`。
- 新增参数：无底层参数，只新增S实验profile。
- 修改参数：`directional_30d_volume_ratio_threshold: 3.0 -> 2.0`。
- 保持参数：高量风险 `×1.5`；低量严格 `<0.5倍` 风险 `×0.5`；其他 `×1.0`；多空均参与；多空1倍前置ATR5逆向冲击禁开；等于阈值不触发。
- 删除参数：无。
- 结果前覆盖率预检：Q的374条有效量比风险意图中，严格 `>3倍` 为21条，严格 `>2倍` 为47条，新增26条（多头22、空头4；均为普通入口）；低量 `<0.5倍` 仍为6条。
- 最小实验：A/C/Q复用Stage021已提交全周期，S只新跑一次 `2018-01-01 -> 2026-05-29`；比较收益、回撤、Sharpe、滑点、交易数、胜率、生存与broker峰值。
- 固定晋级门：A_vs_S和C_vs_S必须同时通过既有全周期门，量能与ATR合同必须通过，且S必须真实改变Q；否则停止，不跑多周期。
- 新增回测结果：S全周期期末权益 `12,979,961.70`、总收益 `8553.3078%`、最大回撤 `-49.0670%`、Sharpe `1.440073`、总滑点 `1,336,820`、总交易次数 `823`、胜率 `52.8551%`、broker10峰值 `90.5587%`、超100% `0` 天。
- 修改/删除回测结果：无；A/C/Q从Stage021提交产物逐值复用，仅S新跑一次。
- 增量结果：S相对Q期末权益减少 `2,155,838.40`、总收益减少 `1437.2256pp`、最大回撤恶化 `4.1636pp`、Sharpe减少 `0.055338`、交易增加2笔；滑点减少 `234,760`、broker10峰值改善 `9.1138pp`。
- 正式门结果：S相对A总收益低 `60.8349pp`，相对C低 `238.9361pp`；A_vs_S和C_vs_S均只有“收益不低于基线”失败。相对Q同时失败收益、回撤和Sharpe门。决策 `stop_s_double_volume_after_full_period`，不进入多周期。
- 风险合同：374条诊断中多头高量/低量/基准 `39/4/255`，空头 `8/2/66`；严格 `>2倍`、等于不触发、高量 `×1.5`、低量 `<0.5倍 ×0.5` 合同全部通过。高量覆盖从Q的21条扩大到47条。
- ATR合同：946条诊断中规则命中14条，多头7、空头7；14条均为正手数候选被置0，配置、方向、严格边界和手数合同通过。
- 外部调研判断：成交量与收益关系依赖流动性压力和市场状态，异常复现研究也不支持把固定两倍阈值视为普适真理；本轮只允许固定证伪。
- 过拟合反思（运行前）：是，高风险。这是连续成交量阈值迭代，且两倍阈值此前已在不同风险倍率结构中研究过。
- 继续价值（运行前）：有，但仅限一次固定全周期证伪。若失败，不扫1.5/2.5倍、风险倍率、方向、年份、品种或起点。
- 生产边界：不改正式物料、master、production、CTP或订单链；订单/撤单API必须为0/0。
- 过拟合反思（运行后）：是，高风险。新增26条高量放大事件经持仓与复利反馈，既没有保住Q的收益和Sharpe，还恶化Q的回撤；两倍阈值不是稳定高质量标签。
- 继续价值（运行后）：否，当前历史阈值优化没有继续价值。S低于A/C收益门且弱于Q，不跑多周期，不扫描1.5/2.5倍、风险倍率、方向、年份、品种或起点。
- 最终生产边界：正式配置、正式物料、master、production、CTP均未修改；订单/撤单API `0/0`，CTP未连接。
- 独立reviewer：逐值复算S/Q参数差异、A/C/Q曲线复用、S指标与门禁、374条量能合同、14条ATR真实拦截及生产边界，结论 `P0=0/P1=0/P2=0`。
- TODO：封存负结果；只保留新增自然OOS的只读观察。
