# Stage037 Stage034空头镜像硬拦截 A/B/C 冻结记录

- line_id：`futures_trend_rollover_shape_same_volume`
- 当前模式：`day`
- 冻结时间：`2026-08-27 18:15 CST`
- 分支：`codex/stage037-short-mirror-block`
- 直接基线：Stage034提交 `a8774b517ff76d56ff74a35743e66d2628167c1b`
- 候选版本：`stage037_stage034_long_short_mirror_hard_block_v1`
- 是否重要突破：否；完整周期风险收益明显改善，但预声明的Stage034 B侧滑点门失败，不能按自动流程晋级。

## 外部调研与判断

- AQR 的 Time Series Momentum 原始论文数据说明，期货趋势研究中对上涨与下跌方向采用对称的多空构造是常见研究框架。
- SSRN 的期货动量研究同样覆盖多头与空头趋势，但没有证据能证明本阶段精确的 `10日/ATR5/3日/4倍` 镜像过滤具有普适性。
- 判断：将已冻结的多头“趋势受损”形态做方向镜像，是一次可证伪的结构一致性实验；它不是外部理论直接推出的最优参数，不能用单次全周期优秀结果消除后验选择偏差。

## 冻结改动

- 严格继承Stage034，仅新增 `enable_short_signal_range_atr_filter=True`；B/C配置逐键差异只有这一项。
- 多头原条件完全不变：
  - A：`range_10d > 3×prior_ATR5` 且 `close[T]-close[T-3] < 0.5×prior_ATR5`；
  - B：`max(High[i]-Low[j]), i<j > 4×prior_ATR5`。
- 新增空头镜像：
  - A：`range_10d > 3×prior_ATR5` 且 `close[T-3]-close[T] < 0.5×prior_ATR5`，即最近3日没有形成足够下跌；
  - B：`max(High[j]-Low[i]), i<j > 4×prior_ATR5`，即先低后高的大幅反弹。
- 两方向内A与B均为OR；严格大于才命中，等于阈值放行。
- 只作用 `flat_entry/reverse_entry/rollover_reopen`，命中后把正候选手数置0；加仓、retry、退出、持仓管理、五日换月和新合约自身历史不变。
- 新增参数：`enable_short_signal_range_atr_filter=True`。
- 修改参数：无。
- 删除参数：无。

## 对照、数据与预声明硬门

- A：当前正式Q；B：Stage034仅多头硬拦截；C：Stage037多空镜像硬拦截。
- A/B逐值复用Stage034冻结产物，只新跑C一次。
- 区间：`2018-01-01 -> 2026-08-25`，实际 `2018-01-02 -> 2026-08-25` 共2098个交易日，15万元空仓独立启动。
- 数据库SHA256：`d7375edac99e182ba3524abfbed92abb035e101d05744cb801ec5c7b5dbd47f5`，与Stage034完全一致。
- C相对A和B：期末权益不得下降、Sharpe恶化不超过0.02、最大回撤恶化不超过2pp、滑点不超过105%、broker100天数不增加、账户生存；语义合同与空头实际命中必须通过。
- 任一门失败即停止，不自动跑多周期，不扫描方向、倍率、窗口、ATR周期、品种或年份救参。

## 运行前反思

- 过拟合判断：是，风险偏高。当前实验来自连续观察历史亏损形态后的规则迭代，参数仍继承此前后验路线；控制方式是只做方向镜像、只跑一个C臂、结果后不救参。
- 继续价值判断：有一次性价值。它能检验空头是否存在与多头同构的“趋势受损后仍开仓”问题；若连对称镜像都无增益，就应直接关闭该方向。

## 回测结果

- 完成时间：`2026-08-27 18:15 CST`；历史、五日换月、过滤语义、数据库和全周期覆盖合同全部通过。
- A 正式Q：期末权益 `14,989,515.10`，总收益 `9893.0101%`，最大回撤 `-44.9033%`，Sharpe `1.468555`，总滑点 `1,741,690`，总交易次数 `846`，胜率 `52.6728%`，broker10峰值 `99.6724%`，超100% `0` 天。
- B Stage034：期末权益 `15,528,313.60`，总收益 `10252.2091%`，最大回撤 `-40.3329%`，Sharpe `1.505701`，总滑点 `1,580,570`，总交易次数 `749`，胜率 `52.9032%`，broker10峰值 `83.1383%`，超100% `0` 天。
- C Stage037：期末权益 `17,051,717.30`，总收益 `11267.8115%`，最大回撤 `-39.9147%`，Sharpe `1.543941`，总滑点 `1,669,965`，总交易次数 `733`，胜率 `53.1984%`，broker10峰值 `93.5807%`，超100% `0` 天。
- C相对B：期末权益 `+1,523,403.70`、收益 `+1015.6025pp`、最大回撤改善 `0.4182pp`、Sharpe `+0.038240`、交易 `-16`；滑点 `+89,395`，为B的 `105.6559%`，超过105%门约 `0.6559pp`。
- C相对A：期末权益 `+2,062,202.20`、收益 `+1374.8015pp`、最大回撤改善 `4.9886pp`、Sharpe `+0.075386`、滑点 `-71,725`、交易 `-113`；A侧全部预声明门通过。
- C共记录 `971` 条过滤诊断，`970` 条有效；OR命中 `152`，其中多头 `79`、空头 `73`。过滤节点前有正手数并实际置0共 `144`，其中多头 `73`、空头 `71`；另有 `8` 条在此前规则已归零，不重复计为实际拦截。
- 条件分解：扩张且方向近3日不足 `140`，有序逆向移动 `12`，重叠 `0`；多空时序、严格大于、OR、入口范围和硬置0合同全部通过。
- 独立reviewer结论：实现与产物 `PASS`，研究晋级 `FAIL`，`P0/P1/P2=0/0/0`。Reviewer独立复算配置唯一差异、数据库与12个Stage034引用文件SHA、A/B曲线复用、`152/79/73/144/73/71/8` 计数和全部核心指标，确认15个预声明门仅B侧滑点门失败；允许上限 `1,659,598.5`，C实际 `1,669,965`，超过 `10,366.5`。
- 决策：`stage037_short_mirror_block_fail_full_period_stop`；唯一失败硬门是C相对B滑点不超过105%。

## 安全边界

- 本次为离线研究，没有连接CTP；order/send/cancel API调用均为 `0`。
- 未修改正式物料、远端master、生产worktree、邮件、launchd或AI池。

## 运行后反思

- 过拟合判断：仍然是，风险偏高。C在全周期明显改善，但规则来自历史图形归纳，而且空头实际拦截只有71个，无法仅凭单一路径证明跨周期稳定性。
- 继续价值判断：有研究解释价值，但按冻结门当前没有自动继续优化或自动多周期的价值。它相对正式A全面改善，说明空头镜像值得保留为明确候选；但相对直接基线B多出5.6559%滑点，已超过预声明成本容忍度，不能事后放宽门槛。
- TODO：保留Stage037代码与全周期产物，不救参、不自动多周期。若用户明确要求，可把多周期作为额外诊断，而不是改写本次失败结论。
