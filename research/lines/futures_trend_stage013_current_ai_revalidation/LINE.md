# Stage013 当前 AI 重验研究线

- line_id: `futures_trend_stage013_current_ai_revalidation`
- 创建时间: `2026-07-10 18:35 CST`
- 当前模式: `day`
- 资产/策略: 商品期货趋势 / 当前 C9 15w 独立研究分支
- 当前状态: Stage005 独立 review 发现 P0 账户状态账本不一致；Stage001/002/003/005 的 Stage013 绩效结论全部失去晋级资格，Stage004 原 AI 生效边界法证仍有效；进入 Stage006 权威权益对账修复
- 基准: 当前官方 AI `55` 个 eval_date + 当前 C9/15w

## 研究动机

Stage013 账户状态 pilot 曾在旧 frozen AI 下完成 `17` 个逐半年起点验证，正收益 `17/17`、收益胜正式 `14/17`、回撤改善 `14/17`；但当前官方 AI 文件已经变为 `504` 行、`55` 个 eval_date，既不等于 Stage061 frozen AI，也不等于 Stage062 full-monthly candidate AI。不能沿用旧曲线，必须同引擎重验。

## 冻结规则

- A：当前官方 AI + 当前 C9/15w。
- C：A + 原 Stage013 账户状态 pilot。
- 仅当账户回撤绝对值 `>=30%`、当前活跃持仓 `<=1`、且是新的 `flat_entry` 时，把该次开仓降为 `1` 手。
- 不改 AI、退出、0.5R 开仓日止损重试、单品种 25% 保证金上限、最多 4 个持仓、相关门、forced-margin、增量保证金或成本。
- 不调整 `30%/1个持仓/1手`，不按品种、日期、方向或年份加例外。

## Stage001 硬门槛

- C 全周期收益为正，且相对 A 收益保留 `>=70%`。
- C 全周期最大回撤改善 `>=3pp`。
- C 2022 年内最大回撤改善 `>=5pp`。
- C 固定压力窗 `2022-07-15 -> 2024-05-10` 最大回撤改善 `>=3pp`。
- C broker10 峰值不得高于 A。
- A/C 当前 AI eligibility 归一化完全一致；Stage013 事件必须全部满足冻结触发条件。
- 独立 agent review 无 P0/P1。

## 反过拟合边界

- Stage001 只跑当前 AI 的一个冻结 A/C。
- 不扫描账户回撤、活跃持仓、pilot 手数、月份、品种、方向或恢复条件。
- 通过全部硬门槛才允许逐半年；失败即关闭。

## 开始前反思

- 过拟合：低。规则来自 2026-07-01 前序冻结版本，本阶段只更换为当前 AI 输入做时点外重验。
- 继续价值：有。它直接作用于“深回撤且仓位已空”的恢复失败，而不是普遍削弱所有趋势入场，理论上比协方差全局缩手更能保留右尾。

## Stage001 结果

- A：期末权益 `5,996,631.00`，总收益 `3897.7540%`，最大回撤 `-55.3701%`，Sharpe `1.3967`，滑点 `759,970`，交易 `641`，非零日胜率 `52.8302%`，逐笔胜率 `45.8716%`，broker10 `88.3398%`。
- C：期末权益 `5,984,961.70`，总收益 `3889.9745%`，最大回撤 `-38.1717%`，Sharpe `1.4585`，滑点 `489,650`，交易 `639`，非零日胜率 `53.0516%`，逐笔胜率 `45.5385%`，broker10 `81.5638%`。
- 收益保留 `0.9980`；全周期/2022/固定压力窗回撤改善 `17.1984/5.3500/21.0464pp`；broker10 改善 `6.7760pp`。
- Pilot `55` 次，减少 `8,172` 手；全部为 `flat_entry`，触发回撤 `30.56%-38.89%`，活跃持仓 `0/1`，最终 `1` 手，条件违规 `0`。
- A/C AI 都是 `504` 行、`55` 个 eval_date，归一化 hash `df020c940d576868`，官方文件 SHA16 `fc50e035cd66b65e`。
- 独立 review：`P0=0/P1=0/P2=0`，确认 A/C 同口径、drawdown 只使用当日及历史、`0.30=30%`、55 次事件条件和指标复算均成立；允许扩逐半年。

## Stage002 逐半年预声明

- 起点：`2020-01/07` 到 `2026-01`，共 `13` 个独立冷启动；终点统一 `2026-06-30`。
- A/C 每个起点都使用当前同一 AI eligibility 和同一引擎；不沿用主路径资金或持仓。
- 成熟样本定义：交易日 `>=252`。
- 成熟 C 必须全部正收益。
- 成熟起点中 C 最大回撤改善或持平比例 `>=80%`，且任何成熟起点回撤不得比 A 恶化超过 `3pp`。
- A 为正收益的成熟起点，C/A 总收益保留中位数 `>=70%`；`2020-01` 全路径继续满足 `>=70%`。
- C 的跨起点最差最大回撤相对 A 至少改善 `3pp`；C 跨起点最差 broker10 不得高于 A。
- Stage002 独立 review 必须无 P0/P1；失败后不调整 `30%/1/1` 或日期、品种、方向。

## Stage002 结果与独立审查

- 13 个起点、11 个成熟样本；成熟 C 正收益 `11/11`，回撤改善或持平 `11/11`，回撤恶化超过 3pp 为 `0`。
- A 正收益成熟起点收益保留中位/最小 `1.0813/0.9562`；2020 全路径收益保留 `0.9980`。
- 跨起点最差回撤 A/C `-55.3701%/-43.7940%`，改善 `11.5761pp`；最差 broker10 `88.3398%/81.5638%`。
- 独立 review `P0=0/P1=0/P2=3`、正确性置信度约 `97%`。复算误差 `<1e-12`，新进程复跑一致，未发现缓存、日期污染或未来函数。
- Pilot 路径事件 `470` 条、去重市场事件 `77` 个；全部满足回撤 `>=30%`、活跃持仓 `<=1`、`flat_entry`、降为 `1` 手，违规 `0`。
- AI A/C 都是 `504` 行、`55` 个 eval_date，候选 `12,199` 行全部启用 AI，允许/拦截 `6,557/5,642`，缺 signal date `0`。
- Stage004 法证纠正了初次月历解释：原 AI walk-forward 固定 `720/180/180` 天，首个 OOS 预测是 `2022-01-28`；2020-2021 的 static18 是明确 pre-AI 政策，不是缺月。
- 2022-01 至 2026-06 月度 AI `54/54` 连续完整；原 50 个 OOS 日期集合完全保留，2026-03 至 05 为 membership-only 恢复，2026-06 为 live inference。
- 最终决策：`stage002_pass_original_ai_policy_continue_cost_execution_validation`。撤销“缺9个月阻塞”，但尚不直接切线上，先做成本、执行和 shadow 验收。

## Stage003 提前生效反事实预声明（历史原假设）

- 外部调研判断：时间序列验证必须保持训练在过去、预测在未来；滚动模型需要显式 warm-up。工程上应保留已有时点快照，只补缺失且当时可生成的月份，不能为了补洞重算并替换已有月份。
- AI 输入：完整保留当前 official `504` 行/`55` 个 eval_date；仅从 Stage062 冻结产物追加 `2021-04 -> 2021-12` 的 `81` 行/`9` 个 eval_date；预计合计 `585` 行/`64` 个 eval_date。
- Cold-start：`2020-01 -> 2021-03` 明确沿用 `2019-12` bootstrap 池，不伪造 15 个独立月池；`2021-04 -> 2026-06` 的 63 个可生成月份必须全部存在。
- A：append-only hybrid AI + 当前 C9/15w；C：A + 冻结 Stage013 `30%/1/1`，不改任何策略参数。
- Stage003 先跑 `2020-01 -> 2026-06-30` 单一起点。C 必须正收益；相对 hybrid A 和 Stage002 当前 official A 的收益保留都 `>=70%`。
- C 相对 hybrid A 的全周期/2022/固定压力窗回撤改善分别 `>=3/5/3pp`，broker10 峰值不高于 A。
- 当前 504 行必须 canonical 完全保留；只能新增 81 行，不能替换已有月。A/C eligibility 必须一致，候选级 AI 明细必须落盘，pilot 条件违规必须为 0。
- 独立 review 必须无 P0/P1。全部通过才允许 Stage004 逐半年；失败即关闭 append-only 输入，不调整 `30%/1/1`、月份、品种、方向或训练规则救参。

## Stage002 运行后反思

- 过拟合：低到中等。没有调参且成熟起点一致，但 13 条路径共享市场时段，不能当作 13 个统计独立样本。
- 继续价值：有。下一步转成本敏感、执行一致性与 shadow；不补 2021 月池，也不调 `30%/1/1`。

## Stage003 结果与独立审查

- 文件机械层：当前 `504/55` 完整保留，追加 `81/9` 后为 `585/64`；A/C 配置和 94 次 pilot 事件机械审计均通过。
- hybrid A：`4,201,665.50/2701.1103%/-52.7853%/Sharpe1.3993/滑点498,000/交易586`。
- hybrid C：`3,582,335.90/2288.2239%/-31.3413%/Sharpe1.5216/滑点223,840/交易586`。
- C 相对 hybrid A 收益保留 `84.7142%`，全周期/2022/压力窗回撤改善 `21.4439/7.8771/30.5078pp`；但相对 current official A 收益只保留 `58.7062%`，未过 70%。
- 独立 review `P0=0/P1=2/P2=3`，数值正确性置信度 `99%`；反向顺序新进程复跑最大差 `<4.7e-10`，无缓存或未来账户状态。
- P1 语义：Stage062/Stage182 使用后来 expanding train + 至少 12 月规则，原历史模型是 720 天 warm-up；追加 2021 月份会立即替代 static18，属于提前生效政策变化，不是数据修复。
- P1 PIT：追加 81 行中 37 行在 eval 前无非零策略状态；排除固定 fu 后仍有 `28/72` 个学习排名行无历史状态，不能称为严格 PIT 原政策重建。
- 决策：`stage003_close_counterfactual_early_activation_no_parameter_rescue`。不扩逐半年、不按月份或 `30%/1/1` 救参；该失败不否定 Stage002。

## Stage004 原始 AI 生效边界法证

- 原模型源码和重建 summary 均确认 train/test/step `720/180/180` 天；predictions `900` 行、50 个 eval_date，首个 `2022-01-28`。
- 当前 eligibility `504` 行/55 个 eval_date：2019-12 static18 边界 + 2022-01 至 2026-06 连续 54 个月；月度缺口 `0`。
- 当前 `ai_probability` 50 个日期与原 predictions 日期集合完全一致；Stage062 在首个 OOS 前实际生成的 9 个月是反事实 early activation。
- 边界法证 `P0=0/P1=0/P2=1`、置信度 `98%`；残余 P2 是 2026-03 至 05 只恢复 membership，原始概率值未字节级恢复，但准入/拦截集合完整。
- 决策：`current_ai_calendar_complete_under_original_oos_policy_stage003_is_counterfactual`。

## 当前反思

- 过拟合：Stage002 否；Stage003 有政策回填偏差但没有参数扫描，已按失败关闭。不能因为 Stage002 更好就发明边界，当前继续的依据是原 720 天源码、原 predictions 与历史冻结记录三重一致。
- 继续价值：Stage002 有，已达到收益保留与回撤目标；Stage003 无。下一步只做部署前稳健性，不再改 alpha 或 Stage013 阈值。

## Stage005 真实引擎成本与执行压力预声明

- 冻结 A/C、当前 AI、`30%/1个持仓/1手` 和 `2020-01-01 -> 2026-06-30` 不变；不新增或调整任何策略参数。
- 在 vn.py 回测引擎初始化前，把每个合约的单位滑点统一设为当前元数据的 `1x/2x/3x`；成本进入逐笔每日净损益，并通过账户权益反馈到后续整数手数和账户状态，禁止回测后静态扣费。
- 每个成本倍率都独立跑 A 当前 C9 与 C Stage013；A/C 使用同一份缩放后滑点表、同一 AI eligibility、同一数据和同一引擎。
- `1x` 新进程复跑必须与 Stage001 的核心日线和核心指标一致；否则视为执行入口漂移，整阶段失败。
- 每个 `1x/2x/3x` 倍率下，C 总收益必须为正，且相对同成本 A 的收益保留 `>=70%`。
- 每个倍率下，C 相对 A 的全周期、2022、固定压力窗最大回撤分别至少改善 `3/5/3pp`；broker10 峰值不得高于同成本 A。
- 每个倍率下，A/C AI 归一化 hash 必须一致；C 的 Stage013 事件必须全部满足回撤 `>=30%`、活跃持仓 `<=1`、`flat_entry` 且最终 `1` 手。
- 滑点表必须逐合约精确缩放且不修改原始元数据；所有 arm 的候选、成交、事件、日线和成本审计均落盘。
- 独立 agent review 必须无 P0/P1；失败后不调整成本倍率、Stage013 阈值、月份、品种、方向或 AI 池救参。

### Stage005 开始前反思

- 过拟合：否。该阶段不搜索参数，只对冻结候选施加预声明、单调更严的执行成本扰动。
- 继续价值：有。Stage013 会通过权益改变后续仓位和触发状态，只有真实引擎成本压力才能判断 Stage002 的收益保留与回撤改善是否可部署。

## Stage005 独立 review 后否决

- 独立 review：`P0=1/P1=2/P2=2`；数值正确性 `99%`，语义正确性 `20%`。
- P0：Stage013 触发读取策略内部 `portfolio_drawdown_pct`，该内部账在有成交时会重复计入仓位变化对应的昨收到今收 PnL，与引擎正式 `account_equity` 不守恒。
- 1x/2x/3x 分别有 `30/55、31/57、73/85` 次 gate 事件按正式日线回撤不足 30%，最低仅 `8.7792%/9.1583%/9.4283%`。
- 因此 Stage001/002/003/005 的 Stage013 收益和回撤改善虽然算术可复算，但建立在错误触发状态上，全部不得晋级或进入 shadow。
- Stage004 的 AI 720 天 warm-up、生效边界和 2022-01 至 2026-06 月历完整性不依赖账户账本，结论继续保留。
- Stage005 只验证单位滑点现金扣费敏感性，不代表完整执行仿真；lineage 和自动语义门也需补全。
- 决策：`stage005_fail_account_state_ledger_mismatch_no_shadow`。

## Stage006 权威权益对账修复预声明

- 不修改官方 C9 基类、实盘配置、AI、退出、止损重试、broker10、forced-margin 或 `30%/1个持仓/1手`。
- C 新增隔离的 Stage013 权威权益跟踪器，仅纠正 Stage013 gate 读取的账户权益；A 继续当前 C9，保证既有 C9 其他账户状态逻辑在 A/C 两侧相同。
- 权威权益按 vn.py 正式日结公式在线累计：昨日权益 + 昨日持仓盯市 PnL + 当日成交到收盘 PnL - 当日佣金与滑点；只能使用当前及过去 bar/trade。
- 实现上记录基础内部账相对正式账的累计重复项 `signed_volume * (今收-昨收) * size`，Stage013 gate 使用扣除该项后的权益和独立高水位；基础策略内部状态不被改写。
- 每个交易日的权威权益必须与最终持久化 `account_equity` 误差 `<=1e-8`；日期缺失、重复处理、position mismatch 或未来日期交易均为 0。
- 所有 gate 事件必须满足权威回撤 `>=30%`、`flat_entry`、`applied=1`、活跃持仓 `<=1`、最终 `1` 手；正式日线同日回撤低于 30% 的事件必须为 0。
- 先只跑 `2020-01-01 -> 2026-06-30` 一个冻结 A/C。沿用 Stage001 硬门：C 正收益、收益保留 `>=70%`，全周期/2022/固定压力窗回撤改善 `>=3/5/3pp`，broker10 不恶化。
- 独立 agent review 必须无 P0/P1。失败即关闭，不调整阈值、月份、品种、方向、成本或 AI 救参；只有通过才允许重新逐半年。

### Stage006 开始前反思

- 过拟合：否。修复目标由账户恒等式和独立 review 的 P0 决定，不使用收益结果选择规则。
- 继续价值：有。只有先恢复账户状态语义，才知道 Stage013 的真实回撤改善是否存在；继续扫描当前错误产物没有价值。

## Stage007 权威权益逐半年预声明

- 起点继续固定为 `2020-01/07` 到 `2026-01` 共 13 个独立冷启动；终点统一 `2026-06-30`，每条路径从 `150,000` 独立初始化。
- A：当前 AI + 当前 C9；C：A + Stage006 权威权益 Stage013，冻结 `30%/1个持仓/1手`，不调整任何策略参数。
- 成熟样本定义仍为交易日 `>=252`；成熟 C 必须全部正收益。
- 成熟起点中 C 最大回撤改善或持平比例 `>=80%`，且任何成熟起点回撤不得比 A 恶化超过 `3pp`。
- A 正收益成熟起点的 C/A 收益保留中位数 `>=70%`；`2020-01` 全路径收益保留 `>=70%`。
- C 跨起点最差最大回撤相对 A 至少改善 `3pp`；C 跨起点最差 broker10 不得高于 A。
- 每个 C 起点的权威权益必须逐日对齐正式 `account_equity`：缺失/重复日期、未来交易、事件权益不一致均为 0，权益/HWM/回撤误差 `<=1e-8`。
- 每条路径的 gate 事件必须全部满足正式回撤 `>=30%`、`flat_entry/applied/opened/active<=1/after=1`；全体路径至少有 1 次事件。
- A/C eligibility normalized hash 必须相同；所有候选行 AI enabled，missing signal date 为 0；原 720 天 OOS 政策边界继续沿用 Stage004 法证。
- 每个起点保存压缩后的 daily、entry candidates、trades、trade events、pilot events；不得只留汇总。
- 独立 agent review 必须无 P0/P1。失败即关闭，不调整 `30%/1/1`、起点、成熟定义、月份、品种、方向或 AI 救参。

### Stage007 开始前反思

- 过拟合：低。起点、成熟定义、绩效门和账本门都沿用运行前冻结口径，没有新增可搜索参数。
- 继续价值：有。单一起点已达到收益保留 `79.99%` 和 2022 回撤改善 `12.01pp`，必须用独立冷启动路径检验是否只是 2020 复利路径特例。

## Stage007 结果与独立审查

- 13 个起点、11 个成熟样本；成熟 C 正收益 `11/11`，回撤改善或持平 `11/11`，回撤恶化超过 3pp 为 `0`。
- 全路径收益保留 `79.9894%`；成熟保留率中位/最小 `100%/57.7149%`；跨起点最差回撤改善 `11.9037pp`。
- 13/13 reconciliation 通过，237 次事件全部满足正式回撤和冻结语义；A/C AI、月历和候选启用审计通过。
- 独立 review `P0=0/P1=0/P2=3`，数值/语义置信度约 `99%/94%`；允许进入单位滑点压力，不代表实盘。
- Stage007 预声明通过，但最终用户目标未完成：`2022-01` 冷启动 C/A 收益保留仅 `57.7149%`，虽然 C 正收益 `65.3752%`、回撤改善 `5.0619pp`。
- 已修 P2：终点后 audit 行不再被静默过滤；逐路径 frame 的 `start_month` 改为真实起点。零事件独立空文件和更完整聚合单测仍为产物 P2。
- 决策：`stage007_pass_reconciled_halfyear_but_final_2022_retention_goal_open`。

## Stage008 修复版单位滑点压力预声明

- A/C、当前 AI、Stage006 权威权益、Stage013 `30%/1个持仓/1手` 和 `2020-01-01 -> 2026-06-30` 全部冻结；不修改任何策略参数。
- 在引擎初始化前逐合约把单位滑点设为当前 metadata 的 `1x/2x/3x`；仅称 slippage sensitivity，不称完整执行验收。
- 每个倍率独立跑 A 当前 C9 与 C 权威权益 Stage013；A/C 使用同一缩放滑点表和同一数据。
- 1x A/C 必须与 Stage006 持久化核心日线一致。
- 每个倍率 C 必须正收益，且相对同成本 A 收益保留 `>=70%`。
- 每个倍率 C 相对 A 的全周期/2022/固定压力窗回撤至少改善 `3/5/3pp`，broker10 不得恶化。
- 每个 C 倍率的权威权益 reconciliation、pre-start、post-end、未来交易和事件语义门必须全部通过；AI normalized hash 必须一致。
- 独立 agent review 必须无 P0/P1；失败即关闭，不调整倍率、Stage013 阈值、月份、品种、方向或 AI 救参。
- 本阶段不解决 `2022-01=57.71%` 目标缺口；即使通过，也只能证明成本敏感性，最终目标保持未完成。

### Stage008 开始前反思

- 过拟合：否。成本倍率和所有门槛在运行前固定，不根据结果选择。
- 继续价值：有。需要确认权威权益修复后的回撤改善不是建立在单一乐观滑点假设上；但成本通过不能替代后续 2022 收益结构改进。

## Stage008 结果与独立审查

- 1x A/C：收益 `3897.7540%/3117.7905%`，保留 `79.9894%`，回撤 `-55.3701%/-40.0046%`，broker10 `88.3398%/88.3322%`，单档通过。
- 2x A/C：收益 `2480.6010%/1345.5562%`，保留 `54.2432%`，回撤 `-57.0944%/-42.2385%`，broker10 `86.7582%/91.4648%`，失败。
- 3x A/C：收益 `1814.2784%/1196.5047%`，保留 `65.9493%`，回撤 `-59.2247%/-44.3503%`，broker10 `90.7077%/93.3607%`，失败。
- 三档逐日权益、滑点、metadata、权威权益 reconciliation、gate 语义、AI parity 和 `62/62` manifest 均机械通过；1x 与 Stage006 核心逐日列完全一致。
- 独立 review：`P0=0/P1=1/P2=3`，数字/语义置信度 `99.5%/90%`。P1 是原 `year_2022` 窗口重置高水位，只能作局部归因；按全账户历史高水位，1x/2x/3x 的 2022 真实水下改善仅 `3.5171/1.1658/0.8601pp`。
- 决策：`stage008_fail_close_no_parameter_rescue`。不调整成本倍率、Stage013 阈值、月份、品种、方向或 AI 救参；最终 `2022-01` 收益保留 `57.7149%` 缺口仍未解决。

## Stage009 全起点机会成本归因预声明

- 本阶段是只读归因，不修改策略、不产生新候选收益曲线、不连接 CTP 或订单 API。
- 输入固定为 Stage007 的 13 个独立半年起点 A/C 原始 daily、entry candidates、trades、closed lots、pilot events 与 reconciliation；禁止只分析 `2022-01` 一条路径后制定规则。
- 对每个 C gate 事件，按 signal date、合约、方向映射下一交易日真实开仓及对应 closed lot；同时映射 A 同信号候选和成交。映射缺失、歧义、多重开仓必须显式报错，不静默丢弃。
- 只使用 gate 当时可见字段做分桶；事后 PnL 只作标签。输出每个起点、年份、原始手数、账户回撤、活跃持仓、既有 AI/OI/趋势质量字段下的避免亏损与压掉赢家机会成本。
- 回撤同时保存两种口径：`local_window_reset_hwm` 仅作局部归因；`account_history_hwm` 作为后续晋级主口径。不得再把前者写成账户真实水下幅度。
- 归因完成后最多预声明一个低自由度结构版本；不扫描年份、品种、方向、2/3/4 手、回撤阈值或多个质量阈值。
- 独立 agent 必须复核映射覆盖、时点可见性、PnL 线性反事实边界和结论置信度。

### Stage009 开始前反思

- 过拟合：低。全 13 起点统一归因，不先选择有利年份或品种；尚不改参数。
- 继续价值：有。`2022-01` 的 29 次 gate 中，2022 年 7 月两笔空头赢家解释了早期恢复机会，但恢复全部原仓位也会放大 2023-2024 亏损；必须跨起点判断固定 1 手是否系统性过度压缩，而不是对这两笔交易打补丁。

## Stage009 结果与独立审查

- 13 个起点的路径事件 `237/237`，C/A 均完整映射；下一交易日间隔 `1天=215/3天=19/春节10天=3`，同日重试各 5 次，开平量守恒。
- 237 是多起点路径事件，去重仅 `67` 个市场信号，其中 `63` 个重复；feature buckets 禁止用于选择 AI/OI 豁免。
- 全体同路径原手数线性值为 `-1,161,805` 毛 PnL；只有 `2022-01` 起点线性净差为正 `+7,750`，恢复全部原仓位没有跨周期依据。
- Account-history 2022 回撤改善：`2020-01 +3.5171pp`、`2021-01 -1.8693pp`、`2022-01 +2.4364pp`；旧 local-reset 对应为 `+12.0107/+8.8121/+2.4364pp`，以后只作辅助。
- 独立 review `P0=0/P1=1/P2=4`，数字/语义置信度 `99.5%/91%`。审查后的下一交易日、非空 signal 冲突、逐 open id 平仓量、部分平仓合成测试和 Stage007 manifest fail-closed 已补；毛损益和数据库非快照限制仍明确保留。
- 决策：`stage009_attribution_complete_one_structural_test_allowed`。固定2手和 AI/OI 豁免关闭，只允许一次参数无关的低水位恢复进度真实引擎证伪。

## Stage010 低水位恢复进度连续释放预声明

- 唯一候选，不扫描参数。A=当前 AI C9；C=A+Stage006 权威权益+连续恢复 ramp；不叠加 Stage013 固定 1 手 gate。
- 继续使用既有 `30%` 回撤触发、`active_positions<=1`、最小 `1` 手和原计划仓位上限，不新增年份、品种、方向、AI/OI、固定手数或恢复阈值。
- 每次账户回撤从低于 30% 跨入深回撤区，开始一个 episode；episode 内保存权威账户最大回撤 `episode_peak_dd`，回撤低于 30% 后清空。
- 当前回撤 `current_dd>=30%` 时，恢复进度为 `clip((episode_peak_dd-current_dd)/(episode_peak_dd-30%), 0, 1)`；分母为 0 时进度为 0。
- 对满足 `flat_entry/active<=1` 的原计划手数 `before`，`after = min(before, 1 + floor((before-1)*recovery_progress))`。新低时为 1 手，恢复到 30% 边界时回到原计划手数；全程向下取整。
- episode 状态只使用当前和历史权威权益，禁止读取未来 bar、未来 PnL 或事后交易标签；AI eligibility 与 A 完全相同。
- 首轮锚点固定为 `2020-01、2021-01、2022-01`，终点统一 `2026-06-30`。选择原因：长期复利、Stage009 发现的 2022 account-history 反例、最终收益保留缺口；不是挑有利样本。
- 三个锚点 A/C 都必须新进程真实引擎运行；A 必须逐日复现 Stage007 同起点基准。
- 三个 C 都必须正收益、相对 A 收益保留 `>=70%`、全期最大回撤至少改善 `3pp`、broker10 不恶化。
- 三个锚点在 2022 日历区间的 account-history HWM 最大回撤都必须优于 A；local-reset 只报告、不参与晋级。
- `2022-01` 必须从 Stage007 的 `57.7149%` 提升到 `>=70%`；不得用中位数掩盖该路径。
- 每个 C 的权威权益逐日 reconciliation、pre-start/in-range/post-end、未来交易、ramp 公式和 AI parity 必须通过。
- 独立 agent review 必须无 P0/P1。锚点任一硬门失败即关闭该公式，不改 rounding、触发线、最小手数、episode 定义或加入质量豁免；全部通过才允许 13 起点。

### Stage010 开始前反思

- 过拟合：低到中等。公式由账户状态和既有 30%/1手边界唯一确定，没有参数扫描；但方向是在看到 2022 恢复缺口后提出，必须由 2021-01 反例和后续全起点证伪。
- 继续价值：有。固定 1 手在深回撤中形成“风险太小所以难以回到触发线”的恢复死锁；连续 ramp 在新低仍防守，只在账户已经实际恢复时增加暴露。

## Stage010 结果与独立审查

- 三锚点真实引擎语义门通过：A 逐日复现 Stage007；C 权威权益 reconciliation、140 次 ramp 事件、3,984 个 episode 日状态、AI parity/usage、候选覆盖和 `64/64` manifest 全部通过。
- `2020-01`：收益保留 `77.8485%`，全期回撤改善 `15.3076pp`，2022 account-history 回撤改善 `3.4592pp`，通过。
- `2021-01`：收益保留 `80.4695%`，全期回撤改善 `10.8633pp`，但 2022 account-history 回撤恶化 `2.061718pp`；硬门失败。
- `2022-01`：收益保留 `93.6421%`，全期回撤改善 `6.5201pp`，2022 account-history 回撤改善 `2.4364pp`；相对 Stage007 固定 1 手的 `57.7149%` 收益保留明显修复。
- 独立 review：`P0=0/P1=1/P2=2`，数字/语义置信度 `99.5%/95%`。P1 确认 2021-01 反例真实，不能用 local-reset 的 `+7.4050pp` 掩盖；P2 假覆盖测试已加固，数据库未完整快照限制继续保留。
- 决策：`stage010_anchor_fail_close_formula_no_parameter_rescue`。不调整取整、30% 触发线、最小手数、episode 定义或质量豁免，不扩 13 起点。

## 当前线结论

- Stage005 揭示旧 Stage013 使用的内部权益账不守恒；Stage006 修复为权威权益后，Stage007 证明固定 1 手虽能降回撤，但 `2022-01` 收益保留只有 `57.7149%`。
- Stage008 在 `2x/3x` 单位滑点下收益保留和 broker10 失败；Stage009 证明 237 条路径事件只有 67 个去重市场信号，不能从 AI/OI 分桶挑豁免。
- Stage010 连续恢复 ramp 能修复 2022 独立起点收益，但在 2021 起点的 2022 主回撤口径反向恶化，最终目标仍未完成。
- 当前没有可晋级候选。Stage010 公式和本线真引擎试验停止；不在 07:00 前顺序尝试第二公式，避免隐性调参和重复既有失败路线。

## 运行后反思

- 过拟合：Stage010 没有参数扫描，但有明显周期偏向；继续按结果改公式会进入过拟合，因此停止。
- 继续价值：继续本公式或相邻账户回撤/手数/储备金变体无价值；未来只读归因 2021 反例仍有价值，若再开题必须预声明真正不同的信息源或结构。

## Stage011 2021 反例只读归因边界

- 本阶段只读取 Stage010 冻结的 `2021-01` A/C daily、candidates、trades、closed lots、ramp events 和 manifest；不修改策略、不生成候选收益曲线。
- 2022 回撤必须使用从 2021 独立起点累计的 account-history HWM；同时报告共同 2022 起始权益、各自峰谷日期和绝对权益差，禁止只解释百分比口径。
- 对 `2022-01-01 -> C的2022谷底` 的全部 opened signal 取并集，按 Stage009 的下一交易日、trade id、closed-lot 量守恒映射 A/C；直接 ramp signal 与后续非 ramp 路径分叉必须分开。
- 已结束交易的 realized PnL 只用于解释，不当作可执行反事实；谷底尚未结束仓位和每日 MTM 差异必须列为残差。
- 引擎在 `on_bars` 前先撮合旧订单；逐候选日把旧 `estimated_equity` 与同日正式 `account_equity` 对账，并用 `昨权益 + holding_pnl + trading_pnl - commission - slippage` 校验日结恒等式；不得把单笔赢家/输家反推成品种、方向或质量过滤器。
- Stage011 独立 review 必须复核映射覆盖、峰谷口径、直接/间接分类和账户恒等式。只有确认是系统性账户账本问题，才允许 Stage012 正确性 A/C；否则本线停止。

### Stage011 开始前反思

- 过拟合：低。只读解释冻结的唯一硬失败，不选择交易规则。
- 继续价值：有。需要区分 ramp 直接效果、保证金/heat/退出反馈和基础 sizing 账本错误，避免把系统交互误诊为阈值问题。

## Stage012 全局权威权益 sizing 正确性 A/C 预声明

- Stage012 是账户正确性修复，不是 Stage010 参数救援。A=当前 AI + 当前 C9；C=A+全局权威权益账，明确关闭 Stage013 固定1手和 Stage010 ramp。
- C 每笔成交后立即消除 Stage006 已识别的 `signed_volume * (今收-昨收) * size` 重复权益项，使后续 `estimated_equity`、sizing equity、free capital、保证金压力、portfolio drawdown 和 heat 状态使用同一正式账户账。
- 不修改风险比例、AI、品种池、止损重试、退出、heat 阈值、强平阈值、最大持仓、滑点、手续费、日期或取整规则。
- 首轮锚点固定 `2020-01、2021-01、2022-01`，终点 `2026-06-30`，初始资金 `150,000`；A 必须复现 Stage010/007。
- C 每日候选日的 `estimated_equity` 必须与候选生成时的同日 Stage006 authoritative equity / 正式 `account_equity` 对齐，并通过完整日结恒等式；最终日线继续按 vn.py 正式账户权益计算。缺日、重复日、未来交易、post-end、成交修正遗漏均为 0。
- 三锚点 C 都必须正收益、相对 A 收益保留 `>=70%`、全期最大回撤至少改善 `3pp`、broker10 不恶化。
- 三锚点 2022 account-history 最大回撤都必须优于 A；`2022-01` 不得用中位数掩盖，local-reset 只作辅助。
- 独立 agent review 必须无 P0/P1。任一锚点硬门失败即关闭，不调风险比例、资本利用率、阈值、方向、品种或日期；全部通过才允许扩 13 起点。

### Stage012 开始前反思

- 过拟合：低。变更由账户恒等式和 Stage005/006 已验证的重复项决定，没有收益参数。
- 继续价值：有。当前 C9 候选日旧 `estimated_equity` 与同日正式权益存在十万级偏差；只有全局修正后，收益与回撤目标才建立在正确 sizing 账本上。

## Stage011 结果与独立审查

- `2021-01` A 的 2022 account-history 峰谷为 `2022-03-09 1,144,978.80 -> 2022-06-02 671,038.80`，回撤 `-41.392906%`；C 为 `2021-10-26 1,075,926.00 -> 2022-07-06 608,386.40`，回撤 `-43.454624%`。
- C 谷底相对 A 同日权益差 `-101,122.40`。谷底前信号并集 `35` 个：直接 ramp `19`、间接路径反馈 `16`；已结束 closed-lot 的 C-A 毛损益分别 `+109,980.00/-195,522.40`，合计解释 `-85,542.40`，残差 `-15,580.00` 保留为未平仓 MTM、成本和其他路径状态。
- Stage010 ramp 的直接减仓在该窗口总体有利；失败由更大的间接路径分叉覆盖，不能据此继续微调 ramp。
- A/C 候选日旧 `estimated_equity` 相对同日正式权益的偏差中位分别 `233,360/357,960`，最大 `289,740/417,870`；正式日结恒等式最大误差 `2.32831e-10`。根因是 position-change 昨收到今收 PnL 在旧内部账重复累计并反馈到 sizing。
- 首轮独立 review 发现候选时点误用前一日正式权益的 P1，已改为同日 `account_equity` 并重建；复审 `P0=0/P1=0/P2=2`，数字/语义置信度 `99.8%/98%`。
- 决策：`stage011_global_legacy_equity_sizing_bug_stage012_correctness_test_allowed`。只允许 Stage012 全局权威权益 sizing 正确性实验，不允许按赢家、品种、方向或日期选规则。

## Stage012 结果与独立审查

- A 复用 Stage010 冻结 current C9 并再次通过 reproduction；C 三个锚点均为新进程真实组合引擎，Stage013 固定1手和 Stage010 ramp 事件数均为 `0`。
- `2020-01` C：期末权益 `2,714,114.10`、收益 `1709.4094%`、最大回撤 `-44.8398%`、Sharpe `1.3218`、滑点 `322,870`、交易 `624`；收益保留 `43.8563%`，全期回撤改善 `10.5304pp`，收益硬失败。
- `2021-01` C：期末权益 `1,614,586.20`、收益 `976.3908%`、最大回撤 `-44.5864%`、Sharpe `1.2790`、滑点 `185,490`、交易 `500`；收益保留 `64.9616%`，全期回撤改善 `9.7315pp`，收益硬失败。
- `2022-01` C：期末权益 `207,673.20`、收益 `38.4488%`、最大回撤 `-46.6358%`、Sharpe `0.3916`、滑点 `20,640`、交易 `301`；收益保留 `33.9436%`，全期回撤恶化 `6.6537pp`，收益和回撤双失败。
- 三锚点 2022 account-history 回撤分别改善 `2.2777/0.5864/0.8277pp`，broker10 均未恶化，但不能掩盖三个收益门全部失败和 2022 起点全期回撤失败。
- 账户语义：`624/500/301` 笔成交逐笔即时修正；候选日 sizing 覆盖 `640/556/463` 天；reconciliation、日结恒等式、AI parity/usage 和 A reproduction 全通过。Stage011/012 manifest 分别 `10/10`、`62/62`，Stage005-012 测试 `41/41`。
- 首次运行的输出根错误已由 RED 测试修复；错根产物隔离到 `/var/tmp/vnpy_stage012_wrong_root_20260711_0500`，正确输出重新跑出且不引用错根目录。
- 独立 review：`P0=0/P1=0/P2=3`，数字/语义置信度 `99.9%/98%`，关闭置信度 `99.9%`。P2 为自动逐 trade id/同日唯一性硬门不足、Stage012 专属集成测试不足、lineage A 引用未显式列 stop-retry 且数据库未完整快照；独立逐笔复算和上游 manifest 已覆盖本次结论。
- 决策：`stage012_anchor_fail_close_no_parameter_rescue`。不得扩 13 起点、不得晋级、不得调整风险比例、资本利用率、阈值、方向、品种或日期救参。

## Stage012 后当前线结论

- 最终目标未完成：目前没有同时满足“每锚点收益保留 `>=70%`、回撤改善且 2022 路径不恶化”的候选。
- Stage010 固定恢复公式虽然在 `2020/2022` 有价值，但 2021 起点的 2022 account-history 回撤反向恶化；Stage012 正确账本则三个收益门全部失败，并在 2022 起点恶化全期回撤。
- 当前 C9 **研究回测内部** sizing 的高收益依赖一个已证实不守恒的旧权益账，不能再把该高收益称为正确账户语义下的有效绩效。
- 本线没有审计正式 CTP、券商账户权益或当前 live profile，因此上述结论不得直接外推成“实盘券商权益错误”；若要判断线上影响，必须另按 futures live SOP 做隔离审计。
- 本线停止继续真引擎变体。Stage012 的工程正确性发现保留，但该 C 候选不晋级、不接 shadow、不改正式配置。

## Stage012 后反思

- 过拟合：Stage011/012 本身不是过拟合，均由账户恒等式驱动且没有收益参数；但在结果出来后继续调风险比例、阈值、月份或品种会形成隐性参数搜索，因此停止。
- 继续价值：继续 Stage010/012 相邻账户回撤、固定手数、储备金或 sizing 小数变体无价值。未来只有真正不同、可预声明且不依赖旧错误账本的信息源/结构，才值得另开研究线。
- 数据边界：`history_database_snapshot_complete=false`，冻结产物和 manifest 可审计，但未来数据库变化后不承诺字节级复现。

## Stage013 正确账本 + guarded quality 25% 预声明

- 跨线可行域审计只保留一个未被单独真实引擎证伪的正交结构：上游冻结的 `AI rank 1-8 + selected_volume>1 + risk_multiplier<2` guarded quality 单腿，按 `floor(before*1.25)` 增加整数手数。
- A=旧 current C9 目标参照；B=Stage012 正确账本；C=B+guarded quality 25%。A/B 只复用 manifest 冻结产物，C 跑 `2020-01/2021-01/2022-01 -> 2026-06-30` 三条真引擎。
- 不使用 OI、xsmom、RSI、ceil、最小加1手、年份、月份、品种、方向或事后盈亏；Stage013 gate/ramp 继续关闭。
- 三锚点 C 相对 A 收益保留都必须 `>=70%`、全期回撤改善 `>=3pp`、2022 account-history 回撤改善、broker10 不恶化；且 C 收益必须逐锚点高于 B。
- 账户 reconciliation、即时 correction、候选日 sizing、quality 事件公式/映射、AI parity 和禁止事件均为硬门。
- 任一门失败即关闭，不调 `25%/rank/risk_multiplier/floor`，不加第二腿，不扩 13 起点。

### Stage013 开始前反思

- 过拟合：低到中等；参数和 selector 均来自本轮前冻结证据，但尚未在正确账本真引擎中验证。
- 继续价值：有；它针对 Stage012 的右尾不足，机制不同于已失败的账户回撤/手数公式。

## Stage013 结果、风险顺序修复与独立审查

- 05:41 初版被独立 review 判定 `P0=0/P1=1/P2=3`：quality 在 Stage830 broker10 cap 和同日 incremental gate 后加手，`15/236` 条计划越过 100% projected broker10，最高 `106.5336%`。初版产物隔离到 `/var/tmp/vnpy_stage013_pre_review_p1_20260711_0541`，不得用于结论。
- TDD 修复把 quality 请求移到基础 volume-tilt hook，先受 broker10 max-affordable 约束，再进入原 incremental gate，finalization 不二次加手；全线测试由 `45/45` 增至 `50/50`。
- 06:45 最终重跑有 `226` 条风险门后计划事件，请求增量 `1,643` 手、最终计划增量 `1,604` 手，5 条被 broker10 cap 缩减；cap ratio `226/226` 为 `1.0`，最大 projected broker10 `99.9914%`、违规 `0`。计划增量不是成交增量：保守映射 `222/226` 有后续开仓、`211/226` 首次成交量等于计划、4 条未成交、11 条不同。
- `2020-01` C：期末权益 `3,258,773.10`、收益 `2072.5154%`、最大回撤 `-42.1405%`、Sharpe `1.3636`、滑点 `365,670`、交易 `624`；C/A 收益保留 `53.1720%`、回撤改善 `13.2296pp`，收益硬失败。
- `2021-01` C：期末权益 `1,993,959.60`、收益 `1229.3064%`、最大回撤 `-43.2564%`、Sharpe `1.3392`、滑点 `226,690`、交易 `501`；C/A 保留 `81.7887%`、回撤改善 `11.0616pp`，通过该锚点。
- `2022-01` C：期末权益 `383,674.50`、收益 `155.7830%`、最大回撤 `-45.4853%`、Sharpe `0.7575`、滑点 `37,050`、交易 `330`；C/A 保留 `137.5292%`，但全期和 2022 account-history 回撤分别恶化 `5.5032/0.2912pp`，双回撤硬失败。
- C 三锚点收益都高于 Stage012 正确账本 B，说明 quality 单腿能恢复右尾；但 2020 收益保留与 2022 回撤不能同时满足，最终 `performance_ok=false`。
- 权威权益、即时 correction、候选 sizing、AI `504行/55个eval_date`、PIT、A/B 冻结复用、Stage012/013 manifest `62/62、47/47` 和 lineage SHA 均闭合；数据库未完整快照的限制保留。
- 最终独立增量 review：`P0=0/P1=0/P2=1`，数字/语义/fail-close 置信度 `99.99%/99%/99.99%`；唯一 P2 是计划增量不等于成交增量，已在记录中明确披露。
- 决策：`stage013_anchor_fail_close_no_parameter_rescue`。不扩 13 起点、不调 `25%/rank/risk_multiplier/floor`、不加 OI、不接正式版或 shadow。

## Stage013 后反思

- 过拟合：本阶段使用预先冻结的一条规则，发现 P1 后只修执行顺序和审计硬门，没有按收益救参；当前不是结果后调参。但继续针对 `2020/2022` 调比例、rank、年份、品种或方向会成为明显过拟合。
- 继续价值：Stage013 候选无继续晋级价值；它只保留“quality 暴露能恢复右尾，但单腿不能跨周期压回撤”的机制证据。当前线停止新真引擎变体。
- 07:00 截止审计：`2026-07-11 06:56 CST` 已停止全部实验；目标未完成（2020 收益保留 `53.1720%`、2022 全期回撤恶化 `5.5032pp`），后台研究进程 `0`，不得将本轮关闭误写为目标达成。
