# futures_trend_stage819_intraday_rules - Stage819候选分钟级规则研究线

## 定位

- 资产：商品期货。
- 上游版本：当前 primary official candidate `official_candidate_stage819_30w_am41_oi08_old_ai_long_tighter_stop_rsi95_v1`。
- 研究目标：基于 Stage819 候选版本的全周期逐笔交易，结合数据分析和分钟K线视觉复盘，挖掘能否用规则类、非AI的日内入场/出场机制提高收益或降低左尾。
- 独立性：本线只做 Stage819 候选的只读法证、规则设计和后续独立 A/C 验证；不得修改当前官方正式版 Stage372/20w，不得连接 CTP，不得调用下单。

## 研究假设

- Stage819 候选的主要价值来自更强右尾和更高进攻性，但问题是回撤尾部不稳。
- 分钟级规则如果有价值，应体现在“确认后入场、错了实时止损、允许有限多次尝试、让顺畅趋势继续运行”，而不是在日线级别死扛。
- 日内规则必须是可解释的价格/波动/成交量/OI规则，不使用 AI、机器学习、未来收益标签或事后最优点。

## 反过拟合约束

- 不按某一年、某品种、某方向、某几笔大赢家或大亏损反推专属规则。
- 不扫分钟窗口、小数阈值、ATR倍数或重试次数来救结果。
- 首阶段只允许固定少数通用规则形状：开盘区间突破、失败快速止损、有限重试、日内追踪止损、收盘前强制退出。
- 所有规则必须能在分钟K逐根推进时实时判定，不能用当日收盘后才知道的信息。
- 图谱和逐笔法证只能产生候选假设；进入策略前必须做冻结规则的真实引擎 A/C 和成本压力验证。

## 当前状态

- Stage099 已完成：按用户“继续”确认，把 C9/15w session daemon 从 `dry-run + submit disabled` 切为 scheduled `live-real`。两个用户级 launchd plist 已更新并重载：日盘 `08:55`、夜盘 `20:55` 均为 `--mode live-real --submit-mode live-real`，并持久注入 `OFFICIAL_LIVE_PHASE_D_REAL_SUBMIT_ENABLED=1`、`OFFICIAL_LIVE_PHASE_D_REAL_ADAPTER_IMPLEMENTED=1`、`OFFICIAL_LIVE_PHASE_D_SESSION_DAEMON_ENABLED=1` 与精确确认文本。切换前手动跑一轮 live-real/no-intent 单周期：Stage927 通过，但 Stage905 `executor_no_intents`、ready `0`，Stage931 因 `no_ready_stage905_intents` 阻断，订单 API `0`；账户 `confirmed_flat`、非零持仓 `0`、Stage906 aligned。安装后两个 launchd label 当前均 `not running`，未手动 kickstart 长跑日盘守护；夜盘会在 `20:55` 自动启动。收尾时手动 Stage927 已恢复为 `real_submit_permitted=0`，不影响 launchd 任务按自身 env 在会话内重新 arming。
- Stage096 已完成：当前 C9/15w 官方 live default 已部署会话级 dry-run 自动化执行骨架。新增 Stage930 session daemon，交易时段内自动发现关注合约、刷新生产只读 tick、刷新账户/持仓、运行 Stage904 日内监控、生成 Stage905 执行意图，并预留 Stage927/Stage931 真实提交闸门；新增 Stage931 CTP submit adapter，真实提交必须同时满足 Stage927 `real_submit_permitted=1`、`OFFICIAL_LIVE_PHASE_D_REAL_SUBMIT_ENABLED=1`、精确确认文本、kill switch 未激活、账户/持仓/意图强对账。新增日盘 `08:55`、夜盘 `20:55` 两个用户级 `launchd` 会话任务，当前均为 `dry-run + submit disabled`。13:41 CST 单轮实测已连接生产只读 CTP，订阅 `MA609.CZCE`，收到 `25` 条 tick，账户 `confirmed_flat`、非零持仓 `0`，Stage904 `intraday_monitor_ready`、Stage905 `executor_no_intents`、Stage906 `reconcile_aligned`，订单 API `0`。结论：自动化已从 timed report 推进到交易时段会话守护；但真实自动开平仓仍未武装，需单独通过 live-real arming gate 后才能启用。
- Stage095 已完成：当前 C9/15w 官方 live default 已部署本机定时自动化报告链路。新增 Stage929 timed-cycle 包装器和两个用户级 `launchd` 任务：`local.qmt-roll.official-live.15w.postclose` 每天 `16:35` 触发，`local.qmt-roll.official-live.15w.evening-report` 每天 `21:05` 触发；latest 报告固定写入 `examples/portfolio_backtesting/backtest_outputs/qmt_roll_official_live_15w_timed_cycle_latest_report.md`，latest summary 固定写入 `..._latest_summary.json`。Stage929 手动 plan-only 自检通过，`wrapper_exit_code=0`、`order_api_called_count=0`、账户 `balance/available=150000.449813`、非零持仓 `0`，Stage905 `executor_no_intents`。同时创建 Codex 线程心跳 `c9-15w-evening-report-summary`，用于 `21:10` 后读取 latest 报告并汇总。结论：现在不需要启动无限循环常驻进程；今晚会按时自动生成报告。但这仍是 shadow/read-only/dry-run/report 自动化，真实报单开关 `OFFICIAL_LIVE_PHASE_D_REAL_SUBMIT_ENABLED` 未开启，完全无人值守真实报单还需要 live-submit adapter review、Stage927 arming gate、账户/持仓强对账和 kill switch 最终验收。
- Stage094 已完成：按用户补齐实盘账户到 15 万后的要求，当前实盘默认从 C9/30w `official_live_stage847_c9_30w_stage819_05r_stop_retry_once` 切为 C9/15w `official_live_stage847_c9_15w_stage819_05r_stop_retry_once`，策略信号逻辑不变，仅部署资金口径和 shadow 冷启动日变化。新增 `OFFICIAL_LIVE_SHADOW_ANALYSIS_START_DATE=2026-06-16`，Stage901/909/903 默认 shadow 起点改为 `2026-06-16`，Stage922 增加冷启动保护，Stage902/913 增加 `analysis_start` 校验，Stage659/902/913/914/927 的 C9 判断改为 family 级别。Stage914 生产 CTP runtime preflight 通过；Stage907 生产只读账户刷新完成，账户 `balance/available=150000.449813`，持仓 `confirmed_flat`，订单 API 计数 `0`。2026-06-16 12:56 CST 目标日解析显示最新完成交易日仍为 `2026-06-15`，早于新 shadow 起点，因此状态为 `target_date_before_live_shadow_start_waiting_fail_closed`；Stage909 plan-only 已确认 2026-06-16 日终刷新会用 `--analysis-start 2026-06-16 --target-date 2026-06-16`。结论：当前官方 live default 已是 C9/15w 且账户资金匹配；从 2026-06-16 冷启动的第一笔理论交易，最早只能在 2026-06-16 日线数据完成并生成 pending order 后，于夜盘窗口 20:55/21:00 起进入 dry-run/执行闸门，不保证一定有信号。
- Stage079 已完成：按用户要求启动 C9 晋升官方正式流程，先做“正式候选注册”，不切 live default。新增 `examples/portfolio_backtesting/qmt_roll_official_candidate_stage847_c9_config.py`，登记版本 `official_candidate_stage847_c9_30w_stage819_05r_stop_retry_once_v1`，冻结 Stage819 30w 基底 + C2 intraday stop + broker10 cap + `0.5R` stop/retry once；修改 `examples/portfolio_backtesting/qmt_roll_official_live_config.py`，将 `OFFICIAL_CANDIDATE_PRIMARY_VERSION` 从 Stage819 30w 切到 C9，同时 `OFFICIAL_LIVE_VERSION` 继续保持 `official_live_stage372_20w_recovery_sleeve`。新增晋升闸门 `research/lines/futures_trend_stage819_intraday_rules/C9_OFFICIAL_PROMOTION_GATE.md`，明确 C9 已可做 primary official candidate，但仍需风险接受、注册后 A/C、最新 shadow、dry-run、broker-state reconciliation、Stage847 wrapper 工程化后才能讨论 live default。静态验证通过：`py_compile` 成功，C9 manifest 可导入，候选清单共 `4` 个，C9 参数为 `0.5R / 1次重试 / 300000`。结论：C9 已从研究版本晋升为官方 primary candidate；这不是正式实盘默认切换。过拟合边界：本阶段不新增参数、不扫 R 倍数/重试次数/月份/品种/方向；风险仍集中在 Stage896/899 的 `-56%/-58%` 回撤尾和 broker10 over-100，后续只能推进 shadow/dry-run/风险接受/工程化。
- Stage078 已完成：按用户要求先做两件事：补齐 Stage898 C9 entry-day 分钟K缺口，并重跑 Stage863/896/897/898/899 核心验证链。新增 `examples/portfolio_backtesting/analyze_qmt_roll_stage900_stage898_c9_gap_backfill.py`，修改 Stage861 full-minute 合并逻辑，使 Stage900 C9 缺口补丁不再被 Stage861 基准 lot 样本过滤；修改 Stage899 并行初始化，让 worker 使用 pid 专属 static universe/eligibility CSV 副本，消除多进程重写同一路径导致的 `EmptyDataError`。Stage900 去重后 symbol-date `8` 个，覆盖 `8/8`，本地缓存 `6` 个、TqBacktest `2` 个，补丁分钟K `2,999` 行。Stage861 重建后 full minute bars `1,482,591`、symbols `220`，Stage819 基准 entry-day `341/341`、pressure key dates `19/19`，并确认 8 个 C9 缺口日全部进入统一分钟源。Stage863 新口径 C9 全周期：期末权益 `51,297,786.20`、总收益 `16,999.2621%`、最大回撤 `-41.6664%`、Sharpe `1.6404`、总滑点 `3,646,200`、总交易次数 `790`、胜率 `53.5299%`、max broker10 `115.0507%`；C10 budget lock 仍与 C9 完全一致，不晋级。Stage896 完整 3 年窗口 C9 `7/7` 正收益，收益中位 `562.2128%`、Sharpe 多数胜 Stage372，但最差回撤 `-56.1208%`、DD40/DD50 `4/1`、broker100 `2`，决策仍是 `stage896_c9_right_tail_with_risk_tail_not_official_replacement`。Stage897 完整 1 年窗口 `12/15` 正收益，仍有 `2018-01/2018-06/2022-01` 三个负窗口。Stage898 复审已清零：`metric_fail_count=0`、`p0_fail_count=0`、`c9_open_missing_full_minute_entry_day_count=0`，决策 `pass_with_execution_semantics_watch`。Stage899 月度起点：全部 `101` 个起点中 `99` 个曾转正，成熟 1 年以上 `89/89` 曾转正且当前全部正收益；最长转正等待 `2018-03` 起点 `158` 日历日 / `108` 交易日，未转正仍有 `2026-04` 和 `2026-05` 两个近端窗口，全月度最差回撤 `-58.0872%`。结论：旧“8 笔 entry-day 缺口”已不再是 C9 否决理由，但 C9 仍不能直接晋升正式默认；后续只做风险尾、broker10、近期未转正和执行语义归因，不做 R 倍数、重试次数、月份、品种、方向救参。
- Stage077 已完成：做 Stage900 C9 深度逐笔可信度审计，决策 `c9_high_return_no_hard_bias_found_in_stage900_checks`。新增脚本 `examples/portfolio_backtesting/audit_qmt_roll_stage900_c9_deep_trade_forensics.py`，只读 Stage863/898 既有输出，不重跑策略优化、不改 C9 规则、不连接 CTP、不调用下单。当前本地 Stage863 C9 全周期 `2018-01-02 -> 2026-05-29`：期末权益 `51,297,786.20`、总收益 `16,999.2621%`、最大回撤 `-41.6664%`、Sharpe `1.6404`、交易 `790`、非零日胜率 `53.5299%`、max broker10 `115.0507%`。Stage900 未发现硬未来函数证据：原始开仓 `333` 行中 `signal_after_fill_bug_count=0`，日内事件 `217` 行中分钟K缺失 `0`、方向性触发失败 `0`，交易数与 summary diff `0`；当前本地 Stage898 P0 完整性检查 `5` 项失败 `0`，Stage073 旧“8 笔 entry-day 缺口”不再适用于当前输出。但 C9 仍不能宣称逐笔盘口级无偏差：fallback 原始开仓 `109` 笔、非 fallback 代理窗口缺复核 `16` 笔、日内阈值价未被采样 OHLC 直接包含 `176/217`、degenerate minute bar `213/217`，top10 winners 占净 PnL `75.4692%`。结论：C9 高收益更像趋势右尾复利和进攻性 sizing 放大，不像明显 bug；后续不应继续调参，若要提高可信度，应对 top PnL / top overshoot / fallback 样本做 tick 或交易所原始分钟源交叉核验。
- Stage076 已完成：基于 Stage899 已有 curves 生成逐月起点资金曲线图，不重跑回测。读取 `101` 个逐月起点、`103,684` 行曲线，输出 `4` 张 PNG：全体归一净值 log 图、按启动年份分面归一净值 log 图、启动后前 `180` 天归一净值线性图、绝对权益 log 补充图。图中高亮 Stage075 的转正前最大浮亏窗口 `2022-04` 和全窗口最大回撤窗口 `2020-07`。结论：逐月资金曲线已经可视化，长期右尾复利强，但冷启动体验差异明显；该图形不消除 Stage898 的 `8` 笔 entry-day 分钟K缺口。
- Stage075 已完成：基于 Stage899 已有逐月起点 curve/summary 做二次统计，不重跑回测。全部 `101` 个逐月起点中 `99` 个曾经转正；按“首次转正前从归一净值 1 承受的最大浮亏”口径，最差为 `2022-04` 起点：最低归一净值 `0.766188`，最低日 `2022-06-29`，首次转正日 `2022-07-12`，等待 `102` 日历日 / `67` 交易日，转正前最大浮亏 `-23.3812%`，期末收益 `562.7523%`。成熟 1 年以上窗口 `89/89` 曾经转正，最差转正前浮亏同为 `-23.3812%`。若口径改为“最终转正窗口整个持有期内最大回撤”，最差为 `2020-07` 起点的 `-56.6137%`，这是转正后的持有期风险，不是冷启动转正前浮亏。结论：有数据、无需重跑；该结果只回答持有体验，不能消除 Stage898 的 `8` 笔 entry-day 分钟K缺口。
- Stage072 已完成：做 Stage897 C9 Jan/Jun 1 年滚动冷启动审计，决策 `stage897_c9_rolling1y_has_negative_windows_not_annual_all_positive`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage897_c9_janjun_rolling1y.py`，按用户要求从 `2018-01-01` 起，每年 `1月/6月` 为起点，窗口长度 `1` 年；本地最新可回测数据到 `2026-05-29`，所以完整 1 年窗口 `15` 个，另有 `2025-06 -> 2026-05-29` 与 `2026-01 -> 2026-05-29` 两个 partial 只作观察。正式版 Stage372/20w 在 `2018-01` 起点返回空结果，不能覆盖 2018 起点要求，因此本阶段只跑 C9，不改正式配置、不连接 CTP、不调用下单、不触发 A/B。完整窗口 C9 正收益 `12/15=80.00%`，收益中位 `60.7455%`，p10 `-12.6720%`，最小 `-20.8667%`，最大 `499.4970%`；负窗口为 `2018-01=-20.6533%`、`2018-06=-20.8667%`、`2022-01=-0.6999%`。风险上最差回撤 `-42.5759%`，DD30 失败 `4`、DD40 失败 `1`、DD50 失败 `0`、broker100 失败 `0`、peak broker10 `91.9780%`。结论：C9 不能说“每个 1 年周期都正收益”；但年度层面没有 broker100/DD50，后续若继续，只能做 2018 与 2022-01 负窗口只读归因，不允许按年份、品种、方向、R 倍数或重试次数补丁化。
- Stage071 已完成：做 Stage896 C9 vs 当前正式版 Stage372/20万的半年步进 3 年滚动测试，决策 `stage896_c9_right_tail_with_risk_tail_not_official_replacement`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage896_c9_vs_official_halfyear_rolling3y.py`，从 `2020-01-01` 起半年一个起点，完整 3 年窗口 `7` 个，另含 `2023-07-01 -> 2026-05-29` terminal partial 只作补充；每个窗口独立冷启动重放，不做净值事后切片，不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单、不触发 A/B。完整窗口中 C9 收益胜 `7/7`、Sharpe 胜 `6/7`，收益中位 `474.7780%` 高于正式版 `259.6375%`，Sharpe 中位 `1.6840` 高于 `1.3596`；但回撤只胜 `1/7`，DD40 失败 `5` vs 正式版 `0`，DD50 失败 `3` vs `0`，broker100 失败 `3` vs `0`，peak broker10 `122.0570%` vs `78.5348%`，broker10 胜 `0/7`。结论：C9 的右尾收益价值真实，但不是正式版替代品；后续若继续，只能做风险尾部只读归因，重点解释 `2020-01/2020-07/2021-01` 等 DD50/broker100 失败窗口，不允许继续扫 R 倍数、重试次数、月份、品种、方向或窗口长度救参。
- Stage070 已完成：做 Stage894 目标覆盖与剩余路线审计，决策 `stage894_goal_coverage_audit_no_promotable_minute_rule_keep_goal_active`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage894_stage893_goal_coverage_audit.py`，只读取 Stage861 decision、Stage891 route matrix/scorecard/visual index/decision、Stage893 decision；不新增交易规则、不跑回测、不接真实引擎、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单、不下载数据、不触发 A/B。审计结果：目标要求 `8` 行，其中研究线隔离、全周期逐笔数据、K线视觉、非AI规则约束均为 `proven`；C9 的实时止损+重试为 `tested_not_promoted`；但 `promotable_minute_improvement=not_proven`、`external_market_breadth=data_missing`、`goal_completion=keep_active`。路线 disposition 共 `13` 行，`proxy_rejected` `4` 条、`true_engine_failed` `3` 条，另有 `data_prerequisite_missing`。结论：当前分钟 K 小变体路线已经充分反证，不能继续在 `first60/OR/R/OI/volume/session` 上救参；目标不能标记完成，因为没有可推广分钟规则，且市场广度仍需先建全市场连续分钟面板。
- Stage069 已完成：做 Stage893 全市场分钟面板本地可行性审计，决策 `stage893_local_market_panel_not_available_no_breadth_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage893_stage892_market_panel_feasibility.py`，只读取 Stage892 C9 features、Stage861 full minute bars、Stage859 gap raw 与本地 `downloaded_futures` 下所有现有分钟 CSV；不下载数据、不新增交易规则、不接真实引擎、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单、不触发 A/B。结果：C9 closed lots `401`、唯一 entry_date `299`、扫描本地分钟 CSV `1,671` 个、日线 CSV `4,289` 个。Stage861 事件面板同日可用合约数 `min=0/p25=1/median=3/p75=6/max=10`，达到 `20` 合约要求为 `0/299`；本地 downloaded minute union `min=0/p25=12/median=14/p75=17/max=23`，达到 `20` 合约要求仅 `12/299=4.0134%`。结论：本地已有分钟文件不足以构建全市场连续 first60 广度面板；不得把最少合约数降到 `10/14/17` 或只取覆盖好的年份/品种来制造规则。市场广度方向若继续，必须先独立构建全市场连续分钟面板；若不补数据，本线应转账户级非交易层生存线或暂停。
- Stage068 已完成：做 Stage892 全市场 first60 广度可行性审计，决策 `stage892_market_breadth_data_scope_not_broad_enough_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage892_stage891_market_breadth_audit.py`，读取 Stage889 C9 features 与 Stage861 full minute bars；固定用每个 entry_date 同日所有合约最早 `60` 根分钟K计算信号方向同向比例，最少合约数 `20`、广度中轴 `50%`，不扫描分钟窗口、广度阈值、品种族群、方向或年份；不新增交易规则、不接真实引擎、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单、不触发 A/B。结果：C9 closed lots `401`，market daily rows `1,408`，但 `market_breadth_missing_lot_pct=100.0%`；所有 `MB1-MB4` 固定 exit60 代理均触发 `0` 笔。K线 atlas page001 显示代表性样本实际可用广度合约数只有 `n=1/3/5` 等，说明 Stage861 full minute bars 对目标交易是完整事件面板，不是全市场连续面板。结论：不能用当前 Stage861 数据源研究市场广度规则；不得把最少合约数降到 `1/3/5` 或按品种/年份救参。若未来继续市场广度，必须先另建全市场连续分钟面板；否则本线应转账户级非交易层生存线或暂停。
- Stage067 已完成：做 Stage891 日内分钟规则路线级证据收束，决策 `stage891_intraday_route_closed_no_promotable_minute_rule_yet`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage891_stage890_intraday_route_closure.py`，只读取 Stage861/863 与 Stage876/878-890 的既有 decision、comparison、proxy、atlas manifest；生成 route matrix、scorecard、visual index、summary chart 和 report；不新增交易规则、不接真实引擎、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单、不触发 A/B。结果：Stage861 全周期 entry-day 分钟K覆盖 `341/341=100%`、pressure key dates `19/19=100%`；Stage891 visual index 统计 `10` 组 manifest、`95` 页 PNG。路线矩阵确认：C9 是唯一有正价值骨架，相对 C4 期末权益 `+4,621,339.60`、最大回撤改善 `+4.5602pp`、Sharpe `+0.031584`，但 max broker10 `114.3987%` 高于 C4；Stage879 早段 OI guard 期末权益相对 C9 `-8,646,381.40`；Stage882 pyramiding 虽多赚 `+84,248,252.10` 但 max broker10 `203.4450%`、DD 恶化 `-19.0568pp`；Stage883 sleeve Sharpe 下降且 max broker10 `127.4316%`；Stage889/890 最优代理分别仅 `+72,650` 和 `+90,000`。结论：当前候选的全周期逐笔分钟K数据分析与视觉审计已完整，但没有可推广、可 A/B 的新增分钟规则；停止 `first60/OR15/R倍数/成交量/OI/sleeve手数/品种/方向/年份` 救参。后续若继续，只能先设计新的低自由度外生信息源或账户级非交易层生存线，且必须证明仍服务于本研究目标。
- Stage066 已完成：做 Stage890 first60 price/OI/volume 三元参与度审计，决策 `stage890_first60_volume_triad_tiny_positive_proxy_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage890_stage889_first60_volume_triad_audit.py`，读取 Stage889 C9 loss-shape features 与 Stage861 full minute bars；只新增 `early_volume_state = volume_expanded / volume_faded_or_equal / volume_missing` 和 `early_triad_state = price_side + oi_side + volume_state` 的只读标签，并做 `V1-V5` 第 60 根退出代理；不新增交易规则、不接真实引擎、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单、不触发 A/B。结果：C9 closed lots `401`，base closed-lot PnL `53,950,264.60`；非 missing 成交量逆向桶极稀疏，`price_adverse__oi_down__volume_expanded` 仅 `1` 笔，`price_adverse__oi_up__volume_faded_or_equal` 仅 `1` 笔，`price_adverse__oi_down__volume_faded_or_equal` 仅 `3` 笔；最好的 `V4_exit60_adverse_oi_up_volume_faded` 只触发 `1` 笔、delta `+90,000.00`、正年份 `1`，`V5` 触发 `3` 笔、delta `+10,770.90`，`V2/V3` 各 `-155,000.00`。结论：成交量三元参与度只保留为复盘解释标签，不进入真实引擎；停止成交量倍数、first60 长度、OI 阈值、品种、方向或年份救参。后续若继续本线，应做路线级收束，或转账户级非交易层生存线/更强低自由度外生信息源。
- Stage065 已完成：做 Stage889 C9 本体亏损分钟形态覆盖审计，决策 `stage889_c9_loss_shape_tiny_positive_proxy_year_fragile_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage889_stage863_c9_loss_shape_coverage_audit.py`，读取 Stage863 C9 closed lots/trades/stop-retry events 与 Stage861 full minute bars；按已冻结低自由度维度 `0.5R`、`1R`、`OR15`、`first60`、entry-day close 做 per-lot shape coverage、EOD/first60/OR proxy、retry role summary 和 K线 atlas；不新增交易规则、不接真实引擎、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单、不触发 A/B。结果：C9 closed lots `401`，总 PnL `53,950,264.60`，loser PnL `-36,510,760.40`，winner PnL `90,461,025.00`；`adverse_first_05r` 覆盖亏损 PnL `54.0518%` 但同时覆盖 winner PnL `17,971,900.00`；`early60_adverse_any_oi` 覆盖亏损 PnL `53.0324%` 但 winner PnL `16,804,470.00`；`progress_first_close_below_entry` EOD proxy `-2,421,820.00`，`progress_first_close_adverse_half` EOD proxy `-2,274,295.00`，`early60_adverse_any_oi` exit60 proxy `-6,389,771.30`，`OR15 extended_gt_1or` skip proxy `-3,242,482.40`。唯一正代理 `neither +/-0.5R + close below entry` 仅 `+72,650.00`，正年份 `1`、负年份 `4`，幅度和稳定性都不足。结论：C9 分钟K本体剩余形态不产生可推广新规则；停止在 `0.5R/1R/OR15/first60/EOD close` 上做小变体，后续若继续本线，应换新的低自由度外生信息源或账户级非交易层生存线。
- Stage064 已完成：做 Stage888 Stage881-887 pyramiding/sleeve 路线收束审计，决策 `stage888_pyramiding_sleeve_route_closed_no_more_param_rescue`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage888_stage887_pyramiding_route_closure.py`，只读取 Stage881-887 既有 decision、comparison、pressure bucket、shape/gate summary 和 atlas manifest；不新增交易规则、不重跑新引擎、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单、不触发 A/B。结果：Stage881 proxy 证明右尾参与真实，`176` 个候选、proxy delta `+34,513,422.10`；但 Stage882 同手数真实加仓虽然相对 C9 多赚 `+84,248,252.10`，max broker10 升到 `203.4450%`、最大回撤恶化 `-19.0568pp`、Sharpe 相对 C9 `-0.0572`；Stage883 1 手 sleeve 只多赚 `+1,046,670.05`，Sharpe 相对 C9 `-0.008835`，max broker10 仍到 `127.4316%`；Stage886 pressure exit proxy `-2,853,650.00`，Stage887 最宽 pressure gate skip proxy `-114,051.15`。结论：`+0.5R progress` 只是右尾参与度标签，不能继续转成新增仓规则；pressure/OI/弱收只保留为复盘标签，不是退出或禁加仓规则。pyramiding/sleeve 分支正式关闭，不扫 progress R、加仓比例、1/2/3 手 sleeve、止损位置、heat 阈值、产品方向、年份或分钟窗口。下一步若继续本线，应回到 C9 本体寻找新的低自由度外生信息源，或转账户级非交易层生存线。
- Stage063 已完成：做 Stage887 Stage883 sleeve 触发压力闸门只读审计，决策 `stage887_sleeve_pressure_gate_not_promoted_proxy_cost_or_too_blunt`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage887_stage883_sleeve_pressure_gate_audit.py`，只读取 Stage883 pyramid events / entry risk / closed lots、Stage885 daily pressure state 和 Stage861 full minute bars；不新增交易规则、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单。固定 gate 只使用 Stage885 既定 `80%` account heat 与前一交易日 pressure state：`G0_prev_pressure`、`G1_pre_add_heat80`、`G2_projected_after_add_heat80`、`G3/G4` 组合 gate、`G5_prev_pressure_same_product_direction`；`A0/A1` current-day pressure 仅作后验归因。结果：Stage883 pyramid events `175`，addon PnL sum `220,565.15`；最宽的 `G2/G3/G4` 阻断 `79` 次、覆盖后来 pressure events `18` 次，但 skip proxy `-114,051.15`，loser saved `56,695.80`，winner cut `-170,746.95`；后续参与 same-product pressure 的 `11` 笔 sleeve 本身仍净赚 `+29,792.40`。结论：前置 heat/pressure gate 也无法低误伤治理 sleeve 分子扩张；sleeve/pyramiding 救援路线停止，不进入真实引擎、不触发 A/B、不扫 heat 阈值、产品方向、年份或分钟窗口。下一步应做 Stage881-887 pyramiding/sleeve route closure，或回到 C9 本体寻找新的低自由度外生信息源/账户级非交易层生存线。
- Stage062 已完成：做 Stage886 pressure-state 内分钟级失败结构只读审计，决策 `stage886_pressure_failure_shape_not_trade_rule_mixed_or_right_tail_cost`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage886_stage885_pressure_failure_structure_audit.py`，只读取 Stage885 C17 pressure days、Stage883 C17 closed lots 和 Stage861 full minute bars；不新增交易规则、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单。固定结构只用 `directional_close_return_pct`、`close_location_signal_side`、`adverse_dominates_progress` 与 OI 正负，不扫描分钟窗口、小数阈值、品种、方向或年份。结果：pressure top-product active lot rows `61`、pressure days `25`、products `5`、missing minute rows `10`；`price_failure_shape` `22` 行、`9` 天，remaining PnL after focus close `+2,853,650`，EOD exit proxy delta `-2,853,650`，loser saved `499,380`，winner cut `-3,353,030`，median next20 return `22.1098%`，negative next20 share `11.1111%`。结论：高压下当日分钟级收弱/区间弱位/逆向主导仍不是退出充分条件；不接真实引擎、不触发 A/B、不做 `price_failure_shape` 阈值救参。后续如果继续，应从“平掉既有右尾”转向“新增 sleeve 前的组合压力闸门只读审计”。
- Stage061 已完成：做 Stage885 全路径持仓压力状态只读审计，决策 `stage885_pressure_state_real_but_not_trade_rule_mixed_outcomes`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage885_stage884_holding_pressure_state_audit.py`，C4/C9 closed lots 读取 Stage863，C17 closed lots 读取 Stage883，按固定状态定义 `broker10>=80% + top1产品方向broker10>=35% + top3产品方向margin share>=0.70` 审计所有交易日；不新增交易规则、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单。结果：C4 pressure `21` 天、C9 pressure `22` 天、C17 pressure `25` 天，其中 C17-only pressure `9` 天；但 C17 pressure 后续 `20` 日 median return `16.9511%`、negative next20 share 仅 `8.0000%`，C9 pressure median next20 return 也为正 `10.8325%`。结论：holding pressure state 是真实风险标签，但不是交易规则；不能写“高压就退出/减仓”，否则会砍右尾。后续若继续，只能在 pressure state 内做更细的分钟级失败结构只读复盘，不扫账户 heat/top1/top3 阈值、品种、方向或年份。
- Stage060 已完成：做 Stage884 Stage883/C17 broker10 路径归因，决策 `stage884_broker10_has_exposure_signal_needs_readonly_followup`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage884_stage883_broker10_path_forensics.py`，只读取 Stage883 输出和 Stage861 分钟K做分子/分母拆解、active lot product-direction 归因、累计 PnL 缺口归因和分钟K atlas；不新增交易规则、不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单。结果：C17 top10 broker10 峰值中 `10/10` 为 `exposure_numerator_expansion`；`2020-10-15` C17 broker10 `127.4316%` vs C9 `78.3721%`，delta `+49.0595pp`，其中 exposure effect `+42.5172pp`；`2020-10-16` exposure effect `+38.4969pp`；后续 `2020-11-23`、`2021-02-23`、`2022-02-16` 等日期即使总 delta 被更高权益分母抵消，exposure effect 仍为 `+18pp` 到 `+22pp`。产品方向归因显示 `jm.DCE long`、`ru long`、`CF.CZCE long`、`AP.CZCE short`、`rb.SHFE long` 等峰值日 active exposure 扩大明显，但这不是品种黑名单证据。结论：Stage883/C17 的 broker10 恶化本质是新增/保留持仓保证金分子扩大，不是 C13 那类权益分母压缩；pyramiding/sleeve 分支停止得更坚决，不扫手数、R、止损、品种、方向、年份或 broker10 日期。
- Stage059 已完成：做 Stage883 C9 `+0.5R` 顺势加仓固定 1 手 sleeve 真实组合引擎审计，决策 `stage883_progress_pyramid_sleeve1_engine_not_promoted`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage883_stage882_progress_pyramid_sleeve1_engine.py`，只新增隔离 C17：C9 保持不变；若入场日先触达 `+0.5R` progress 且没有先触达 `-0.5R` adverse，则在 `+0.5R` 合成最多 `1` 手 add-on sleeve；新增仓止损为原始入场价，入场日回打即合成平仓，否则进入后续日线退出路径；不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单。结果：C17 期末权益 `51,683,814.65`，比 C9 多 `1,046,670.05`；最大回撤 `-41.1625%`，比 C9 改善 `+1.4687pp`；但 Sharpe 降到 `1.6223`，比 C9 低 `0.0088`；max broker10 升至 `127.4316%`，C9 为 `114.3987%`；总滑点 `3,921,000`、交易 `1,051`、胜率 `53.4863%`。事件上固定 1 手 sleeve 触发 `175` 次，入场日未止损 `99` 次，入场日止损 `76` 次，合成 lot realized PnL `+220,620.15`。视觉复核显示 open 样本确实在 `+0.5R` 后小额参与，stopped 样本也按原入场价实时止损；但路径图显示 broker10 峰值仍被新增右尾参与推高。结论：固定 1 手 sleeve 是比 Stage882 同手数加仓更合理的账户预算形状，但仍不能穿越保证金生存线；停止 pyramiding/sleeve 分支，不扫 `2手/3手`、progress R、止损位置、品种、方向或年份。
- Stage058 已完成：做 Stage882 C9 `+0.5R` 顺势加仓真实组合引擎审计，决策 `stage882_progress_pyramid_true_engine_not_promoted`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage882_stage881_progress_pyramid_engine.py`，只新增隔离 C16：C9 保持不变；若入场日先触达 `+0.5R` progress 而不是先触达 `-0.5R` adverse，则按 `+0.5R` 合成同手数加仓一次；新增仓止损为原始入场价，入场日回打即合成平仓，否则进入后续日线退出路径；不改 Stage372 正式版、不改官方候选配置、不连接 CTP、不调用下单。结果：C16 期末权益 `134,885,396.7`，比 C9 多 `84,248,252.1`，总收益 `44,861.7989%`；但最大回撤恶化到 `-61.6881%`，比 C9 差 `-19.0568pp`；Sharpe 降到 `1.5740`，比 C9 低 `0.0572`；max broker10 升至 `203.4450%`，C9 为 `114.3987%`；总滑点 `8,279,150`、交易 `1,045`、胜率 `51.6784%`。事件上 `171` 次加仓，`97` 次入场日未止损，`74` 次入场日止损，合成加仓开仓 `171` 笔全部匹配成交表。视觉复核显示未止损样本确实增厚右尾，但路径图显示 2020 年先出现 `-61.6881%` 深回撤和 broker10 `203.4450%` 峰值。结论：Stage881 代理不是假信号，但同手数真实加仓不具备实盘候选意义；停止本 pyramiding 分支，不扫 progress R、加仓比例、止损位置、品种、方向或年份。后续若继续，只能转账户级右尾参与预算/生存线，而不是救 C16。
- Stage057 已完成：做 Stage881 C9 `+0.5R` 顺势加仓只读代理审计，决策 `stage881_progress_pyramid_proxy_only_needs_true_engine_before_any_promotion`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage881_stage863_progress_pyramid_proxy_audit.py`，读取 Stage863 C9 closed lots 与 Stage861 full minute bars；不改策略、不接真实引擎、不连接 CTP、不调用下单。规则代理固定为：若入场日先触达 `+0.5R` progress，则假设在 `+0.5R` 价位加一笔同手数仓，新增仓止损放在原始入场价；若 entry-day 触发则止损，否则按原始 C9 closed lot 最终退出价退出。结果：closed lots `401`，有效 entry-day 分钟K `393`，pyramid candidates `176 = 43.8903%`；base closed-lot PnL `53,950,264.6`，proxy delta `+34,513,422.1`，proxy closed-lot PnL `88,463,686.7`；`held_to_original_exit` `99` 笔贡献 `+39,780,918.6`，`entry_day_stop` `76` 笔亏 `-5,072,496.5`，`same_bar_stop` `1` 笔亏 `-195,000.0`；年度 `2022` `-170,845.1`、`2026` `-261,204.4` 为负。结论：这是当前少数强正上限线索，但只是代理，不含新增仓保证金、资金联动和后续逐日止损；只允许下一步做一次冻结真实引擎，不触发 A/B、不进入正式候选，不扫 `0.25R/0.5R/1R`、加仓比例、止损位置、品种、方向或年份。
- Stage056 已完成：做 Stage880 C9 时段边界只读审计，决策 `stage880_session_boundary_same_session_retry_not_promoted_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage880_stage863_session_boundary_audit.py`，读取 Stage863 C9 stop/retry events 与 closed lots、Stage861 full minute bars，只按固定交易制度时段做标签：`pre_day_night=00:00-02:45`、`day_session=09:00-15:15`、`post_day_night=21:00-23:59`；不改策略、不接引擎、不连接 CTP、不调用下单。结果：C9 stop/retry events `121` 个，跨时段重试 `9` 个，全部为 `day_session -> post_day_night`；这 9 个原始 matched PnL 为 `+1,138,795.0`。若做 `P1_same_session_only_retry`，即首次 `0.5R` 止损后只允许同连续时段内重试，代理 delta 为 `-1,533,365.0`，winner_cut `-1,816,900.0`，loser_saved 仅 `+283,535.0`，big_winner_cut `0.0`。K线视觉复核显示 `sp2205` 等日盘止损后夜盘重试贡献恢复右尾，禁止跨时段会误伤。结论：时段边界只保留为复盘标签，不推广“禁止夜盘重试/禁止跨时段重试”，不做开盘/收盘分钟小窗口扫描。
- Stage055 已完成：做 Stage879 早段 OI 参与度冻结真实引擎审计，决策 `stage879_early_oi_guard_not_promoted`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage879_stage878_early_oi_guard_engine.py`，复用 Stage863 C4/C9 同口径基准，只新增 C15：C9 保持不变；若入场日最早 `60` 根1分钟K没有触达 `+0.5R` progress，且第 `60` 根时信号方向价格收益为负、OI变化为负，则按第 `60` 根收盘价退出，当天不重试。C15 触发早段 OI guard `14` 次、volume `2,551`；期末权益 `41,990,763.2`，比 C9 少 `8,646,381.4`、比 C4 少 `4,025,041.8`；最大回撤 `-35.4692%`，相对 C9 改善 `+7.1620pp`，但 Sharpe 降到 `1.5681`，max broker10 升到 `119.3842%`。视觉复核显示规则确实按第60根附近退出，但 path chart 显示它压低长期权益分母并推高 broker10。结论：早段 OI-down no-progress 直接退出不推广，不做滚动起点和成本压力；Stage878 的 OI/参与度只保留为复盘/解释标签，不再继续扫分钟窗口、OI阈值、成交量阈值、品种、方向或年份。
- Stage054 已完成：做 Stage878 入场日早段价格/OI参与度只读审计，决策 `stage878_early_oi_participation_has_signal_no_engine_yet`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage878_stage861_early_oi_participation_audit.py`，读取 Stage861 entry lot features 与 full minute bars，固定使用入场日最早 `60` 根1分钟K，按信号方向价格顺/逆向与 OI 增/减做四象限分桶；不改策略、不接引擎、不连接 CTP、不调用下单。结果：`favorable_price_oi_up` `122` 笔贡献 PnL `+30,581,100`，胜率 `57.3770%`，big winner `22`，是当前最清晰的右尾参与度标签；`adverse_price_oi_up` `70` 笔 PnL `-1,991,820`，`adverse_price_oi_down` `82` 笔 PnL `-5,577,190`。但只读退出代理仍有明显右尾代价：`P2_exit_adverse_price_any_oi` 表面 delta `+7,569,010`，同时 winner_cut `-12,102,545`、big_winner_cut `-2,111,510`；`P3_exit_non_favorable_price_oi_up` 更会砍 `-21,694,425` 赢家。视觉复核显示 `adverse_price_oi_up` 同时含左尾和 `OI201` 这类早段逆向后修复的大赢家，`favorable_price_oi_up` 也含早段强参与但后续失败的亏损。结论：OI/参与度是一个新的低自由度信息维度，有条件进入一次冻结真实引擎设计审计；但不能直接推广，且不得扫描 `30/60/90/120`、OI 小数阈值、成交量阈值、品种、方向或年份。
- Stage053 已完成：做 Stage877 分钟规则路线覆盖审计，决策 `stage877_intraday_rule_route_covered_no_promotable_rule_yet`。本阶段不新增脚本、不新增交易规则，只汇总 Stage861-876 的当前证据：Stage861 已完成全周期 entry-day 分钟K覆盖 `341/341=100%`，full minute bars `1,479,592` 根、`216` symbols，entry atlas `57` 页、pressure atlas `3` 页；Stage863 确认 C9 `0.5R stop + reclaim retry once` 是唯一正价值骨架，C9 期末权益 `50,637,144.6`、总收益 `16,779.0482%`、最大回撤 `-42.6313%`、Sharpe `1.631178`，相对 C4 期末权益 `+4,621,339.6`、Sharpe `+0.031584`，但 max broker10 从 C4 `111.4255%` 升至 C9 `114.3987%`，days over 100pct 从 `4` 升至 `7`，不能推广。Stage867-876 又连续反证高热 no-retry、close-confirm retry、retry_failed cooldown、progress-confirm、固定止盈、利润锁定、二次重试、重试后无进展 EOD 退出、OR extension 追价过滤。结论：当前目标中的“研究线、全周期逐笔数据分析、K线视觉、非AI规则审计”已覆盖；但“找到可推广、可 A/B 的分钟入场/出场增收规则”尚未被证明。若继续，不应再在同一组 OR/R/重试/确认/锁盈特征上做小变体，只能转账户/持仓层生存线或外生低自由度信息源。
- Stage052 已完成：做 Stage876 OR extension 追价只读审计，决策 `stage876_or_extension_chase_proxy_not_promoted_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage876_stage861_or_extension_chase_audit.py`，读取 Stage861 entry lot features 与 full minute bars，按固定 `OR15` 和 `1x OR width` 计算入场价相对信号侧开盘区间边界的扩展，不改策略、不接引擎、不连接 CTP、不调用下单。结果：Stage861 entry lots `341`，base realized PnL `+28,171,880`；`inside_or_or_opposite` `260` 笔，PnL `+30,565,390`，big winner `28`；`edge_to_1or` `37` 笔，PnL `-1,472,105`；`extended_gt_1or` `43` 笔，PnL `-1,271,055`。`P3_block_all_beyond_edge` skip proxy 表面 delta `+2,743,160`，但 winner_cut `-5,378,765`、big_winner_cut `-1,538,700`，且年度上 `2021` `-934,640`、`2020` `-352,520`、`2026` `-165,310`。视觉复核显示它能抓到 `fu2205/jm2301/SH409` 等追价左尾，但也会误伤 `OI201/sp2205/au2412` 等 beyond-edge 延展赢家。结论：不接 OR extension 真实引擎，停止 OR 分钟数、OR width 倍数、追价阈值、品种、方向或年份扫描。
- Stage051 已完成：做 Stage875 C9 `open_after_reentry` 后日内进展/持仓只读审计，决策 `stage875_reentry_no_progress_eod_exit_rejected_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage875_stage863_reentry_progress_hold_audit.py`，读取 Stage863 C9 stop/retry events、closed lots 与 Stage861 full minute bars，审计重试后同日未触达 `+0.5R progress` 的持仓若按 EOD 退出会怎样；不改策略、不接引擎、不连接 CTP、不调用下单。结果：C9 `open_after_reentry` `26` 笔全部匹配 closed lots，其中 `12` 笔 `no_progress_after_reentry` 实际后续 PnL `+1,783,150`，EOD 盯市代理仅 `+138,400`，若 EOD 退出会少赚 `1,644,750`；全部 `26` 笔若 EOD 退出会少赚 `3,027,100`。最大误伤来自 `OI201.CZCE` long，`2021-09-24` 重试后当天无 `+0.5R progress`，但实际后续 `+2,090,000`，单笔 EOD 代理少赚 `2,121,920`。视觉复核显示个别样本 EOD 退出能救亏损，但右尾误伤压倒左尾修复。结论：不接“重试后未进展 EOD 退出”引擎，停止 progress 阈值、等待窗口、EOD 平仓、品种、方向或年份扫描。
- Stage050 已完成：做 Stage874 C9 `retry_failed` 后同日第二次重试可行性只读审计，决策 `stage874_second_retry_not_promoted_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage874_stage863_second_retry_feasibility_audit.py`，读取 Stage863 全量分钟K口径 C9 stop/retry events 与 Stage861 full minute bars，只看 C9 `flat_retry_failed` 后同日剩余分钟K是否再次 reclaim 原入场价，并在 reclaim 后是否再次触发同一个 `0.5R` stop；不改策略、不接引擎、不连接 CTP、不调用下单。结果：C9 `flat_retry_failed` `25` 笔，同日第二次 reclaim `9` 笔，占 `36.0000%`；其中 `7` 笔再次触发 stop，二次重试条件失败率 `77.7778%`；只有 `2` 笔保持到日内结束，第二次 reclaim 后达到 `+0.5R progress` 仅 `3` 笔；二次失败额外亏损保守估计 `-218,210`，两笔 open_after_second_reentry 的日内收盘盯市 `+61,020`，保守 same-day proxy `-157,190`。视觉复核显示部分 EOD 反弹样本在实时纪律下会先被第二次 stop 打掉，不能用后验收盘救参。结论：不接 Stage875 二次重试真实引擎，停止重试次数分支，不扫 `2/3` 次、等待分钟数、R 小数、品种、方向或年份。
- Stage049 已完成：做 Stage873 C9 `+2R 后锁 +1R` 真实逐分钟利润锁定引擎验证，决策 `stage873_profit_lock_not_promoted`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage873_c9_profit_lock_real_engine.py`，在 C9 基础上按每个 live layer 的实际 entry 与原始 stop distance 计算 R：逐分钟先触及 `+2R` 后把保护位上移到 `+1R`，之后逐分钟触及 `+1R` 合成平仓；同一根分钟K触发与回落只激活不出场。重跑前修正持仓日锁盈状态的时间顺序保护，修正后 `activation_time > exit_time` 为 `0`。结果：C14 期末权益 `38,695,654.4`，比 C9 少 `11,941,490.2`；总收益 `12,798.5515%`，低于 C9 `16,779.0482%`；最大回撤 `-42.8439%`，比 C9 差 `0.2127pp`；Sharpe `1.6288`，低于 C9 `1.6312`；max broker10 `119.6549%`，高于 C9 `114.3987%`；profit lock events `62`，其中 entry-day `30`。视觉复核显示 C14 确实按分钟K锁住部分盈利，但同时持续削弱 2021/2022 后右尾复利，权益分母下降还推高 broker10 百分比。结论：Stage872 的乐观锁盈上限无法落成真实引擎；停止利润锁定分支，不扫 R、小数阈值、品种、方向或年份；不进入官方候选、不触发 A/B。
- Stage048 已完成：做 Stage872 C9 右尾保护与利润锁定代理审计，决策 `stage872_fixed_takeprofit_rejected_profit_lock_upper_bound_promising_needs_real_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage872_c9_profit_lock_proxy_audit.py`，读取 Stage870 C9 closed lots 与 Stage861 完整分钟K，对 `1R/2R/4R/8R` 固定止盈、保本和锁盈上限做全周期逐笔代理审计，并生成 summary chart 与 4 页分钟K atlas；不改 Stage372 官方正式版、不改 Stage819 候选配置、不连接 CTP、不调用下单。结果：C9 lots `401`，有效代理样本 `230`，valid sample PnL `49,601,777.3`，big winners `21` 笔、PnL `35,808,560.0`。固定止盈全线净负：`1R` delta `-29,726,373.8`、`2R` delta `-22,984,986.4`、`4R` delta `-23,201,181.6`、`8R` delta `-6,116,950.8`，且 `8R` 仍会触发全部 `21` 个 big winners、winner cut `-20,831,746.8`。乐观锁盈上限中 `+2R 后锁 +1R` delta `+16,002,662.8`，触发 `45` 笔且不触发 big winners；但这是上限，不计入途中洗出赢家的真实代价。视觉复核显示大赢家右尾依赖持仓后延展，固定止盈会截断；锁盈样本确有先到 `+2R` 后回落形态；低 MFE 左尾则无法靠利润保护解决。结论：固定止盈路线停止；利润锁定仅允许一次冻结真实引擎验证，不得继续扫 R、小数阈值、品种、方向或年份。
- Stage047 已完成：做 Stage871 Stage870 broker10 路径只读归因，决策 `stage871_broker10_worse_mainly_equity_denominator_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage871_stage870_broker10_path_forensics.py`，读取 Stage870 C4/C9/C13 的 curve、closed lots、entry risk 与 Stage861 完整分钟K，对 C13 top10 broker10 峰值做分子/分母分解、product-direction active lot 归因、累计 realized PnL 缺口归因和 3 页分钟K atlas；不改策略、不改 Stage372 官方正式版、不改 Stage819 候选配置、不连接 CTP、不调用下单。结果：C13 top10 broker10 峰值中 `8/10` 是 `equity_denominator_compression`，`1/10` 是 `exposure_numerator_expansion`，`1/10` 是 `mixed_or_lower_than_c9`。C13 top1 `2020-11-23` broker10 `120.7738%` vs C9 `114.3987%`，但 C13 broker10 margin 比 C9 少 `178,842.84`，主要来自 C13 equity / C9 equity 只有 `0.7842`；top2 `2020-12-01` 也类似，C13 broker10 margin 比 C9 少 `46,491.72`，但权益分母只有 `0.7627`。唯一明显分子扩张是 `2020-09-09 MA.CZCE long`，属于单个早期路径，不足以写通用规则。结论：Stage870 的 broker10 恶化主要是 progress-confirm 砍掉前序右尾、压低权益分母造成，不能反推新的仓位缩手或 progress 参数小变体；停止 C13 作为后续引擎基础，若继续应回到 C9 的右尾保留机制，做赢家保护/独立生存线只读审计。
- Stage046 已完成：做 Stage870 `progress-confirm recovery` 真实组合引擎验证，决策 `stage870_progress_confirm_recovery_not_promoted`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage870_stage847_progress_confirm_recovery_engine.py`，在 C9 基础上把重入语义改为：首次 `0.5R` adverse stop 先退出；之后同日只有触及原入场方向 `+0.5R progress` 才合成重开；重开后若回到原入场价立刻合成止损；不改 Stage372 正式版、不改 Stage819 候选配置、不连接 CTP、不调用下单。结果：C13 期末权益 `46,668,137.3`，比 C9 少 `3,969,007.3`；最大回撤 `-38.7460%`，比 C9 改善 `+3.8853pp`；Sharpe `1.5783`，低于 C9 `1.6312`；max broker10 `120.7738%`，高于 C9 `114.3987%`。事件上 C13 将 C9 的 `51` 次重入压缩为 `29` 次 progress-confirm 重入，其中 `16` 次仍回到原入场价止损、`13` 次保持 open；`flat_no_progress_reentry` 增至 `96` 次。结论：更严格的 progress 确认可降低回撤，但明显牺牲收益和 Sharpe，并把 broker10 峰值推高；停止 progress-confirm recovery，不扫 progress R、recovery stop 位置、确认分钟数、品种、方向或年份。
- Stage045 已完成：做 Stage869 C9 `retry_failed` 后续同产品方向损伤只读审计，决策 `stage869_retry_failed_next_same_pd_cooldown_rejected_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage869_stage868_post_retry_damage_audit.py`，读取 Stage868 C9 stop/retry events、Stage865 C9 entry audit、Stage866 entry path features 与 Stage861 full minute bars，对每个 C9 stop/retry event 匹配事件日之后第一笔同 `product_vt_symbol + direction` 的 matched entry；不改 Stage372 正式版、不改 Stage819 候选配置、不连接 CTP、不调用下单。结果：C9 stop/retry events `121`，其中 `flat_retry_failed` `25`，有后续同产品同方向 entry `21`。若在 `retry_failed` 后阻断下一笔同产品同方向 entry，affected next PnL 为 `+11,000,934.0`，proxy delta `-11,000,934.0`；loser saved 只有 `+1,693,386.8`，但 winner cut `-12,694,320.8`，big winner cut `-9,221,240.0`，误伤 `3` 个 big winner。视觉 atlas 显示后续 entry 路径混杂：`lh/fu/MA` 有继续失败，但 `rb/jm/OI` 贡献大右尾。结论：二次失败是实时可知的“当前尝试失败”，不是后续同产品方向失效的充分条件；停止 stop/retry 派生 cooldown，不扫 cooldown 天数、broker10 阈值、年份、品种或方向。
- Stage044 已完成：做 Stage868 `close-confirm next-open retry` 真实组合引擎验证，决策 `stage868_close_confirm_retry_not_promoted`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage868_stage847_close_confirm_retry_engine.py`，在 C9 基础上把重试语义从“触碰原入场价即按原入场价重试”改成“分钟收盘重新站回原入场价后，下一分钟开盘重试一次”；不改 Stage372 正式版、不改 Stage819 候选配置、不连接 CTP、不调用下单。结果：C12 期末权益 `50,061,893.1`，低于 C9 `575,251.5`；最大回撤 `-43.1561%`，比 C9 差 `-0.5249pp`；Sharpe `1.6233`，低于 C9 `1.6312`；max broker10 `114.0579%`，只比 C9 `114.3987%` 改善 `0.3408pp`。事件上 C12 stop/retry `121` 笔，touch reclaim `51`，close-confirm reentries `51`，retry failed `25`，与 C9 的重试/失败数量本质一致；`51` 笔重试中 `40` 笔重试价不同于原入场价，重试确认延迟中位 `78` 根分钟K，但没有筛掉失败重试。结论：收盘确认后下一分钟重试没有抓住 C9 主要矛盾，只改变成交时间/价格；停止 close-confirm/retry 成交语义小变体，不扫确认窗口、分钟数、R、品种、方向或年份。
- Stage043 已完成：做 Stage867 高热 stop-first 不重试真实组合引擎验证，决策 `stage867_high_heat_no_retry_not_promoted`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage867_stage866_high_heat_no_retry_engine.py`，在 C9 基础上验证实时可执行近似：entry projected broker10 `>=90%` 且入场日先触发 `0.5R` adverse stop 时，立即平仓且当天不再按 reclaim 重试；不改 Stage372 正式版、不改 Stage819 候选配置、不连接 CTP、不调用下单。结果：C11 期末权益 `47,772,889.7`，低于 C9 `2,864,254.9`；最大回撤 `-43.1208%`，比 C9 差 `-0.4895pp`；Sharpe `1.6029`，低于 C9 `1.6312`；max broker10 从 C9 `114.3987%` 降至 `111.5707%`，但收益和风险调整收益损失过大。事件上 high-heat no-retry 仅 `9` 笔，其中 reclaim observed `5` 笔、retry_failed observed `2` 笔，说明 Stage866 的理想代理无法直接实时化：高热不是错误充分条件，粗暴禁重试会误伤可恢复路径。结论：停止高热 no-retry 分支，不扫 `90%`、`0.5R`、时间窗、品种、方向或年份；若继续，必须换一个新的实时可观测第一性结构。
- Stage042 已完成：做 Stage866 高热入场分钟路径只读审计，决策 `stage866_high_heat_minute_path_no_engine_yet`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage866_stage865_high_heat_minute_path_audit.py`，读取 Stage865 entry audit 和 Stage861 full minute bars，对 C9 matched entries 按入场日分钟路径分类为 `progress_first`、`stop_no_reclaim`、`stop_reclaim_no_second_stop`、`stop_reclaim_retry_failed`、`no_05r_event`、`missing`；不改 Stage372 正式版、不改 Stage819 候选配置、不连接 CTP、不调用下单。结果：已匹配 entries `327`，SBB0 高热 entries `23`，分钟K缺失 `8`。全样本 `progress_first` `168` 笔，matched PnL `47,890,100.8`，big winner `18` 笔；SBB0 高热中 `progress_first` `9` 笔，matched PnL `+482,660`，big winner `2` 笔，直接反证机械高热缩手。SBB0 高热中 `stop_reclaim_retry_failed` 只有 `4` 笔，matched PnL `-395,392.1`，big winner `0`，代理 `HH_NR1_retry_failed_only_no_retry` 的 PnL delta `+153,334.8`、大赢家削减 `0`；但样本太少，只能作为下一步一次冻结规则草案线索，不能直接进真实引擎、A/B 或正式候选。下一步只允许围绕“高热 + 先0.5R失败 + 回到入场附近 + 再次0.5R失败”的实时二次失败纪律做冻结语义，不得扩展为热度阈值、R小数、时间窗、品种、方向或年份扫描。
- Stage041 已完成：做 Stage865 账户层 sizing brake 只读代理审计，决策 `stage865_sizing_brake_proxy_too_blunt_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage865_stage864_sizing_brake_proxy_audit.py`，读取 Stage863 C9 `entry_risk/closed_lots/curve` 与 Stage864 峰值归因输出，审计 C9 下单时已知账户字段：投影 broker10、下单前 broker10、单笔新增 broker10、近期权益扩张，并生成 summary chart 与 4 页峰值前分钟K atlas；不改 Stage372 正式版、不改 Stage819 候选配置、不连接 CTP、不调用下单。结果：`334` 个 C9 entry 上下文，匹配 closed lots `327` 个。`SBB0_projected90_heat_buffer` 标记 `24/334=7.1856%`，平均缩手 `30.9046%`，峰值前 `7` 个唯一 entry 中命中 `3` 个，但代理 PnL delta `-152,059.1`，亏损修复 `+205,705.6` 小于赢家削减 `-357,764.7`，且大赢家削减 `-272,008.6`；`SBB1_nearcap90_largeadd20` 代理 PnL delta `-115,974.1`；`WATCH_single_add20` 覆盖 `183/334=54.7904%`、含 `11` 个大赢家，明显过宽。结论：账户热度缩手能碰到部分 broker10 峰值前风险单，但当前形状太钝，不能进真实引擎；下一步必须回到分钟级价格路径失败/二次失败纪律，不得继续扫 `90/20/50` 阈值。
- Stage040 已完成：做 Stage864 C9/C4 broker10 峰值只读归因和分钟K视觉复盘，决策 `stage864_peak_forensics_no_rule_yet`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage864_stage863_broker10_peak_forensics.py`，读取 Stage863 固定输出和 Stage861 全量分钟K，实际加载相关分钟K `1,448,736` 根、`214` 个合约；不改 Stage372 正式版、不改 Stage819 候选配置、不连接 CTP、不调用下单。结论：C4 broker10 top peak 为 `2022-04-07`，broker10 `111.4255%`；C9 top peak 为 `2020-11-23`，broker10 `114.3987%`。C9 峰值不是单个品种/日期/方向问题，而是早期 stop/retry 改变权益和资金占用路径后，后续相同信号在更高权益/更少约束下获得更大 sizing；`2020-11-23` 的 AP101 short、rb2101 long、OI101 long 分别较 C4 放大 `+4/+8/+1` 手，`2020-11-26` 还出现 C9-only `jm2101.DCE long` 贡献 broker10 pct `+40.6869pp`。C10 同品种同方向加仓锁无法命中这种路径；下一步只能做实时账户层 sizing brake 的只读反事实，禁止按峰值日期、品种或方向写黑名单。
- Stage039 已完成：做 Stage863 `C4/C9/C10` 同口径全量分钟K真实引擎重算，决策 `stage863_c10_no_effect_budget_lock_not_promoted`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage863_stage847_c10_budget_lock_engine.py`，使用 Stage861 full minute bars `1,479,592` 根、`216` symbols，重跑 C4、C9、C10；不改 Stage372 正式版、不改 Stage819 候选配置、不连接 CTP、不调用下单。结果：C4 期末权益 `46,015,805.0`、总收益 `15,238.6017%`、最大回撤 `-47.1915%`、Sharpe `1.5996`、总滑点 `3,023,410`、交易 `678`、胜率 `53.0630%`、max broker10 `111.4255%`；C9 期末权益 `50,637,144.6`、相对 C4 `+4,621,339.6`，最大回撤 `-42.6313%`、改善 `+4.5602pp`，Sharpe `1.6312`，但 max broker10 升至 `114.3987%`。C10 与 C9 完全重合：期末权益、回撤、Sharpe、滑点、交易数、broker10 均无差异；`budget_lock_created=121`，但 `budget_lock_effective_events=0`、`blocked=0`、`reduced_volume=0`。结论：同品种同方向 stop/retry 后加仓预算锁不是当前矛盾，C10 不晋级；C9 在全量分钟K口径下有价值，但下一步只能做 broker10 峰值归因，不能继续微调 C10。
- Stage038 已完成：做 Stage862 Stage861 完整分钟K规则假设审计，决策 `stage862_stop_retry_budget_lock_watch_structural_filters_rejected_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage862_stage861_rule_hypothesis_audit.py`，读取 Stage861 全覆盖 entry features/full minute bars/visual manifest、Stage847 C9 真实引擎上下文，并重算 `P1-P5` lot-level proxy、`S1-S4` 结构破坏规则、`H1-H6` 假设分类和 5 页视觉复核；不改策略、不接引擎、不连接 CTP、不调用下单。结果：base lot PnL `28,171,880`；`P2_stop05_retry_on_entry_reclaim` proxy delta `+6,558,265.1`、big winner delta `-983,330`，但 Stage847/C9 相对 C4 虽权益 `+6,871,220`、最大回撤恶化 `-2.4518pp`，所以只能作为 building block，不能 standalone 晋级。`no_same_day_recovery` `59` 笔 PnL `-13,030,405`，说明左尾清晰但该信息本身是 EOD hindsight；`no_stop05_hit` `171` 笔 PnL `+39,238,295`，要求未来规则不得误伤右尾。Stage842 子集曾有线索的 `S3_two_stop_side_closes_before_reclaim` 在 Stage861 完整覆盖下转负，full coverage delta `-566,700`、big winner delta `-5,276,980`；OR15 过滤、60m 1R 快速确认、S1-S4 结构破坏均不进入引擎。下一步若继续，只允许冻结设计 `H1 + 风险预算锁定 + 二次失败纪律` 的真实引擎；不得继续扫 OR、确认分钟窗、R 小数、重试次数、品种、方向或年份。
- Stage037 已完成：做 Stage861 Stage860 后完整分钟K视觉图谱重算，决策 `stage861_full_visual_atlas_complete_no_rule`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage861_stage860_full_visual_atlas.py`，读取 Stage825 closed lots、Stage849 pressure features、Stage860 combined patch minute bars，并与原 Stage825 minute sources 合并，重算全周期入场日分钟特征、分桶统计、entry atlas 和 pressure atlas；不改策略、不接引擎、不连接 CTP、不调用下单。结果：full minute bars `1,479,592`、symbols `216`，Stage860 patch minute bars `38,354`；Stage825 entry-day 覆盖 `341/341 = 100%`，Stage849 pressure key dates 覆盖 `19/19 = 100%`；entry atlas `57` 页，pressure atlas `3` 页。结论：全周期分钟视觉证据阻断已解除，但 Stage861 只证明证据完整，不证明任何日内规则；下一步必须做 Stage862 数据分桶 + K线人眼复核的一致性归纳，筛出少数低自由度、可实时执行、可被反证的规则假设。
- Stage036 已完成：做 Stage860 Stage859 raw 导入与完整覆盖重算，决策 `stage860_full_minute_coverage_restored_no_rule`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage860_stage859_full_coverage_import.py`，合并 Stage855 本地 raw patch 与 Stage859 TqBacktest raw，重算 Stage853 全部 gap requests、Stage825 entry-day closed lots 覆盖和 Stage849 pressure key dates 覆盖；不改策略、不接引擎、不连接 CTP、不调用下单。结果：Stage853 `126` 个 gap requests 全部覆盖，Stage855 贡献 `29`、Stage859 贡献 `97`，剩余缺口 `0`；combined patch minute bars `38,354`、symbols `80`。Stage825 entry-day 覆盖从原始 `227/341 = 66.5689%` 恢复到 `341/341 = 100%`，其中 2018 `25/25`、2019 `45/45`；Stage849 pressure key dates 从原始 `7/19 = 36.8421%` 恢复到 `19/19 = 100%`。结论：Stage034 的覆盖偏差阻断已解除，但 Stage860 只证明数据覆盖，不证明任何日内规则；下一步必须用 combined patch minute bars 重算全量视觉特征和 K 线图谱。
- Stage035 已完成：做 Stage859 Stage856 剩余分钟K缺口替代源侦察与 TqBacktest 全量补抽，决策 `stage859_tqsdk_backtest_gap_backfill_full_success_no_rule`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage859_stage856_tqsdk_backtest_gap_backfill.py`，先扫描本地非日线分钟 cache 与 AKShare/Sina，再用 `TqBacktest + get_kline_serial(60)` 对 Stage856 剩余 exact contract/date 缺口全量补抽；不改策略、不接引擎、不连接 CTP、不调用下单。结果：Stage856 剩余 `97` 个请求、`91` 个 `(vt_symbol, required_date)` batch，TqBacktest `91/91` 成功、失败 `0`，覆盖 entry-day `90/90`、pressure key date `7/7`，覆盖 priority abs PnL `6,434,115`、big-winner requests `6`，新增 `31,118` 根分钟K、`65` 个合约，目标日期最少 `225` 根分钟K。本地非日线 cache 和 AKShare/Sina 对目标日期新增覆盖均为 `0`，主补数路径确认为 TqBacktest 回放。结论：Stage034 的数据阻断已在 raw 层解除，但这仍不是交易规则证据；下一步必须 Stage860 导入 Stage859 raw、重算覆盖和偏差，再 Stage861 重画全量 K 线图谱。
- Stage034 已完成：做 Stage858 Stage857 后覆盖偏差审计，决策 `stage858_coverage_bias_severe_no_rule`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage858_stage857_coverage_bias_audit.py`，只读取 Stage855/856/857/849 既有输出，按 covered / missing 审计年份、品种、方向、盈亏、big winner 和 pressure episode 覆盖偏差；不改策略、不接引擎、不连接 CTP、不调用下单。Stage855 后 entry-day 分钟K覆盖 `251/341 = 73.6070%`，仍缺 `90` 笔；2018 年 `0/25`、2019 年 `0/45` 完全无覆盖；未覆盖样本绝对 PnL `6,087,275`，含 `6/31` 个 big winner；Stage849 pressure key dates 覆盖 `12/19 = 63.1579%`，仍缺 `7` 个 key dates，pressure paired lots 缺 `3/8`。结论：当前 covered subset 存在系统性时间/右尾/压力段偏差，不能生成新的全周期分钟级入场/出场规则；下一步只能补数/替代分钟源，或把后续分析严格限定为 `2020+ covered subset` 的局部假设且不能声称全周期成立。
- Stage033 已完成：做 Stage857 Stage855 新增覆盖分钟K视觉图谱，决策 `stage857_stage855_patch_visual_atlas_no_rule`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage857_stage855_patch_visual_atlas.py`，将 Stage855 本地 raw patch 新增覆盖的 `24` 笔 Stage825 entry-day lots 和 `5` 个 Stage849 pressure key dates 转成特征表与 K线图谱；不改策略、不接引擎、不连接 CTP、不调用下单。结果生成 `7,236` 根 patch minute bars 的新增视觉证据：entry atlas `4` 页、pressure atlas `1` 页；新增 entry lots PnL 合计 `1,440,860`，含 `2` 个 big winner。新增样本显示 `1R target_first` 桶盈利强，但 `0.5R stop_first`、`30m fail_fast` 桶里仍混有 big winner，继续反证简单 no-retry/fail-fast；新增 `fu2209 2022-04-18/19` 压力图谱显示持仓后连续不利路径，不是单纯入场分钟确认问题。结论：Stage857 是视觉证据补强，不允许生成新规则；下一步应补数或做覆盖偏差审计。
- Stage032 已完成：做 Stage856 Stage855 后剩余分钟K缺口下载尝试，决策 `stage856_remaining_gap_download_attempt_no_rule`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage856_stage855_remaining_gap_download.py`，按 Stage855 `remaining_download_manifest` 用 TqSdk `DataDownloader` 尝试下载剩余 exact contract/date 分钟K；不改策略、不接引擎、不连接 CTP、不调用下单。结果 `84/84` 个批次全部失败，失败原因一致为当前 TqSdk 账号不支持下载历史数据功能；downloaded minute bars `0`、newly covered requests `0`。覆盖状态保持 Stage855 后水平：Stage825 closed lots 入场日分钟K覆盖 `251/341 = 73.6070%`，Stage849 压力 key dates 覆盖 `12/19 = 63.1579%`；剩余 gap requests `97`，绝对 PnL 影响 `6,434,115`，含 `6` 个 big-winner。结论：这是数据权限阻断，不是策略证据；补数完成前不允许写新的日内交易规则或引擎。下一步只能开通历史下载权限后重跑 Stage856，或寻找替代分钟源/本机 raw，优先补 `FG209/fu2205/fu2209/rb2210/FG601/AP210/lc2401` 等关键缺口。
- Stage031 已完成：做 Stage855 Stage854 本地 raw 分钟K导入与覆盖重算，决策 `stage855_local_raw_patch_imported_coverage_improved_no_rule`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage855_stage854_local_raw_import.py`，只读取 Stage854 `local_import_manifest` 指向的本地 raw CSV 并抽取 exact `required_date` 分钟K，不下载数据、不改策略、不接引擎、不连接 CTP、不调用下单。导入 patch minute bars `7,236` 根，覆盖 Stage853 gap requests `29/126`，涉及 `18` 个合约、`26` 个合约日期，覆盖缺口绝对 PnL `9,378,200`，含 `2` 个 big-winner；剩余 gap requests `97`，绝对 PnL `6,434,115`，含 `6` 个 big-winner，仍需 `84` 个下载批次。Stage825 closed lots 入场日分钟K覆盖从 `227/341 = 66.5689%` 提升到 `251/341 = 73.6070%`，增量 `24` 笔；Stage849 压力 key dates 覆盖从 `7/19 = 36.8421%` 提升到 `12/19 = 63.1579%`，增量 `5` 个 key dates。新增覆盖了 `fu_long_20220325_0401` 的 `2022-03-25`、`fu_long_20220418_0419` 的 `2022-04-18/19`、`fu_long_20220506_0509` 的 `2022-05-06/09`；仍缺 `FG209.CZCE 2022-05-24/27/06-02`、`fu2205.SHFE 2022-03-30/04-01`、`fu2209.SHFE 2022-05-27/31` 等压力关键日期。结论：这是证据覆盖改善，不是策略突破；下一步只能下载剩余 exact 合约日期并重跑 Stage825/849 图谱，补完前仍不允许写新交易规则或引擎。
- Stage030 已完成：做 Stage854 Stage853 后分钟K补数清单与本地 raw 预检，决策 `stage854_local_raw_partial_recovery_then_download_manifest_no_rule`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage854_stage853_minute_fetch_manifest.py`，只读取 Stage853 缺口与本地 `downloaded_futures/*` raw minute CSV，不下载数据、不改策略、不接引擎、不连接 CTP、不调用下单。Stage853 `126` 个 exact contract/date gap requests 中，本地 raw 已可恢复 `29` 个、涉及 `18` 个合约，影响绝对 PnL `9,378,200`，含 `2` 个 big-winner；仍需下载 `97` 个、涉及 `65` 个合约，拆成 `84` 个下载批次，影响绝对 PnL `6,434,115`，含 `6` 个 big-winner。raw 命中主要来自 `tqsdk_stage452_true_path_fallback_1455`、`tqsdk_stage504_next_real_open_fallback_backfill`、`tqsdk_stage506_next_real_forward_risk_signal_frontier`。本地可恢复高影响包括 `ru2501.SHFE 2024-09-12`、`hc2210.SHFE 2022-07-07`、`fu2205.SHFE 2022-03-25`、`ru2605.SHFE 2026-02-13`、`fu2209.SHFE 2022-05-06`；仍需下载高影响包括 `rb2210.SHFE 2022-07-07`、`FG601.CZCE 2025-11-05`、`fu2205.SHFE 2022-03-30/2022-04-01`、`AP210.CZCE 2022-04-06`、`lc2401.GFEX 2023-11-07`、`fu2209.SHFE 2022-05-27/2022-05-31`。结论：先合并本地 raw 可恢复分钟K，再按 manifest 下载剩余 exact 合约日期；补完并重跑 Stage825/849 图谱前，仍不允许写交易规则或引擎。
- Stage029 已完成：做 Stage853 Stage852 后分钟K缺口根因审计，决策 `stage853_minute_gap_mostly_true_missing_contract_or_date_no_rule`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage853_stage852_minute_gap_audit.py`，只读取 Stage825/849/852 输出和现有两个分钟源，不下载数据、不改策略、不接引擎、不连接 CTP、不调用下单。Stage825 entry-day missing `114` 个 + Stage849 pressure missing `12` 个，去重后 gap requests `126`；exact contract/date 已在源内可用为 `0`，需要补数 `126`，涉及 `80` 个 exact symbols，缺口绝对 PnL 影响 `15,812,315`，big-winner requests `8`。根因为 `exact_contract_missing_all_sources` `79` 个、`exact_contract_missing_required_date` `46` 个、`exact_contract_missing_required_date_but_product_date_exists` `1` 个。补数优先级靠前为 `ru2501.SHFE`、`fu2209.SHFE`、`fu2205.SHFE`、`hc2210.SHFE`、`rb2210.SHFE`、`FG601.CZCE`、`AP210.CZCE`、`ru2605.SHFE`、`jm2209.DCE`。结论：这不是 Stage825 过滤漏掉现成数据，而是真实合约分钟源缺失；补 exact contract/date 并重画图谱前，不允许继续写 `PDEG-v0`、产品方向阈值、R 倍数、OR 或重试次数规则。
- Stage028 已完成：做 Stage852 Stage851 后路线复盘与分钟K覆盖缺口审计，决策 `stage852_route_review_no_new_rule_until_minute_coverage_or_new_first_principle`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage852_stage851_route_review.py`，只读取 Stage825/849/851 既有输出，不改策略、不接引擎、不连接 CTP、不调用下单。Stage825 全周期 `341` 笔 closed lots 中入场日分钟K覆盖 `227` 笔，覆盖率 `66.5689%`，缺口 `114` 笔，缺口绝对 PnL 影响 `14,948,615`，缺口中含 `8` 笔 big winner。Stage849 压力段关键日期 `19` 个，仅覆盖 `7` 个，覆盖率 `36.8421%`；`fg_short_20220524_0602` 与多个 `fu_long` episode 完全缺分钟K。Stage851 PDEG-v0 仍是命中压力但过宽：entry flag rate `47.4320%`、closed flag rate `50.5405%`、flagged big-winner PnL `24,065,430`。结论：不写新规则、不接引擎；继续价值只在补 `fu/ru/hc/rb/jm/FG` 等高影响缺口分钟K并重画图谱，否则暂停持仓后 product-direction survival 分支。
- Stage027 已完成：做 Stage851 PDEG-v0 只读代理反事实审计，决策 `stage851_pdeg_proxy_catches_pressure_but_too_broad_no_engine`。新增脚本 `examples/portfolio_backtesting/analyze_qmt_roll_stage851_stage850_pdeg_proxy_audit.py`，只读取 Stage847 C9 的 `entry_risk/trades/curve/closed_lots` 与 Stage849 pressure paired lots，不改策略、不接引擎、不连接 CTP、不调用下单。PDEG-v0 proxy 能命中 `7/8` 个 pressure pairs，flagged pair PnL delta `-289,040`，unflagged pair PnL delta `-97,920`，说明预算冻结形状确实碰到压力机制；但全样本触发过宽：entry_risk `331` 行中 flagged `157` 行，触发率 `47.4320%`；closed lots `373` 笔、matched `370` 笔、flagged `187` 笔，flag rate `50.5405%`；flagged closed-lot PnL `+27,580,024.6`，覆盖 `17` 个 big winner 和 `+24,065,430` big-winner PnL。结论：PDEG-v0 当前形状不是精准生存线，而是机械降风险，不能接真实引擎；不得用新增小数阈值、品种名、年份、方向或 `1.35x` 手数比救参。若继续本研究线，只能重新寻找更本质的实时价格路径证据或更完整分钟K覆盖。
- Stage026 已完成：写入 Stage850 产品方向敞口生存线规则设计草案，决策 `stage850_product_direction_exposure_guard_design_only`。本阶段不接引擎、不跑回测、不连接 CTP、不调用下单；外部调研继续支持“止损”和“仓位/保证金风险管理”应分层处理。直接否决的形状包括产品名/年份/方向黑名单、`0.4R/0.6R` 或 OR/重试次数救参、复刻 C5 broker10 `>100%` forced deleverage、复刻 C6 top3/direction share 阈值、生硬 cooldown、以及用 C4/A 路径或 `1.3549` 手数比做 live 输入。唯一保留的低自由度候选是 `PDEG-v0 drawdown budget freeze`：账户离开权益高水位后，只对当前或下单后将成为最大保证金贡献的产品方向 key 冻结风险/保证金预算；新开/加仓/重试/换月重开不得超过该 key 最近 flat 时记录的预算权益，broker10 超过既有 `100%` 硬生存线时也只允许减“预算外增量”，不直接砍正常趋势底仓。它仍只是设计草案，不进入官方候选、不触发 A/B；下一步只能做 Stage027 只读反事实，检验它能否事前命中 Stage849 的 `8/8` 同路径更大仓且不大面积误伤右尾，否则停止持仓后生存线分支。
- Stage025 已完成：做 Stage849 C9/C4 压力段逐日与分钟K只读复盘。结论 `stage849_pressure_path_forensics_no_rule_yet`：沿 Stage024 预声明的 `fu.SHFE long`、`AP.CZCE long`、`FG.CZCE short`、`fu.SHFE short` 压力段，共 `7` 个 episode、`8` 对 paired lots；`8/8` 都是 C4/C9 同合约、同入场/退出日期、同价格、同退出原因，但 C9 手数更大。C9-C4 paired 合计手数 `+817`、风险金额 `+449,837.4`、PnL `-386,960`；C9/C4 手数比中位数 `1.3549`，区间 `1.2920 -> 1.3696`。`FG.CZCE short` episode 显示 C9 exposure proxy `16,921,200` vs C4 `12,425,400`，broker10 峰值 C9 `103.1305%` vs C4 `94.9333%`；`fu_short_20220622_0629` C9 `415` 手 vs C4 `303` 手。分钟K关键日期 `19` 个，覆盖 `7` 个，覆盖率 `36.84%`；`AP.CZCE long` 和 `fu.SHFE short` 有完整视觉证据，显示同价位路径下 C9 只是更大仓；`fu.SHFE long` 与 `FG.CZCE short` 多数缺分钟K，不能作为完整分钟级规则证明。结论：问题已从入场日 stop/retry 小参数转向持仓后账户/规模状态；不写新引擎、不触发 A/B。
- Stage024 已完成：做 Stage848 C9/C4 在 `2022-03-09 -> 2022-06-29` 峰谷窗口的只读归因和分钟K视觉复盘。结论 `stage848_c9_peak_trough_forensics_no_rule_yet`：C9 窗口 peak equity `10,205,981.8`、trough equity `4,772,131.8`、窗口跌幅 `-53.2418%`、window cum net PnL `-4,794,830`、交易 `38`、滑点 `162,200`、max broker10 `103.1305%`；C4 分别为 `7,777,212.8`、`3,827,167.8`、`-50.7900%`、`-3,494,105`、交易 `34`、滑点 `112,130`、max broker10 `96.9170%`。C9-C4 峰值权益高 `+2,428,769`，谷底只高 `+944,964`，窗口跌幅恶化 `-2.4518pp`，window cum net PnL 多亏 `-1,300,725`，max broker10 高 `+6.2135pp`。窗口内 C9 stop/retry 只有 `3` 个，同入口 PnL 约 `-526,000`，不足以单独解释日级差额。产品方向归因显示负向主要为 `fu.SHFE long -300,250`、`AP.CZCE long -261,650`、`jm.DCE short -80,940`、`SM.CZCE long -47,710`，正向抵消为 `sp.SHFE long +244,020`、`au.SHFE long +124,960`、`MA.CZCE long +72,050`。压力日显示 C9 在 `FG.CZCE short`、`fu.SHFE long/short` 等单产品方向压力簇里名义敞口更大，例如 `2022-05-27` broker10 C9 `103.1305%` vs C4 `94.9333%`，`2022-06-29` 同一 `fu2209.SHFE short` C9 `415` 手 vs C4 `303` 手。K线 atlas 显示 SM 为下午止损无重入，MA 多空为开盘1分钟先止损、假收复后再失败。结论：C9 弱点不是 stop/retry 事件单独造成，而是进攻增强后在持仓压力段放大同方向敞口和权益分母回撤；不生成 C10 小参数变体。
- Stage023 已完成：验证 Stage847 C9 `C4 + 0.5R 实时止损 + 原入场价重回后允许一次重试` 冻结真实组合引擎。结论 `stage847_c9_not_promoted_stop_retry_fullpath_failed`：C9 期末权益 `37,395,131.2`、总收益 `12365.0437%`、最大回撤 `-53.2418%`、Sharpe `1.4910`、滑点 `2,610,040`、交易 `730`、胜率 `53.3156%`、broker10 峰值 `109.9858%`。相对 C4，C9 多赚 `+6,871,220.4`、Sharpe `+0.0392`、broker10 峰值从 `115.4012%` 降到 `109.9858%`，但最大回撤从 C4 `-50.7900%` 恶化到 `-53.2418%`，恶化 `2.4518pp`；交易从 `677` 增至 `730`，滑点从 `2,079,430` 增至 `2,610,040`。C9 stop/retry 事件 `72` 个，`flat_no_reentry=44`、`open_after_reentry=12`、`flat_retry_failed=16`，重回入场价 `28` 个、重试失败 `16` 个。K线图谱显示 `flat_no_reentry` 多为入场附近快速打穿 `0.5R` 且未有效收复，`open_after_reentry` 恢复并不总是顺畅，`flat_retry_failed` 多为假收复后再次震荡打止损。C9 证明 stop+retry 有进攻价值，但没有穿过 C4 回撤闸门；不进入官方候选、不触发 A/B、不做年度/成本压力扩展，停止 `0.5R/重试一次` 分支救参。
- Stage022 已完成：做 Stage846 Stage825 入场质量只读法证。结论 `stage846_stop_retry_proxy_positive_but_needs_real_engine`：Stage825 closed lots `341` 笔，入场日分钟K覆盖 `227` 笔。`target_first_05r` 是右尾核心，`125` 笔总 PnL `+37,712,095`，big winner PnL `+26,596,810`；`stop_first_unrecovered_close_bad` `44` 笔总 PnL `-12,322,385`，胜率 `11.36%`。P2 `0.5R` 实时止损后若重回原入场价允许一次重试的 gross proxy 为 `+5,749,762.1`，亏损修复 `+7,780,490.5`，赢家损伤 `-1,998,228.4`，big winner 损伤仅 `-383,080`；比 P1 no-retry 更少误杀右尾。P4 `60m 1R` 确认硬过滤 gross delta `-8,977,625`、big winner 损伤 `-15,693,510`，明确反证快速确认过滤。P3 OR15 简单拒单 gross 为正 `+6,511,760`，但与 Stage834 的 OR15 close/hold 交易语义反证冲突，不晋级。Stage846 不进入官方候选、不触发 A/B；下一步只允许冻结一个真实分钟引擎 C9 验证 P2，不扫 R倍数/分钟窗/重试次数。
- Stage021 已完成：做 Stage845 C8 释放资金复用压力簇归因。结论 `stage845_reuse_cluster_evidence_mixed_no_rule`：20日窗口 `43` 个事件中 `33` 个有增量复用，增量行 `89`，增量风险 `+2,445,213.4`，增量 PnL `+3,361,304.0`。但增量风险 vs broker10 delta 的 Spearman 仅 `0.0809`、线性相关 `0.0167`，top direction share vs broker10 delta 相关 `0.0650`，证据弱。`broker_delta_top_quartile` 增量 PnL `+872,618.6`，`drawdown_delta_worst_quartile` 增量 PnL `+2,065,243.6`，入场日 `broker_80_100` 与 `broker_ge100` 桶也为正；反而 `broker_lt30` 桶为负 `-301,292.6`。复用压力簇不足以形成冷却/闸门规则，不进入引擎、不进入候选、不触发 A/B；下一步转入入场质量只读法证。
- Stage020 已完成：做 Stage844 C8 释放资金与风险压力只读归因。结论 `stage844_c8_diagnostic_reuse_positive_but_pressure_worse`：C8 相对 C4 期末权益仍高 `+2,528,195.6`，但 direct structural lots `43` 笔直接 PnL 差为 `-3,168,065.0`，其中 `18` 笔修亏损 `+1,344,365.0`，`25` 笔误杀赢家/加亏 `-4,512,430.0`。20日 nearest reuse 反而为正：增量 C8 暴露 `89` 行、增量风险 `+2,445,213.4`、增量 PnL `+3,361,304.0`，反证 blanket cooldown。真正失败机制是路径压力：20日窗口最大 broker10 差 `+43.8475pp`，最差 drawdown 差 `-19.0226pp`，`6` 个事件窗口内 C8 broker10 超过 `100%`；C8 broker10 峰值 `135.6309%` vs C4 `115.4012%`。停止 S3 救参，下一步只允许研究低自由度复用纪律或入场质量控制。
- Stage019 已完成：做 Stage843 C8 `C4 + S3_two_stop_side_closes_before_reclaim` 冻结真实组合引擎。结论 `stage843_c8_not_promoted_s3_structural_break_fullpath_failed`：C8 期末权益 `33,052,106.4`，高于 C4 `2,528,195.6`；但最大回撤 `-51.4922%`，相对 C4 `-50.7900%` 恶化 `0.7023pp`；Sharpe `1.3872`，低于 C4 `1.4519`；broker10 峰值 `135.6309%`，显著高于 C4 `115.4012%`。C8 structural events `43`、closed structural lots `43`、C2 events `51`、cap events `25`、cap reduced volume `511`。C8 不进入年度起点压力、不进入成本压力、不进入官方候选、不触发 A/B；停止 S3 结构破坏退出路线。
- Stage018 已完成：做 Stage842 止损后结构破坏 taxonomy。结论 `stage842_s3_positive_gross_but_stage841_negative_not_promoted_engine_watch`：Stage825 全量 `341` 笔 closed lots 中入场日分钟K覆盖 `227` 笔，`0.5R` 逆向触发 `101` 笔，触发后仍有恢复形态 `62` 笔。最佳只读形状 S3 `two_stop_side_closes_before_reclaim` 触发 `88` 笔，全量 gross delta `+4,103,675`，其中亏损修复 `+9,865,085`、赢家误伤 `-5,726,410`；Stage841 可匹配事件 `24/25`，S3 在该子集触发 `19` 笔，触发 `killed_c4_winner` `6` 笔、`saved_c4_loser` `8` 笔，子集 delta `-3,643,210`。结论为正线索但不晋级，不进入官方候选，不触发 A/B。
- Stage017 已完成：做 Stage841 C7 fail-fast 事件误伤法证。结论 `stage841_diagnostic_only_c7_failfast_hurts_by_killing_recoverable_entries`：C7 fail-fast `25` 个事件全部匹配到 C4 同入口 lots；匹配事件中 C4 总 PnL `+1,417,025.0`，C7 总 PnL `-2,059,431.3`，C7 相对 C4 净损 `-3,476,456.3`。其中 `killed_c4_winner` `10` 个造成 `-4,413,255.2`，大于 `saved_c4_loser` `10` 个带来的 `+1,084,639.7`；止损后当日重新站回入场 `6` 个，到达 `+0.5R` `3` 个，到达 `+1R` `2` 个。分钟K atlas 显示 C7 常把趋势初期可恢复抖动当成错误入场，解释了 Stage840 的真实引擎失败。
- Stage016 已完成：验证 Stage840 C7 `C4 + 120m 0.5R fail-fast no-retry` 真实组合引擎。结论 `stage840_c7_not_promoted_stop_failfast_timewindow_route`：C7 触发 fail-fast `25` 次，C2 事件 `52` 次，cap 事件 `32` 次；C7 期末权益 `26,118,143.3`、总收益 `8606.0478%`、最大回撤 `-52.6280%`、Sharpe `1.3351`、总滑点 `1,993,300`、交易 `682`、胜率 `52.7928%`、broker10 峰值 `132.7826%`。相对 C4，C7 少赚 `4,405,767.5`，最大回撤恶化 `1.8380pp`，Sharpe 降 `0.1168`，broker10 峰值恶化 `17.3814pp`。Stage839 的 H3 lot-level gross 线索穿不过真实资金联动，停止 fail-fast 时间窗路线。
- Stage015 已完成：做 Stage839 C2 未覆盖失败交易分钟K法证。结论 `stage839_uncovered_failure_no_single_clean_rule_yet`：Stage819 baseline `341` 笔 closed lots，总 PnL `28,171,880`；亏损 `179` 笔、亏损合计 `-32,489,020`。C2 `stop_first` 可覆盖亏损 `43` 笔、`-14,784,145`；未覆盖亏损仍有 `136` 笔、`-17,704,875`，其中有分钟证据 `70` 笔、`-11,725,485`，缺分钟 `66` 笔、`-5,979,390`。H1 `target_first` 后保本保护 gross delta `-1,000,420`，否决；H2 `neither + 入场日收盘逆向退出` gross delta `+737,350`，误伤赢家 `-3,694,495`，暂不进引擎；H3 `120m 0.5R fail-fast no-retry` gross delta `+3,461,542.4`，但误伤赢家 `-5,032,679.2`，且真正未覆盖亏损只覆盖 `13` 笔，只能作为一次冻结真实引擎验证线索。
- Stage014 已完成：验证 Stage838 C6 `C4 + 持仓后 concentration-aware survival` 压力起点。结论 `stage838_c6_cluster_survival_not_enough`：C6 对 A 收益胜 `4/4`，但回撤胜仅 `1/4`；对 C4 回撤胜 `3/4`，但收益中位差 `-24.1263pp`。A broker100 失败 `0/4`，C4 `4/4`，C6 仍为 `4/4`；C6 最大 broker10 `124.9520%`，DD50 失败仍 `3/4`。cluster 事件 `7` 次、减仓 `173` 手，但主要打在 `2021-02-19/2022-03-29` 的 CF long 簇，没有命中关键 `2022-07` 黑色/燃油 short 压力簇。C6 不进入年度全样本，不进入正式候选，不进入官方 A/B。
- Stage013 已完成：做 Stage837 C4 持仓后全路径压力法证。结论 `stage837_holding_pressure_cluster_rule_shape_supported`：broker 锚点 `8` 个，top3 产品方向簇高集中率 `100%`，short 方向集中率 `87.5%`，equity denominator 正贡献比例 `50%`。`2022-07` broker100 多数来自黑色/燃油 short 集群，top3 share 约 `78.9%` 至 `81.6%`、short share `100%`；`2019-01 2021-12-22` 是 long 集群例外，top3 share `100%`。但关键 `2022-07` 压力合约分钟K基本缺失，不能宣称分钟级实时止损已被证明。
- Stage012 已完成：做 Stage836 止损后释放资金再使用归因。主口径为 `nearest-stop`，每个 C-vs-A 增量开仓只归因给最近的前序日内止损；辅助口径为允许重叠的 event-window。结果反证 blanket cooldown：C2 10日 incremental C exposure PnL `+3,532,434.4`，C4 10日 incremental C exposure PnL `+2,267,950.0`。C2 `C_only` 为负 `-594,960.0`，但 `C_larger` 为强正 `+4,127,394.4`；C4 `C_larger` 也为强正 `+2,420,390.0`，且 C4 10日总 risk delta 为 `-1,308,353.2`，说明 broker cap 已压缩总体暴露。决策 `stage836_reuse_incremental_positive_no_blanket_cooldown`，不做止损后全局冷却或同品种冷却。
- Stage011 已完成：对 Stage827 C2 与 Stage830 C4 的日内止损触发事件做只读事件级归因和分钟K图谱。C2 `51` 个事件 closed 全匹配、baseline 匹配 `49`，直接贡献 `+5,347,448.4`；C4 `51` 个事件 closed 全匹配、baseline 匹配 `48`，直接贡献 `+6,871,695.8`。`stop_first` 桶 `91` 个匹配事件贡献 `+12,179,364.2`，说明 C2 止损本身大多是在修失败单；但 2020 年为负、`ru.SHFE` 为负，且完整 C2/C4 路径仍有 DD/broker 尾部风险。决策 `stage835_c2_direct_events_positive_but_path_risk_unresolved`，不推广为新策略。
- Stage010 已完成：验证“入场日 OR15 确认/假突破规避”只读 lot-level overlay。C6 `OR15 close confirm + retry2` covered-lot 净差 `-3,200,100`；C7 `OR15 hold5 confirm + retry2` covered-lot 净差 `-2,463,770`。二者虽然能过滤 `stop_first` 左尾（C6 `+10,369,355`，C7 `+11,302,320`），但严重伤害 `target_first` 快速走顺右尾（C6 `-14,395,710`，C7 `-14,591,315`），决策 `stage834_or15_confirmation_not_promoted`。OR15 形状不进入真实组合引擎 A/C。
- Stage009 已完成：在 C4 基础上叠加持仓后 `forced_margin_deleverage` 生存线，固定 `trigger=100%/target=100%/broker_multiplier=1.65/priority=largest_margin`，只跑 Stage832 压力起点。C5 收益仍强，4个起点对 A 收益胜 `4/4`，但 broker100 失败仍为 `4/4`，max broker10 从 C4 最高 `115.40%` 恶化到 C5 `125.53%`；DD50 失败仍 `3/4`，且 `2019-01` 回撤从 C4 `-50.7898%` 恶化到 C5 `-59.5303%`。forced 事件 `11` 次、关闭 `109` 手，主要集中 `CF.CZCE short`，未命中 Stage832 的 `2022-07` 黑色/燃油压力簇。决策 `stage833_c5_stress_survival_not_enough`，C5 不进入全年度验证。
- Stage008 已完成：对 Stage831 中 C4 的 broker100/DD50 压力起点做只读归因。压力起点为 `2018-01/2019-01/2020-01/2021-01`；C4 在四个起点收益和 Sharpe 仍强于 A，但 broker100 天数分别为 `2/3/2/2`，DD50 天数为 `13/13/13/0`，max broker10 到 `115.40%/104.98%/114.47%/108.12%`。入口 cap 事件显示开仓前投影最高 `1.2375 -> 1.2713`，开仓后均压到约 `1.0`，说明入口 cap 按设计生效；真正压力来自持仓后盯市、权益分母塌缩和黑色/燃油短仓集群保证金分子。决策 `read_only_forensics_no_promotion`。
- Stage001 已完成：建立 Stage819 2018起点全周期 closed lots、分钟K覆盖率、逐笔入场日分钟特征、规则候选分桶和全量图谱。
- Stage002 已完成：冻结 C1 `30分钟0.5R fail-fast + 最多2次重试` 与 C2 `1R止损先于1R确认则退出`，做 Stage819 候选内部 lot-level minute overlay A/C。
- Stage003 已完成：将 C2 放入隔离 subclass + 自定义 engine 组合路径验证；A 精确复现 Stage819，C2 触发 51 次。
- Stage004 已完成：只读归因 C2 回撤恶化机制；C2 直接止损事件在 2022 年合计正贡献 `+605,911`，但释放资金后同一批机会的手数和风险预算被放大，2022-03-09 至 2022-06-29 窗口 C-A 净损益差 `-774,050`，最大保证金/权益差 `+45.7703pp`。
- Stage005 已完成：做 C2 + A opened volume cap 反事实。C3 期末权益 `29,771,186.8`，高于 A `3,448,456.8`，但最大回撤 `-59.23%` 仍比 A `-54.75%` 差；相对裸 C2，回撤从 `-62.77%` 修到 `-59.23%`，证明账户层预算方向有效但不充分。
- Stage006 已完成：做 C2 + broker10 保证金/权益 `100%` 入口闸门。C4 期末权益 `30,523,910.8`，高于 A `4,201,180.8`；最大回撤 `-50.79%`，好于 A `-54.75%` 且显著好于裸 C2 `-62.77%`；Sharpe `1.452`，总滑点 `2,079,430`，交易次数 `677`，胜率 `53.63%`。
- Stage007 已完成：冻结 Stage006 参数做年度起点稳健性。成熟起点 `2018-01` 到 `2025-01` 共 `8` 个，C4 收益胜 `8/8`、Sharpe 胜 `8/8`、回撤胜 `5/8`、收益+回撤双胜 `5/8`；但 DD50 失败 `3` vs A `1`，broker100 失败 `4` vs A `0`，决策 `stage831_c4_not_robust_enough`。
- 当前结论：C2 裸规则不晋级；C4 也不能晋级。C4 证明日内止损释放资金有进攻价值，但 `2019/2020/2021` 起点回撤和 broker10 路径恶化，说明入口保证金闸门不是完整生存线。
- 当前结论补充：Stage027 否决 PDEG-v0 当前形状。它命中压力段但也命中更多右尾，说明 Stage819/C9 的右尾和压力段共享“账户回撤中大产品方向敞口”状态；单靠账户/产品方向预算变量无法区分好坏。继续用阈值缩小触发面会变成事后补丁。
- 当前结论补充：Stage026 没有把 Stage025 的压力品种、日期或 `1.3549` 手数比写成规则，而是把后续空间收窄为一个可被反证的预算冻结设计。若 Stage027 只读反事实不能证明 PDEG-v0 在不新增阈值的情况下命中关键同路径更大仓，并且不大面积误伤右尾，则应停止持仓后 product-direction survival 分支。
- 当前结论补充：Stage025 把 Stage024 的机制判断进一步压实。C9 在压力段不是因为换了交易时点而失败，而是 `8/8` paired lots 同路径下系统性更大仓，手数比中位数 `1.3549`；这意味着后续候选不能再围绕入场日 `R` 倍数和重试次数，而应先设计一个不含品种名/年份/小数扫描的 product-direction exposure guard 草案。分钟K覆盖只有 `36.84%`，所以还不能宣称分钟级持仓生存线已被视觉证明。
- 当前结论补充：Stage024 解释了 Stage023/C9 的主要失败机制。C9 窗口内 stop/retry 事件本身不足以单独解释 `-1,300,725` 的日级净 PnL 差；更核心的是 C9 因前期高权益和重试路径保留更大进攻性，在 `fu/AP/FG/jm/SM` 等单产品方向压力段承受更大手数、broker10 和相对自身高水位的权益回撤。下一步不能做 `0.4/0.6R`、重试次数、开盘分钟窗、单品种/方向过滤这类补丁；若继续，只能抽象成低自由度的持仓后账户状态/产品方向压力生存线。
- 当前结论补充：Stage023 已经把 Stage846 P2 的 stop+retry proxy 落到真实组合引擎。结果说明“错了先退、重回入场价再试一次”确实能提高右尾和 Sharpe，但它不是生存线：相对 C4 回撤恶化，且 2022 峰谷仍是主要失败路径。停止沿 `0.5R/重试次数/分钟窗` 救参；若继续，应回到持仓后权益分母脆弱、产品方向簇集中和 broker10 压力的实时治理，而不是继续做入场日 stop/retry 小变体。
- 当前结论补充：Stage022 显示，分钟级入场质量里最有机制价值的不是更严确认，而是“先止损、可重试”。`0.5R` 先反向且当天无法收复的样本是清晰左尾；但 `stop_first_recovered` 和 `target_first_05r` 证明 no-retry 或快速确认会杀可恢复右尾。下一步只能做一个冻结 C9 真实引擎验证 P2 语义；不得继续扫 OR、R倍数、确认分钟或重试次数。
- 当前结论补充：Stage021 反证“释放资金后按 broker/drawdown/压力簇直接冷却”。高 broker 桶和 broker delta top quartile 中新增仓仍贡献正收益，且增量风险与 broker10 恶化相关性很弱。不要继续扫 broker bucket、cooldown days、同方向/跨品种风险占比阈值；下一步回到分钟级入场质量，寻找不误伤右尾的实时规则。
- 当前结论补充：Stage020 解释了 Stage019 的“多赚但更危险”。C8 的 S3 直接退出不是正贡献，净损 `-3,168,065.0`，因为误杀右尾大于修复左尾；但释放资金后的 20日增量复用 PnL 为正 `+3,361,304.0`，所以也不能做全局冷却。真正要研究的是释放资金后是否堆到同一压力簇、同一方向簇，或在权益分母脆弱时推高 broker10。下一步不得沿 S3 连续根数/OR/R倍数救参。
- 当前结论补充：Stage019 反证 S3 结构破坏退出真实接入。Stage018 的只读 gross `+4,103,675` 在真实资金联动下转化为“多赚但更危险”：C8 回撤、Sharpe、broker10 全部弱于 C4。问题不是连续根数需要微调，而是日内退出改变了后续资金再使用和持仓压力路径。停止 S3 分支，不做 `2/3/4` 根、OR长度、R倍数、品种、方向或年份救参。
- 当前结论补充：Stage018 显示“连续两根止损侧收盘”比固定时间 fail-fast 更有结构含义，能修复一批 `no_same_day_recovery` 左尾；但它仍误杀 `18` 个赢家，且在 Stage841 C7 失败事件子集为负。不能把全量 gross `+4,103,675` 当成策略成立。若继续，只允许一个冻结真实引擎 C8 做反证；不得扫描连续根数、OR长度、R倍数、品种、方向或年份。
- 当前结论补充：Stage017 解释了 Stage016/Stage840 的失败机制。C7 不是因为事件匹配错误而失败，而是固定 `120m 0.5R` fail-fast 的语义太粗：它确实救下部分 C4 亏损，但更大规模误杀可恢复的趋势右尾，尤其 OI/lh/sp/fu 等事件。下一步不能继续救 `120m/0.5R/retry`，只能转向“止损后结构破坏”这类低自由度、可实时判定的只读 taxonomy。
- 当前结论补充：Stage016 反证 `120m 0.5R fail-fast no-retry`。问题不是 `120m` 或 `0.5R` 需要继续微调，而是初期小逆向经常会误伤后续右尾、改变 C4 的复利路径，并把 broker10 压力推到更差的后续路径。停止 fail-fast 时间窗、R 倍数和重试次数扫描。
- 当前结论补充：Stage015 显示 C2 未覆盖左尾没有单一干净形状。`target_first` 桶虽然包含后续亏损，但总 PnL `36,237,740`、赢家 PnL `39,262,010`，不能做简单保本/追踪保护；`neither` 桶总 PnL仍为正，不能全砍。唯一可继续的 H3 `120m 0.5R fail-fast` 也只是一次冻结引擎验证线索，不允许继续扫时间窗或 R 倍数。
- 当前结论补充：Stage014 反证 concentration-aware holding survival 作为 C4 的补丁。问题不是 `trigger/target/broker_multiplier/top3_share/direction_share` 需要继续微调，而是 runtime 生存线会过早改变 long 集群路径，却没有命中后续 exact broker10 的关键 short 压力簇，且会让 broker100 与 `2019-01` 回撤更差。停止 C4 survival branch 的阈值救援。
- 当前结论补充：Stage013 支持一个账户/持仓层的 broad rule shape：当 broker10 压力与 top3 产品方向簇集中、方向集中同时出现时，组合进入持仓后集中度风险状态。但这不是产品过滤或年份过滤证据，也不是分钟级出场已成立的证据；DD50 仍多是权益路径问题，broker100 才是持仓保证金/集中度问题。
- 当前结论补充：Stage012 反证“止损后冻结资金/同品种冷却”的粗规则。止损后新增/放大 C 暴露在 10 日窗口总体正贡献，负贡献也多为跨品种、跨多日，不是同品种马上复仇开仓。下一步应转向持仓后保证金集中、产品簇集中和权益分母压力，而不是继续研究 cooldown。
- 当前结论补充：Stage011 显示 C2/C4 的直接止损事件总体是正贡献，问题不在“1R 止损本身完全错误”，而在止损后释放的保证金和风险预算如何被组合重新使用。下一步禁止继续扫止损倍数、年份、品种或日内时段过滤，应转向释放资金再使用纪律。
- 当前结论补充：Stage058 已把 Stage057/881 的强代理落成真实组合引擎，并反证“同手数 `+0.5R` 加仓”作为可推广策略。C16 期末权益大幅高于 C9，但最大回撤恶化到 `-61.6881%`，Sharpe 低于 C9，max broker10 达 `203.4450%`，这不是可执行候选。pyramiding 方向的第一性结论是：右尾增厚真实存在，但账户层生存约束先于收益；不得继续扫 `0.25R/0.75R/1R`、加仓比例、止损位置、品种、方向或年份救 C16。
- 当前结论补充：Stage059 已把 Stage058 失败后的“账户级右尾参与预算”落成固定 1 手 sleeve 真实引擎，并继续反证推广价值。C17 比 C9 多赚 `1,046,670.05`、最大回撤改善 `1.4687pp`，说明小额 sleeve 确实能保留部分右尾；但 Sharpe 低于 C9，max broker10 从 `114.3987%` 升至 `127.4316%`，说明哪怕最小正整数参与也会重新制造保证金峰值。pyramiding/sleeve 分支停止，不再扫 `2手/3手`、progress R、止损位置、品种、方向或年份。
- 当前结论补充：Stage060 解释了 Stage059 的 broker10 恶化来源。C17 top10 broker10 峰值 `10/10` 是 `exposure_numerator_expansion`，不是权益分母压缩；`2020-10-15` exposure effect `+42.5172pp`，`2020-10-16` `+38.4969pp`，后续峰值日期即使总 delta 被更高权益分母抵消，分子扩张仍持续存在。结论是 sleeve/pyramiding 不是参数没调好，而是 right-tail participation 直接增加账户压力；后续若继续，只能先做不依赖品种名/日期的持仓层压力状态只读定义，不能再写新的 sleeve 入场变体。
- 当前结论补充：Stage061 将 Stage060 的“持仓层压力状态”做成全路径只读分桶，结果反证了直接交易化。C4/C9/C17 在 `broker10>=80% + top1产品方向>=35% + top3 share>=0.70` 下的 pressure state 后续 20 日中位收益均为正，C17 pressure 的 negative next20 share 仅 `8%`。这说明压力状态是定位标签，不是退出/减仓规则；后续不能扫 pressure 阈值救参，只能在 pressure state 内继续找真正的分钟级失败结构。
- 当前结论补充：Stage057 找到一个与此前退出/过滤路线不同的右尾增厚强线索。固定 `+0.5R` progress 后同手数加仓、原始入场价止损的只读代理 delta 为 `+34,513,422.1`，但它是上限代理，不含保证金、资金联动和后续逐日止损。Stage058 已完成唯一允许的真实引擎验证并不推广。
- 当前结论补充：Stage056 反证“禁止夜盘重试/禁止跨时段重试”的直觉。C9 的跨时段重试只有 `9` 个，但原始 matched PnL 为 `+1,138,795.0`；同连续时段重试代理会净损 `-1,533,365.0`，主要来自误伤 `sp2205` 等恢复右尾。时段边界是有价值的复盘标签，但不是可推广交易规则。
- 当前结论补充：Stage008 显示 DD50 与 broker100 不是同一个问题。`2022-06` DD50 很多日期 C4 已空仓或低保证金，是相对自身高水位的权益路径回撤；`2022-07-07` broker100 是持仓后黑色/燃油短仓集群的保证金压力，部分起点由权益分母塌缩主导，部分由 C4 更大持仓保证金分子主导。下一步只能研究 full-path holding margin survival 或释放资金再使用纪律。
- 当前结论补充：Stage009 反证简单持仓后强制减仓生存线。原因不是“100% 阈值需要微调”，而是 runtime margin survival 未命中 exact broker10 压力簇，且改变路径后会释放新风险。停止沿 `trigger/target/broker_multiplier` 小数扫描救 C4。
- 当前结论补充：A opened volume cap 是反事实归因，不是 live-feasible 规则；不能把 C3 包装成正式候选。
- 当前结论补充：C4 仍不是官方正式候选替代。Stage006 full-path max broker10 margin/equity 到 `115.40%`；Stage007 跨起点显示 C4 broker100 失败 `4` 次而 A 为 `0`，且 DD50 失败更多。不能因收益和 Sharpe 胜出就忽略尾部路径风险。
- C1 也改善收益和回撤，但新增 `58` 次交易、滑点增加 `199,920`，且重试组为负贡献；暂不作为主线。
- 主要限制：Stage867 已反证 Stage866 高热二次失败代理的直接实时化；Stage868 反证了 C9 重试成交语义小变体；Stage869 反证了 `retry_failed` 后续同产品方向 cooldown；Stage870 又反证了 progress-confirm recovery；Stage871 进一步证明 C13 的 broker10 恶化主要是权益分母被前序右尾削弱，不是当下持仓分子扩大；Stage872 反证固定止盈；Stage873 又把 `+2R 后锁 +1R` 利润保护从乐观上限落到真实引擎后反证；Stage874 进一步反证了 C9 `retry_failed` 后机械增加同日第二次重试；Stage875 又反证了“重试后当天无 `+0.5R progress` 就 EOD 平仓”的直觉；Stage876 反证了按固定 OR15/1xOR 做入场追价过滤；Stage877 已做路线覆盖审计，确认同一组分钟K派生特征继续救参价值低；Stage878 找到早段价格/OI参与度这个新信息维度，但 Stage879 将最窄 early OI-down no-progress 真实接入后已反证；Stage882/883 又连续反证同手数 pyramiding 与固定 1 手 sleeve。高热本身不是错误，真实盘不能提前知道 reclaim 后是否会二次失败；收盘确认没有减少重试/二次失败数量；二次失败后的后续同产品方向 entry 仍贡献大右尾，但这不等于同日继续二次重试有效；重试后当天未马上展开也不是错误充分条件，`OI201.CZCE` 等样本仍能贡献右尾；开盘区间外追价确实能标记部分左尾，但也会误伤 beyond-edge 延展赢家，不能作为错误充分条件；早段价格/OI参与度能解释右尾集中，但早段逆向、OI下降或非 `favorable_price_oi_up` 都不是错误充分条件；更严格的 progress 恢复确认虽然降回撤，但牺牲收益/Sharpe 且推高 broker10；固定止盈和利润锁定都会砍掉 C9 的核心右尾，削弱权益分母；顺势加仓/小 sleeve 虽能增加收益或降低回撤，但会重新推高 broker10。当前不允许继续做 C10 锁的小变体、cooldown 天数、R 小数、重试次数、账户热度阈值扫描、close-confirm 窗口、progress R、recovery stop、固定止盈、利润锁定阈值扫描、二次/三次重试、等待分钟数、重试后无进展 EOD 退出、progress 阈值、OR 分钟数、OR width 倍数、追价阈值、OI 小数阈值、成交量阈值、early OI 退出变体、pyramiding/sleeve 手数或 R 倍数、峰值日期/品种/方向黑名单，也不允许把 `non-progress`、high-heat、reclaim、retry_failed、open_after_reentry、beyond_edge、early_oi_state 粗暴屏蔽。C9 虽在 Stage861 全量分钟K口径下改善 C4 收益/回撤/Sharpe，但 max broker10 仍更高，不能直接接 A/B 或候选推广。若继续本线，应转账户/持仓层生存问题，或寻找新的低自由度外生信息源。
- Stage004 进一步限制：窗口内最大负贡献是 `fu.SHFE long`，但全样本最差 C2-vs-A 相对回撤差出现在 2020-10/11；不得按 2022、单品种、方向或 R 倍数做补丁式救参。

## 后续规划

- Stage001：只读逐笔法证与分钟K图谱。
- Stage002：冻结 1-2 个低自由度日内规则候选，做分钟级可执行语义设计，不接正式版。
- Stage003：优先把 C2 写进完整组合引擎开关，在 Stage819 候选上做真实 A/C 回测，检验收益、回撤、滑点、交易次数、资金联动和保证金路径。
- Stage004：已完成，不调参，确认 C2 回撤恶化主因是释放资金后的同机会规模/风险预算放大，不是 C2 直接止损事件本身。
- Stage005：已完成，C2 + A opened volume cap 缓和回撤但不可实盘，且仍未修回 A 的回撤水平。
- Stage006：已完成，broker10 `100%` 入口闸门是 live-feasible 账户层规则，不引用 A 路径、不引用未来结果；全周期优于 A 的收益和回撤，但 full-path broker10 仍超过 100%，暂不升级为正式候选。
- Stage007：已完成，冻结参数年度起点显示收益/Sharpe 稳定增强但 DD50 和 broker100 尾部失败更多，C4 不晋级。
- Stage008：已完成 C4 broker100/DD50 超限日归因。结论是入口 cap 工作正常，但持仓后盯市、权益分母和产品集中暴露仍会重新生成风险；禁止扫描 `1R`、保证金阈值、broker multiplier、冷却天数、品种过滤或年份过滤。
- Stage009：已完成 C4 + forced margin survival 压力起点验证，C5 不晋级，不进入全年度起点。禁止继续扫 `forced trigger/target/broker multiplier`。
- Stage010：已完成 OR15 入场确认/假突破规避只读验证。结论为不推广，不进入真实引擎；停止 OR 长度、hold bars、重试次数和 OR 反侧止损扫描。
- Stage011：已完成 C2/C4 真实止损事件级法证。结论为直接事件正贡献但路径风险未解，不进入正式候选。
- Stage012：已完成止损后释放资金再使用归因。结论为 10日 incremental C exposure 总体正贡献，不进入 cooldown 规则。
- Stage013：已完成持仓后全路径压力法证。结论是 broker100 压力存在产品方向簇集中形状，但 DD50 与 broker100 不同源，且分钟K覆盖不足，不进入正式候选。
- Stage014：已完成冻结低自由度 concentration-aware holding pressure engine 试验。结论为 C6 不晋级；停止 C4 持仓后 survival 阈值扫描。
- Stage015：已完成 C2 未覆盖失败交易分钟K法证。结论为未覆盖左尾没有单一干净规则；H1 否决，H2 暂停，H3 仅保留一次冻结真实引擎验证线索。
- Stage016：已完成 C7 `C4 + 120m 0.5R fail-fast no-retry` 真实引擎验证，结论为不晋级并停止 fail-fast 时间窗路线。
- Stage017：已完成 C7 fail-fast 事件误伤法证。结论为 C7 失败主要来自误杀可恢复趋势右尾，不进入真实引擎、不进入官方候选、不进入 A/B。
- Stage018：已完成“止损后结构破坏”只读 taxonomy。S3 `two_stop_side_closes_before_reclaim` 全量只读 gross 为正，但 Stage841 子集为负且误伤右尾，不晋级。
- Stage019：已完成 C8 真实组合引擎。结论为 C8 多赚但回撤、Sharpe、broker10 均弱于 C4，停止 S3 结构破坏退出路线；因未过全周期 C4 闸门，不再展开年度起点压力和成本压力。
- Stage020：已完成 C8 释放资金与 broker10/回撤压力归因。结论为 S3 直接退出负贡献、后续复用正贡献、路径压力恶化；停止 S3 救参。
- Stage021：已完成 C8 释放资金复用压力簇归因。结论为证据混杂，不支持冷却/压力簇闸门；停止 broker bucket、cooldown days、方向/品种占比阈值救参。
- Stage022：已完成入场质量只读法证。结论为 P2 `0.5R 实时止损 + 重回原入场价允许一次重试` 是正线索但不是策略；P4 快速确认反证，P3 OR15 暂不晋级。
- Stage023：已完成 C9 `C4 + 0.5R 实时止损 + 原入场价重回后允许一次重试` 真实组合引擎。结论为 C9 增收、Sharpe 更高、broker10 峰值下降，但相对 C4 最大回撤恶化 `2.4518pp`，不晋级；停止 stop/retry 分支救参。
- Stage024：已完成 C9/C4 在 `2022-03-09 -> 2022-06-29` 峰谷窗口的只读归因和视觉复盘。结论为 C9 弱窗口不是 stop/retry 事件单独造成，而是进攻增强后在同一产品方向压力段放大名义敞口、broker10 和权益分母回撤；不生成 C10 参数变体。
- Stage025：已完成 Stage849 只读归因。结论为 `8/8` 压力 paired lots 同路径但 C9 更大仓，数据上支持“规模/持仓状态问题”而不是“入场时点问题”；分钟K覆盖 `36.84%`，视觉证据只在 `AP long` 和 `fu short` 上较完整。
- Stage026：已完成，只写规则设计草案，不接引擎。保留 `PDEG-v0 drawdown budget freeze` 作为唯一后续只读反事实候选；不进入官方候选、不触发 A/B。
- Stage027：已完成 PDEG-v0 只读反事实审计。结论为能命中 `7/8` pressure pairs，但触发 `47.4320%` entry rows 和 `50.5405%` matched closed lots，覆盖 `17` 个 big winner，不接引擎。
- Stage028：已完成路线复盘，不写新规则。结论是当前研究线仍有条件继续，但继续价值只在补完整分钟K覆盖和视觉证据；不能继续救 `PDEG-v0` 或写新的产品方向阈值。
- Stage029：已完成分钟K数据缺口补齐审计。结论是 `126/126` gap requests 的 exact contract/date 在现有分钟源中不可用；这是补数问题，不是现成数据过滤问题。
- Stage030：已完成 Stage854 补数清单与本地 raw 预检。结论是 `126` 个缺口中 `29` 个可由本地 raw 先合并恢复，`97` 个仍需按下载批次补 exact contract/date；本阶段不写规则。
- Stage031：已完成 Stage855 本地 raw 导入与覆盖重算。结论是 Stage825 覆盖从 `227/341` 提升到 `251/341`，Stage849 压力 key dates 从 `7/19` 提升到 `12/19`；仍不写规则。
- Stage032：已完成 TqSdk `DataDownloader` 下载尝试；`84/84` 个批次全部因历史数据下载权限不足失败，新增覆盖为 `0`。结论是数据权限阻断，不是策略证据，不能重复无效下载或直接写新规则。
- Stage033：已完成 Stage855 新增覆盖分钟K视觉图谱。结论是新增 `24` 笔 entry-day 和 `5` 个压力 key date 的视觉证据有复盘价值，但继续反证简单 fail-fast/no-retry，不生成新规则。
- Stage034：已完成覆盖偏差审计。结论是 covered subset 存在严重系统偏差：2018/2019 完全缺分钟K，未覆盖 `90/341` 中含 `6` 个 big winner、`3/8` 个 pressure paired lots，pressure key dates 仍缺 `7/19`。暂停全周期规则设计；下一步优先补数/替代分钟源，短期无法补数时只能做 `2020+ covered subset` 局部假设审计。
- Stage035：已完成 Stage856 剩余分钟K缺口 TqBacktest 全量补抽。`91/91` batches 成功，覆盖 `97/97` 剩余请求，新增 `31,118` 根分钟K；本阶段只补 raw，不生成规则。下一步 Stage860 导入 Stage859 raw 并重算 Stage825/849 覆盖，确认是否达到 entry-day `341/341` 与 pressure key dates `19/19`。
- Stage036：已完成 Stage859 raw 导入与完整覆盖重算。Stage825 entry-day 已达 `341/341 = 100%`，Stage849 pressure key dates 已达 `19/19 = 100%`，combined patch minute bars `38,354`。下一步 Stage861 重算全量视觉特征并重画全量 entry-day / pressure K 线图谱；仍不写规则。
- Stage037：已完成 Stage861 完整分钟K视觉图谱。Stage825 entry-day `341/341` 和 Stage849 pressure key dates `19/19` 均有完整分钟K，生成 entry atlas `57` 页、pressure atlas `3` 页。下一步 Stage862 做全量证据归纳和规则假设审计；仍不写真实引擎、不触发 A/B。
- Stage038：已完成 Stage862 规则假设审计。结论为 `P2` stop/retry 仍是唯一可保留的 building block，但 Stage847/C9 已证明 standalone 回撤不过关；OR15、60m确认、S1-S4结构破坏全部反证。下一步只能做冻结 `H1 + risk-budget lock + second-failure discipline` 真实引擎，或者暂停本线等待新第一性特征。
- Stage039：已完成 Stage863 C10 预算锁真实引擎。结论为 C10 不晋级，因 `budget_lock_effective_events=0`，C10 与 C9 完全重合；但 C9 在 Stage861 全量分钟K口径下相对 C4 期末权益 `+4,621,339.6`、最大回撤改善 `+4.5602pp`、Sharpe 提升 `+0.0316`，同时 max broker10 恶化到 `114.3987%`。下一步只做 C9/C4 broker10 峰值归因，重点解释 `2020-11-23` 和压力窗口中后续 sizing 放大机制；不得继续微调 C10 锁。
- Stage040：已完成 Stage864 C9/C4 broker10 峰值只读归因。结论为 C9 峰值风险来自早期 stop/retry 改变权益/资金路径后，后续同信号 sizing 被放大；C10 同品种同方向加仓锁不能命中。下一步只允许做账户层 sizing brake 的只读反事实，检验其是否能在 broker10 峰值前实时触发且不误伤右尾；不得按峰值日期、品种或方向写规则。
- Stage041：已完成 Stage865 账户层 sizing brake 只读代理审计。结论为账户热度 brake 太钝：SBB0/SBB1/SBB2 都是净负代理，并且会削掉大赢家；`WATCH_single_add20` 覆盖过宽。下一步不再沿 `90/20/50` 阈值救参，转向高热入场后的分钟K快速失败、二次失败或价格确认失败纪律。
- Stage042：已完成 Stage866 高热入场分钟路径只读审计。结论为高热 `progress_first` 是右尾，不能机械缩手；`HH_NR1_retry_failed_only_no_retry` 是唯一可继续线索，但只有 `4` 笔，下一步只允许一次冻结规则草案或真实引擎验证，不得继续扩大触发面。
- Stage043：已完成 Stage867 高热 stop-first no-retry 真实引擎。结论为 C11 不晋级：降低 broker10 不是免费的，误伤 reclaim 后未二次失败路径，收益、回撤、Sharpe 均弱于 C9；停止高热 no-retry 分支，不再救 `90%/0.5R/时间窗/品种/方向/年份`。
- Stage044：已完成 Stage868 close-confirm next-open retry 真实引擎。结论为 C12 不晋级：它没有减少 C9 的重试数量或 retry failed 数量，只改变重试成交时间/价格；收益、回撤、Sharpe 均弱于 C9，broker10 改善极小。停止重试成交语义小变体，不再救 close-confirm 窗口、分钟数、R 或重试次数。
- Stage045：已完成 Stage869 C9 `retry_failed` 后续同产品方向损伤审计。结论为 cooldown 不晋级：阻断 `retry_failed` 后下一笔同产品同方向 entry 会少赚 `11,000,934.0`，且砍掉 `3` 个 big winner；停止 stop/retry 派生 cooldown，不再救 cooldown 天数、broker10 阈值、年份、品种或方向。
- Stage046：已完成 Stage870 progress-confirm recovery 真实引擎。结论为 C13 不晋级：最大回撤相对 C9 改善 `+3.8853pp`，但期末权益少 `3,969,007.3`、Sharpe 低 `0.0529`、max broker10 恶化 `+6.3750pp`；停止 progress-confirm recovery，不再救 progress R、recovery stop、确认窗口、品种、方向或年份。
- Stage047：已完成 Stage871 Stage870 broker10 路径归因。结论为 C13 broker10 恶化主要来自权益分母压缩：top10 C13 broker10 峰值中 `8/10` 为 `equity_denominator_compression`，只有 `1/10` 为分子扩张；不把 C13 转成仓位缩手引擎。下一步若继续，回到 C9，做右尾保留和独立生存线只读审计。
- Stage048：已完成 Stage872 C9 右尾保护与利润锁定代理审计。结论为固定止盈不晋级，`1R/2R/4R/8R` 全部净负且误杀全部 big winners；`+2R 后锁 +1R` 只有乐观上限价值，下一步至多做一次冻结真实引擎验证，不得继续扫锁盈阈值。
- Stage049：已完成 Stage873 C9 `+2R 后锁 +1R` 真实逐分钟利润锁定引擎。结论为 C14 不晋级：期末权益比 C9 少 `11,941,490.2`，最大回撤和 Sharpe 未超过 C9，max broker10 反而升至 `119.6549%`；停止利润锁定分支，不再扫 R、阈值、品种、方向或年份。
- Stage050：已完成 Stage874 C9 `retry_failed` 后同日第二次重试可行性审计。结论为二次重试不接真实引擎：`25` 笔 retry_failed 中只有 `9` 笔同日第二次 reclaim，其中 `7` 笔再次触发 stop，保守 same-day proxy `-157,190`；停止重试次数分支，不再扫 `2/3` 次、等待分钟数、R 小数、品种、方向或年份。
- Stage051：已完成 Stage875 C9 `open_after_reentry` 后日内进展/持仓审计。结论为未进展 EOD 退出不接引擎：`26` 笔 open_after_reentry 中 `12` 笔当天未触达 `+0.5R progress`，但实际后续 PnL `+1,783,150`，EOD proxy 仅 `+138,400`，会少赚 `1,644,750`；全体 open_after_reentry 若 EOD proxy 会少赚 `3,027,100`。停止进展阈值、等待窗口和 EOD 平仓分支。
- Stage052：已完成 Stage876 OR extension 追价审计。结论为 OR 追价过滤不接引擎：`P3_block_all_beyond_edge` skip proxy 表面 `+2,743,160`，但会砍 `-5,378,765` 赢家和 `-1,538,700` big winner，且 `2021`、`2020`、`2026` 为负；停止 OR 分钟数、OR width 倍数、追价阈值、品种、方向或年份扫描。
- Stage053：已完成 Stage877 分钟规则路线覆盖审计。结论为研究线已覆盖全周期逐笔 entry-day 数据分析与 K线视觉，但尚无可推广分钟规则：C9 是唯一正价值骨架，收益、回撤、Sharpe 优于 C4，但 max broker10 更高；后续派生小变体均已反证。下一步不再沿同一组分钟K派生特征救参，只能转账户/持仓层生存线或外生低自由度信息源。
- Stage054：已完成 Stage878 早段价格/OI参与度审计。结论为 OI/参与度是新信息维度但不直接进引擎：`favorable_price_oi_up` `122` 笔 PnL `+30,581,100`、big winner `22`，但早段逆向退出代理会砍 `-12,102,545` 赢家和 `-2,111,510` big winner。下一步只允许一次冻结真实引擎设计审计，且必须保护右尾，不得扫分钟窗口、OI/成交量阈值、品种、方向或年份。
- Stage055：已完成 Stage879 早段 OI guard 真实引擎。结论为 C15 不推广：虽然最大回撤从 C9 `-42.6313%` 改善到 `-35.4692%`，但期末权益降至 `41,990,763.2`、比 C9 少 `8,646,381.4`，Sharpe 降至 `1.5681`，max broker10 升至 `119.3842%`。早段 OI/参与度只保留为复盘解释标签；停止 early OI 退出变体，后续若继续应转账户/持仓层生存或新的低自由度外生信息源。
- Stage056：已完成 Stage880 C9 时段边界只读审计。结论为禁止跨时段/夜盘重试不推广：C9 跨时段重试 `9` 个全部为日盘止损后夜盘重试，原始 matched PnL `+1,138,795.0`；`P1_same_session_only_retry` 代理 delta `-1,533,365.0`，winner_cut `-1,816,900.0`，loser_saved 仅 `+283,535.0`。时段边界只保留为复盘标签；停止开盘/收盘分钟小窗口和 day/night 禁重试分支，后续若继续应转账户/持仓层生存或新的低自由度外生信息源。
- Stage057：已完成 Stage881 C9 `+0.5R` progress pyramiding 只读代理。结论为强正上限但不推广：`176/401` 笔触发，proxy delta `+34,513,422.1`，其中 `held_to_original_exit` `+39,780,918.6`，但 `entry_day_stop/same_bar_stop` 合计 `-5,267,496.5`，且 `2022/2026` 为负。本阶段已读取 `version-ab-experiment`，但因只是代理、不是真实组合回测，不触发 A/B、不写 `back_log.md`。下一步只允许一个冻结真实引擎：`C9 + 入场日先触达 +0.5R 后同手数加仓一次 + 新增仓原入场价止损`；不扫 progress R、加仓比例、止损位置、品种、方向或年份。
- Stage058：已完成 Stage882 C9 `+0.5R` progress pyramiding 真实组合引擎。结论为收益右尾极强但不推广：C16 期末权益 `134,885,396.7`，比 C9 多 `84,248,252.1`，但最大回撤 `-61.6881%`、Sharpe `1.5740`、max broker10 `203.4450%` 均不通过。停止本 pyramiding 分支，不进入滚动起点、成本压力、正式候选或 A/B；后续若继续，只能转账户级右尾参与预算/生存线，不能救 C16。
- Stage059：已完成 Stage883 C9 `+0.5R` progress 固定 1 手 sleeve 真实组合引擎。结论为 C17 有右尾贡献但不推广：期末权益比 C9 多 `1,046,670.05`、最大回撤改善 `1.4687pp`，但 Sharpe 低 `0.0088`，max broker10 恶化至 `127.4316%`。停止 pyramiding/sleeve 分支；后续不做 `2手/3手`、R 倍数、止损位置、品种、方向或年份救参。若继续本线，只能转到账户/持仓层生存问题或新的低自由度信息源。
- Stage060：已完成 Stage884 Stage883/C17 broker10 路径归因。结论为 C17 风险来自持仓保证金分子扩大：top10 broker10 峰值 `10/10` 为 exposure numerator expansion，`2020-10-15` C17 vs C9 broker10 delta `+49.0595pp` 中 exposure effect `+42.5172pp`。这进一步否定 sleeve/pyramiding 参数救援；若继续本线，只能做 holding-level pressure state 的只读抽象，且不得用峰值日期、品种或方向黑名单。
- Stage061：已完成 Stage885 持仓压力状态全路径只读审计。结论为 pressure state 是风险定位标签但不是交易规则：C17 pressure `25` 天、C17-only `9` 天，但 C17 pressure median next20 return `16.9511%`、negative next20 share `8.0000%`。不接引擎、不触发 A/B、不扫 pressure 阈值；若继续，只能在 pressure state 内做分钟级失败结构只读复盘。
- Stage062：已完成 Stage886 pressure-state 内分钟级失败结构审计。结论为高压下当日收弱也不是退出规则：`price_failure_shape` EOD exit proxy delta `-2,853,650`，winner cut `-3,353,030`，median next20 return `22.1098%`，negative next20 share `11.1111%`。停止 pressure-day close 弱势退出分支；若继续，只允许只读检查 Stage883 sleeve 触发前是否已有/即将进入组合压力状态，目标是阻断新增风险预算，而不是平掉既有右尾。
- Stage063：已完成 Stage887 sleeve 触发压力闸门审计。结论为前置 heat/pressure gate 也不推广：`G2/G3/G4` 阻断 `79` 次但 skip proxy `-114,051.15`，winner cut `-170,746.95` 大于 loser saved `56,695.80`；后来参与 same-product pressure 的 `11` 笔 sleeve 净赚 `+29,792.40`。停止 sleeve/pyramiding 压力 gate 救援，不扫 heat 阈值、产品方向、年份、分钟窗口或 sleeve 手数。下一步若继续本线，应做 Stage881-887 route closure，或回到 C9 本体寻找新的低自由度外生信息源/账户级非交易层生存线。
- Stage064：已完成 Stage888 pyramiding/sleeve 路线收束审计。结论为该分支关闭：Stage881/882 证明右尾参与真实，但 Stage882 max broker10 `203.4450%`、最大回撤 `-61.6881%`；Stage883 1 手 sleeve 仍 Sharpe 下降且 max broker10 `127.4316%`；Stage886/887 又反证 pressure exit 与 pressure gate。停止新增仓救参，不扫 progress R、手数、止损、heat、产品方向、年份或分钟窗口。后续若继续本线，应回到 C9 本体的新外生信息源或账户级非交易层生存线。
- Stage065：已完成 Stage889 C9 本体亏损分钟形态覆盖审计。结论为 C9 本体剩余分钟形态没有足够强的新规则：覆盖亏损最多的 `adverse_first_05r` 与 `early60_adverse_any_oi` 同时覆盖大量赢家；所有主要可执行 proxy 为负，唯一正的 `neither +/-0.5R + close below entry` 仅 `+72,650` 且正年份 `1`、负年份 `4`。停止在 `0.5R/1R/OR15/first60/EOD close` 上救参；后续若继续本线，应换新的低自由度外生信息源或账户级非交易层生存线。
- Stage066：已完成 Stage890 first60 price/OI/volume 三元参与度审计。结论为成交量维度没有把 C9 first60 逆向左尾稳定分离出来：非 missing 成交量逆向桶触发极少，最好的 `V4` 仅 `1` 笔、delta `+90,000`，`V5` 仅 `3` 笔、delta `+10,770.9`，而 `volume_expanded` 相关代理为负或无触发。成交量三元参与度只保留为解释标签，不接真实引擎；停止成交量倍数、first60 长度、OI 阈值、品种、方向或年份救参。下一步若继续本线，应做路线级收束，或转账户级非交易层生存线/更强低自由度外生信息源。
- Stage067：已完成 Stage891 日内分钟规则路线级证据收束。结论为全周期逐笔数据分析与 K线视觉证据已经完整，C9 是唯一正价值骨架，但 Stage878-890 后续外生参与度、时段边界、顺势加仓、sleeve、C9亏损形态、成交量三元参与度均未产出可推广分钟规则；推广/A-B 触发条件未满足。停止当前分钟K小变体路线；后续若继续，只能先设计新的低自由度外生信息源或账户级非交易层生存线，并证明仍服务于本研究目标。
- Stage068：已完成 Stage892 全市场 first60 广度可行性审计。结论为当前 Stage861 full minute bars 是目标交易完整事件面板，不是全市场连续分钟面板；`market_breadth_missing_lot_pct=100.0%`，固定市场广度代理全部无触发，不能用当前数据源写市场广度规则。若未来继续该方向，必须先构建全市场连续分钟面板并验证 entry_date 覆盖数；否则应转账户级非交易层生存线或暂停。
- Stage069：已完成 Stage893 全市场分钟面板本地可行性审计。结论为本地已有分钟 CSV 仍不足以支撑全市场 first60 广度规则：Stage861 事件面板同日可用合约数中位 `3`、最大 `10`，downloaded minute union 中位 `14`、最大 `23`，但只有 `12/299=4.0134%` 的 C9 entry_date 达到 `20` 合约要求。不得降低最少合约数或按覆盖好的年份/品种救参；若继续市场广度，先建独立连续分钟面板，否则转账户级非交易层生存线或暂停。
- Stage070：已完成 Stage894 目标覆盖与剩余路线审计。结论为本线已经满足研究线隔离、全周期逐笔数据分析、K线视觉复盘、规则类非AI约束；C9 已测试实时止损+重试骨架但不推广；新增分钟规则仍 `not_proven`，市场广度仍 `data_missing`，因此目标保持 active，不声明完成。后续禁止继续 `first60/OR/R/OI/volume/session` 小变体救参；若继续，只有先建全市场连续分钟面板这一条高对齐路线，或另开账户级非交易层生存线。
- Stage073：已完成 Stage898 C9 回测可信度独立审计。独立 agent 指出的 Stage896/897 使用旧分钟源问题成立，已将滚动脚本改为 Stage861 full-minute 口径并重跑。修正后 Stage896 C9 完整 3 年窗口 `7/7` 正收益，median return `479.2947%`，worst DD `-56.6137%`，peak broker10 `106.6510%`；Stage897 完整 1 年窗口 `13/15` 正收益，负窗口为 `2018-01=-4.9218%`、`2018-06=-3.6223%`。Stage898 复算 `225` 项指标失败 `0`，但 P0 fail `1`：Stage863 C9 `384` 笔开仓中 `8` 笔 entry-day 缺 Stage861 full minute bars，导致日内规则会跳过这些样本。当前不能宣称 C9 回测“无任何偏差”；下一步必须补齐这 8 个 exact contract/date 分钟K并重跑 Stage863/896/897/898。
- Stage074：已完成 Stage899 C9 逐月起点首次转正等待期审计。从 `2018-01` 到 `2026-05` 每月一个独立起点，统一跑到 `2026-05-29`。全部 `101` 个起点中 `99` 个曾经转正，未转正为 `2026-04`（已过 `58` 日历日，期末 `-6.86%`）和 `2026-05`（极短 empty result）；成熟样本（至少 1 年）`89/89` 曾经转正且期末均为正。最长首次转正等待为 `2018-03` 起点：`158` 日历日 / `108` 交易日 / `5.191` 月，首次转正日 `2018-08-06`；等待期中位数 `14` 日历日，P90 `72.4` 日历日。该统计只回答冷启动等待期，不消除 Stage898 的 8 笔分钟K缺口，补数后仍需重跑。
