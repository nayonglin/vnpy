# Stage143：2026-06-29 实盘今晚/明早无信号复核

记录时间：2026-06-29 20:32 CST

所属研究线：futures_trend_stage819_intraday_rules

## 背景

用户要求复核当前实盘今晚和明早是否没有交易信号，并逐品种解释没有信号的原因，同时确认信号链路是否有 bug。

当前工作模式：day。

当前实盘版本：`official_live_stage847_c9_15w_stage819_05r_stop_retry_once` / `Stage847-C9-15w`。

## 本次操作

新增参数：无。

修改参数：无。

删除参数：无。

代码修改：无。

运行内容：

1. Stage922 目标日解析：确认最新完成交易日为 `2026-06-29`，本地 shadow 和数据均已到 `2026-06-29`。
2. Stage173 数据更新：`--mapping-start 2026-06-01 --bar-start 2026-06-29 --end 2026-06-29`，19 个合约全部保存，`failed_count=0`、`empty_count=0`、`max_saved_date=2026-06-29`。
3. Stage901 官方实盘 shadow：`analysis_start=2026-06-16`、`analysis_end=2026-06-29`。
4. Stage914/907 生产只读检查：正式 `ctp_live.local.env` 与 `vnpy_ctp/api/libs` runtime preflight 通过；显式只读刷新成功，`readonly_refresh_completed_snapshot_ready`，订单 API `0`。
5. Stage906 fresh 对账：`reconcile_aligned`，broker/shadow 都是 `rb2610.SHFE short 11`，活动委托 `0`。
6. Stage260/905 执行闸门：`signal_count=0`、`pending_order_count=0`、`execution_candidate_count=0`、`intent_count=0`、`ready_count=0`。
7. Stage929 `email-policy never` 手动报告：信号明细为空；目标日仅有一个底层候选 `ru2609.SHFE short_case2`，不是最终交易信号。

## 结果

今晚/明早日线级可提交交易信号：无。

开仓信号：无。

平仓信号：无。

当前策略持仓：`rb2610.SHFE short 11`。

实盘/影子盘对账：已在 20:25 fresh broker 快照口径确认 aligned。

订单 API 调用：`send_order_api_called_count=0`、`cancel_order_api_called_count=0`。

AI 池：Stage935 preflight 显示月度 AI 池已是当前口径，`expected_eval_date=current_eval_date=2026-05-29`，当前池为 `SA.CZCE / si.GFEX / MA.CZCE / AP.CZCE / FG.CZCE / OI.CZCE / rb.SHFE / jm.DCE / fu.SHFE`。

目标日底层候选但非最终交易：

- `ru2609.SHFE`：`short_case2`，`candidate_status=skipped`，`skip_reason=short_signal_rejected`。同时它不在当前 AI Top 池中，AI 排名 `12/18`，分数 `0.492266`，Top8 门槛 `0.575260`，主要拖分项为 120 日波动排名末位和最近 20 日无开仓。

## 逐品种归因

- `SA.CZCE / SA609.CZCE`：空头排列存在，但 2026-06-29 没有新的可开仓触发；无平仓对象。
- `si.GFEX / si2609.GFEX`：空头排列存在，但没有新的可开仓触发；无平仓对象。
- `MA.CZCE / MA609.CZCE`：空头排列存在，但没有新的可开仓触发；无平仓对象。
- `AP.CZCE / AP610.CZCE`：MA5/10/20/40 未形成完整多头或空头排列；无平仓对象。
- `FG.CZCE / FG609.CZCE`：空头排列存在，但没有新的可开仓触发；此前实盘实时止损已同步进 shadow，当前无 FG 持仓，因此也没有平仓信号。
- `OI.CZCE / OI609.CZCE`：MA5/10/20/40 未形成完整多头或空头排列；无平仓对象。
- `rb.SHFE / rb2610.SHFE`：已有空单 11 手，但无新增开仓；日线退出未触发。2026-06-29 收盘 `3095`，前两日高点 `3121`，MA20 前值 `3150.8`，且仍为空头排列，所以 `short_prev2day_stop`、`short_ma_stop`、`short_alignment_break` 均未触发。
- `jm.DCE / jm2609.DCE`：MA5/10/20/40 未形成完整多头或空头排列；无平仓对象。
- `fu.SHFE / fu2609.SHFE`：未形成完整可用趋势触发；无平仓对象。

## Bug 复核

1. 不是 Stage659/Stage901 `signal_plan` 漏掉最终日 pending order：Stage901 `pending_orders` 为空，Stage260 执行候选也为 `0`。
2. 不是历史候选误发到今日邮件：Stage929 目标日 blocked candidate 只包含 `candidate_date=2026-06-29` 的 `ru2609.SHFE`，没有混入 6月18日/22日/23日旧候选。
3. 不是 AI 池旧接线：Stage935 preflight 通过，当前 AI 池 eval date 为 `2026-05-29`，和月更设计一致。
4. 不是 broker/shadow 持仓不一致导致信号被错挡：20:25 fresh broker 快照下 Stage906 为 `reconcile_aligned`。
5. Stage929 手动 plan-only 报告在 20:30 因只读快照年龄超过 300 秒显示 broker 快照不可用，这是报告口径的保守提示；本次独立 fresh Stage906 已确认 aligned，且 20:55 交易守护启动时仍会重新拉 fresh 快照。

## 回测结果

本阶段不是回测版本，不新增、修改或删除回测结果。

## 反过拟合判断

否。本次只是按固定实盘版本做日常信号和执行链路审计，没有调整参数、品种、方向、AI 排名或风控阈值。

## 继续价值判断

是。每天实盘前复核 Stage901/260/905/906/929 能防止漏单、历史候选误报和 broker/shadow 不一致导致的误判。

## 后续 TODO

1. 20:55 后检查 live-real session daemon 是否重新拉取 fresh broker/tick，并保持 `ready_count=0`。
2. 若夜盘/明早 rb 触发分钟级实时止损，必须用 Stage904/905/931 execution ledger 与 Stage901 live_stop_alignment 做三方对账。
