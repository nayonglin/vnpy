# Stage152 1:1 逻辑重建断点审计

- line_id：`futures_trend_stage819_intraday_rules`
- 当前模式：`day`
- 记录时间：`2026-06-30 23:04 CST`
- 工作区/分支：`/Users/bytedance/Desktop/person/vnpy` / `master`
- 阶段性质：只读审计；按用户要求停止 deleted-file/search 类找回，验证能否从旧代码与当前可用输入逻辑 1:1 重建
- 是否重要突破：否
- 是否触发A/B：否

## 外部调研与判断

- 参考资料：本轮按用户明确要求不再外部搜索、不做 `rm -rf` 后的文件残留找回；仅使用本仓旧记录、Git 旧提交 worktree、当前可用 CSV/JSON/数据库输入做确定性复跑与断点定位。
- 我的判断：1:1 重建在逻辑上成立的前提是旧的原始输入状态也能复原，包括 Stage53 源账本、主力映射、日线/分钟数据、候选/成交/position change、Stage149 执行代理 detail。当前证据显示这些输入状态尚未复原，不能用“特征公式固定”推导出“AI 池必然相同”。

## 本次变更

- 新增脚本：无
- 修改脚本：无
- 删除脚本：无
- 新增参数：无
- 修改参数：无
- 删除参数：无

## 回测/归因参数

- 数据区间：重点核对 `2020-01-02 -> 2026-04-30` Stage53/66/67 源链，以及 Stage819/847 下游旧记录
- 账户规模：Stage53 口径 `200,000`，Stage67 shadow 使用记录内口径，Stage847/C9 旧记录另行作为下游行为校验
- 成本口径：沿各脚本原始配置；不改滑点、不调参数
- 样本过滤：只读核对旧记录与当前/旧提交复跑结果
- 策略/归因口径：先找最早不一致断点，再决定是否进入 Stage66/67/75/78/819/847 下游重跑

## 结果

### Stage67 AI pool shadow：当前同名产物已经不是旧结果

| 口径 | baseline end | ai_top8 end | top8 相对 baseline | trades/slippage |
| --- | ---: | ---: | ---: | ---: |
| 旧 Stage67 记录 | `2,902,355` | `3,172,760` | `+270,405` | `357 / 133,260` |
| 当前同名 shadow | `2,742,710` | `2,509,540` | `-233,170` | `323 / 121,150` |

- 结论：同名 `qmt_roll_ai_product_pool_shadow_portfolio_*` 当前产物已经不能代表旧 Stage67。旧结果里 `ai_top8` 是正贡献，当前变成负贡献；这不是下游 C9 代码单独能解释的问题。

### Stage53 源账本：旧提交复跑仍不能贴上旧记录

| 口径 | end | return | max DD | Sharpe | slippage | trades |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 旧 Stage53 `volref30_current` | `2,683,135` | `1241.57%` | `-37.20%` | `0.9939` | `354,660` | `1173` |
| 旧提交 `6fbf08e` + 当前可用输入复跑 `volref30_current` | `2,303,485` | `1051.74%` | `-37.67%` | 约 `0.90` | `352,570` | `1166` |
| 旧 Stage53 `floor35` | `2,902,355` | `1351.18%` | `-36.99%` | `1.0225` | `349,080` | `1158` |
| 旧提交 `6fbf08e` + 当前可用输入复跑 `floor35` | `2,562,395` | `1181.1975%` | `-36.1015%` | `0.9472` | `347,740` | `1156` |

- 结论：旧提交代码本身不足以 1:1 复原旧 Stage53。`floor35` 的交易次数和滑点接近旧值，但权益仍差 `339,960`，更像是主力映射、日线数据库或某个输入快照不一致，而不是单纯策略代码差异。

### Stage149 执行代理输入：当前不是旧状态

- 当前 `qmt_roll_stage449_minute_session_rebuild_full_proxy_detail_*` 只有表头，数据行 `0`。
- 旧记录显示 Stage149 detail 旧状态有 `692` 行；当前 Stage501/502/506 seed map 会退化为 `close_map=0/open_map=0`。
- 结论：即便 AI 池上游修好，下游 Stage819/825/847 的成交代理也还没有回到旧状态。

## 输出文件

- report：无新增报告
- summary：本 stage 记录
- orders：无
- daily：无
- quality：无

## 结论

- 本阶段结论：当前还没有达到 1:1 重建。最早可证断点已经前移到 Stage53/66/67 AI 池源链，以及 Stage149 执行代理 detail。
- “特征固定”的正确含义：特征公式固定；但特征值依赖 Stage53 position_changes、entry snapshots、trades、主力合约映射、日线/分钟数据和训练样本窗口。只要这些输入状态不同，同一流程也会得出不同 AI 池。
- 是否进入下一步：是，但不能继续在 Stage847/C9 下游反复跑曲线；应先把 Stage53 与 Stage149 恢复到旧记录 checksum/指标附近。
- 下一步：
  - 先重建或定位旧 `tqsdk_main_contract_mapping_2020_2026_04.csv`、旧日线数据库状态、旧 pairwise/AI 输入快照，目标是复原旧 Stage53 `floor35=2,902,355/trades=1158/slippage=349,080`。
  - Stage53 过关后，重跑 Stage66 market predictions、Stage67 AI pool shadow、Stage68/75/78，再进入 Stage819/847。
  - 并行重建 Stage149 detail 旧状态，目标不是空 seed，而是旧记录约 `692` 行代理 detail。

## 过拟合反思

- 运行前判断：否
- 运行后判断：否
- 原因：本轮只做旧记录和旧提交的硬指标对比，不改策略参数、不按结果拟合曲线。真正需要避免的是手工指定 `rb/SM`、手调 AI 池或调阈值去贴旧权益，那会把重建变成过拟合。

## 继续价值反思

- 运行前判断：是
- 运行后判断：是，但价值在上游输入复原，不在下游重复回测
- 原因：已经定位到下游不一致之前的 Stage53/67 与 Stage149 输入问题；只要能恢复这些输入，后续链条才有 1:1 验证意义。若 Stage53/149 无法恢复，则必须承认只能做功能性重建，不能宣称字节级或行为级 1:1。

## 合入建议

- 是否更新本线 `LINE.md`：暂不更新；等待 Stage53/149 是否能进一步复原后统一整理
- 是否更新 `research/registry.md`：否
- 是否追加根目录 `memory.md/back_log.md`：否
