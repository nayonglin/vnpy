# Stage220 C9 实盘 0.5R 状态与单笔手数对齐实施计划

> 执行范围：仅本地代码、单元/集成测试和研究记录；不连接 CTP，不触发报单。

1. 在当前研究线新增定向测试，先证明旧实现存在以下失败：进展状态跨轮询丢失、同批 tick 不按时间判断、ledger 无法恢复策略状态、500/503 手被 20 手限制拦截。
2. 在 Stage904 复用 append-only execution ledger，新增 `c9_initial_progress_confirmed` 事件及精确键查询。
3. 将 Stage904 初始阶段的阈值判断改为对新鲜 tick 按 `_dt` 稳定排序逐条处理；同 tick 冲突时不利事件优先。
4. 状态仅保护“首次止损前”阶段，不改变强制退出、重进失败止损和最终止损。
5. 将 Phase D 的 `max_single_order_volume` 设为 `0`（无限），Stage905 仅在配置值大于 0 时执行本地手数上限检查。
6. 运行定向测试、相关既有测试和语法检查；随后做独立代码复核并写结果 stage。

## 完成标准

- 首次 +0.5R 进展可跨轮询和重启恢复。
- 批内进展先于不利触发时不平仓；不利先到或同 tick 冲突时平仓。
- 500/503 手不会因原 20 手软件上限阻断。
- 其他风控仍在；测试期间委托 API 调用数为 0。

