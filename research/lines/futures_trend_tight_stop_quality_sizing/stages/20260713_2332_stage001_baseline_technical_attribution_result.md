# Stage001 当前主策略基准与技术归因结果

- 时间：`2026-07-13 23:32 CST`
- line_id：`futures_trend_tight_stop_quality_sizing`
- 阶段：`Stage001`
- 是否重要突破：否
- 结论：基准与归因数据完整性通过；四个预声明紧止损规则全部不具备晋级资格。

## 当前版本与参数

- 版本：`official_live_stage847_c9_15w_stage819_05r_stop_retry_once`。
- 区间：`2020-01-01 -> 2026-06-30`；初始资金 `150,000`。
- 新增参数：无。
- 修改参数：无。
- 删除参数：无。
- AI：主策略原有月池保持原样；技术规则表不含 AI 字段。

## 新增回测结果

- 期末权益：`5,996,631`。
- 总收益：`3897.754%`。
- 最大回撤：`-55.370112%`。
- Sharpe：`1.396723`。
- 总滑点：`759,970`。
- 总交易次数：`641`，其中 `314 open + 327 close`。
- 胜率：非零日胜率见 summary；机会口径因规则不同不使用单一总胜率作为晋级指标。
- 最长水下：`438` 个交易日。

## 完整性审计

- `327` 个 closed lot 毛利 `6,606,601`，完全且仅一次归入 `288` 个独立 flat-entry 机会。
- `314 open = 288 flat + 16 stop retry + 10 rollover`；orphan 与 unclassified 均为 `0`。
- 日线净利 `5,846,631`，毛利减净利等于滑点 `759,970`；权益逐日累计最大残差 `2.33e-10`。
- T-1 核心技术特征覆盖 `287/288 = 99.6528%`；未来引用 `0`；唯一缺口 `lh2109.DCE` 只有 60 根 T-1 历史。
- 数据库以 SQLite `mode=ro` 读取，运行前后 SHA256 均为 `4ba0b0d97c62109cb1105c11b7244df077240fe211b61c61bde770a13f19eb9e`。

## 预声明规则结果

- `tight_directional_efficiency`：50 笔，三段 R 为 `445.697 / 17.079 / 20.897`，但 2022 占正年度 R `78.49% > 60%`，失败。
- `tight_range_position`：2 笔，发现与验证均为负，失败。
- `tight_ma_adx`：23 笔，样本不足且年度稳定性不足，失败。
- `tight_strong_close`：2 笔，验证与封存无事件，失败。
- 新增回测结果：上述 fresh baseline、归因表和三张图。
- 修改回测结果：首次独立审查发现旧 CSV 源、lineage 与 ATR 时点问题后，已切换当前只读 DB、补齐 retry/rollover lineage 并重跑；本文件记录的是修复后唯一有效结果。
- 删除回测结果：修复前的 85.77% 覆盖与不完整 closed-lot 归因作废，不作为证据。

## 独立 Agent Review

- reviewer：`Plato / 019f5c0a-446a-73e3-9865-e47f13e2d325`。
- 结论：完整性 `PASS`，P0 `0`、P1 `0`、P2 `3`，置信度 `99%`；资格 `FAIL`。
- P2-1：真实行情 ADX/DI 与 TA-Lib 有小幅 seed 偏差；替换 TA-Lib 后 `tight_ma_adx` 仍为同一 23 笔，不影响本次结论。后续若再次使用 ADX，必须先修复。
- P2-2：15 日 source 匹配未把 source ID、时差、数量差和候选数全部落盘；本次 298 次匹配的最早成交均唯一，不影响本次结果。以后复用 lineage 前补齐。
- P2-3：LINE 状态过期；本次同步修复文档。

## 反思与 TODO

- 运行后过拟合：四条预声明规则本身没有结果后扫参，但继续救阈值会过拟合，因此全部关闭。
- 继续价值：原四规则没有继续价值；基准、归因数据和“极小止损不是独立质量信号”的结论有继续价值。
- TODO：仅允许把 Stage001 单变量结构作为新的、明确标注事后形成的隔离假设；不得把它冒充原预声明规则晋级。

