# Stage003 较小止损中等实体风险转移预声明

- 时间：`2026-07-13 23:36 CST`
- line_id：`futures_trend_tight_stop_quality_sizing`
- 阶段：`Stage003`
- 性质：事后形成、隔离探索的当前主策略真实引擎 A/B
- 是否重要突破：否，尚未回测

## 证据状态

- 本假设不是 Stage001 四个预声明复合规则之一，不能声称为真正样本外，也不能直接晋升正式版。
- 规则形成后、任何真实引擎结果可见前一次性冻结；后续不得调阈值、倍率、年份或品种。
- 固定事件审计：45 笔、17 个产品、双方向、7 年；发现/验证/封存 R 分别 `293.267 / 21.825 / 103.974`，PnL 均为正，7/7 年 R 为正，单年最大正 R 占比 `51.78%`。

## 唯一规则

1. 特征只用开仓日前一根已完成日线 T-1。
2. `stop_atr14 <= 0.515281`，即 discovery 止损/ATR 的较小一半。
3. `0.312987012987013 < body_ratio <= 0.5525550867323019`，即 discovery 实体占比第二四分位；排除 doji 型极小实体和扩张型大实体。
4. 同时满足为 quality：正常风险预算 `1.25x`；其余正常 flat-entry 为 `0.75x`。
5. 恢复袖套/低风险冷启动豁免为 `1.0x`。
6. 不扫描倍率，不加品种、年份、方向和账户状态例外。

## A/C 与硬门

- 四锚点：`2020-01-01`、`2021-01-01`、`2022-01-01`、`2024-01-01`，统一至 `2026-06-30`。
- 每个锚点收益为正，C/A 收益保留 `>=70%`。
- 至少 `3/4` 锚点最大回撤改善 `>=3pp`，必须包含 2022；无锚点恶化超过 `1pp`。
- A/C 保持相同信号、AI 月池、止损/止盈、`0.5R` 当日重试、成本、broker10、保证金与组合风险门。
- 独立 agent 必须检查代码、结果、时点、数据、AI 边界、配置 diff、统计口径与置信度；影响结果的问题修复后重跑。

## 运行前反思

- 过拟合：是，高风险。规则来自当前全样本归因，四锚点只是不同冷启动，不是独立样本；即使通过也只能作为前瞻 shadow 候选。
- 继续价值：是。45 个事件跨品种、双方向和 7 年稳定，机制简洁，值得用真实引擎检验整数手数、成本和组合门后的实际路径。

