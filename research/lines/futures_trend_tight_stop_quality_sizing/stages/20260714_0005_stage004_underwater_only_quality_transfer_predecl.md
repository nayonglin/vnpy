# Stage004 水下才启用质量风险转移预声明

- 时间：`2026-07-14 00:05 CST`
- line_id：`futures_trend_tight_stop_quality_sizing`
- 阶段：`Stage004`
- 性质：Stage003 机制失败后的唯一新逻辑候选，尚未回测
- 是否重要突破：否

## Stage003 给出的机制证据

- 修复后 Stage003 使用正确 T-1、原阈值和 `1.25/0.75`，2020/2021 起点同时改善回撤并保留收益。
- 2024 起点收益保留只有约 `62.41%`，且回撤恶化约 `1.32pp`；年度分解显示 2025 盈利少约 `65,720`。
- 判断：全天候把普通机会降至 `0.75x` 会在盈利阶段持续裁掉有效趋势，不能穿越不同冷启动。

## 唯一冻结规则

1. 质量定义、T-1 时点、TA-Lib ATR、阈值保持 Stage003 完全不变：
   - `stop_atr14 <= 0.515281`；
   - `0.312987012987013 < body_ratio <= 0.5525550867323019`。
2. 账户状态严格因果：用信号日收盘后、下个交易日开仓前已知的 `portfolio_drawdown_pct`。
3. 当 `portfolio_drawdown_pct == 0`（处于自身权益前高）时，所有候选风险权重均为 `1.0`，与 A 完全一致。
4. 当 `portfolio_drawdown_pct > 0`（水下）时：quality `1.25x`，other `0.75x`。
5. 恢复袖套仍豁免为 `1.0x`；非 flat-entry 不变。
6. 不增加最小回撤阈值，不扫描权重，不使用品种、方向、年份或 AI 字段。

## 为什么不是救参

- Stage003 的 `1.25/0.75` 与质量阈值全部保留，没有改成更有利的小数。
- Stage004 新增的是二元账户状态边界：在前高不动，在水下才做风险转移；它直接针对 Stage003 已确认的“盈利阶段长期削减”机制，而不是选择某段历史收益最佳阈值。
- 本候选仍是当前样本启发，过拟合风险高；即使通过也只能进入前瞻 shadow，不能直接正式化。

## A/C 硬门

- 四锚点固定：2020-01、2021-01、2022-01、2024-01，统一到 `2026-06-30`。
- 每个锚点收益为正且 C/A 收益保留 `>=70%`。
- 至少 `3/4` 回撤改善 `>=3pp`，必须包含 2022；任何锚点不得恶化超过 `1pp`。
- 保存 A/C 全量 daily、trades、entry candidates、entry risk、trade events 和 stop retry；AI、配置、时点、权重与成交守恒全部 fail-close。
- 回测后拉新的独立 agent 做全面 review；影响结果的问题必须修复重跑。

## 运行前反思

- 过拟合：是，高风险。状态门由 Stage003 路径归因形成，虽然没有新数值阈值，仍然不是样本外。
- 继续价值：是。它保留 Stage003 在水下阶段的风险释放，同时从第一性原理上避免盈利阶段无差别降仓，机制简单且可证伪。

