# Stage005 水下高质量机会单边增仓预声明

- 时间：`2026-07-14 00:53 CST`
- line_id：`futures_trend_tight_stop_quality_sizing`
- 阶段：`Stage005`
- 性质：Stage004 失败后的机制拆解，尚未回测
- 是否重要突破：否

## 已知事实

- 严格分钟会话修复后的 Stage004 在四锚点均降低最大回撤，但 `2021-01` 收益保留为 `69.7749%`，低于预声明 `70%` 硬门。
- 不允许因为只差 `0.2251pp` 而放宽门槛或微调小数参数。
- Stage004 同时做了两件事：水下质量机会 `1.25x`，普通机会 `0.75x`。因此结果尚不能区分回撤改善来自质量增仓，还是主要来自广泛降风险。

## 唯一冻结规则

1. 继续使用 Stage003 的 T-1 纯行情质量定义：`stop_atr14 <= 0.515281` 且 `0.312987012987013 < body_ratio <= 0.5525550867323019`。
2. 当前权益处于自身前高时，所有 flat-entry 风险权重为 `1.0x`。
3. 当前权益水下时，仅 quality flat-entry 为 `1.25x`；other flat-entry 保持正式版 `1.0x`。
4. 恢复袖套、非 flat-entry 保持 `1.0x`。
5. 不新增 AI 特征，不扫描权重、阈值、品种、方向、年份或回撤区间。

## A/C 硬门

- 固定 `2020-01/2021-01/2022-01/2024-01 -> 2026-06-30` 四锚点。
- 每个锚点收益为正且 C/A 收益保留 `>=70%`。
- 至少 `3/4` 锚点回撤改善 `>=3pp`，必须包含 `2022-01`；任何锚点不得恶化超过 `1pp`。
- A/C 必须使用同一严格分钟 patch、同一正式 AI 月池、同一成本与 0.5R 当日止损重试逻辑。
- 完成后必须由新的独立 agent 审计；影响结果的问题必须修复并重跑。

## 运行前反思

- 过拟合：是，仍为高风险。质量阈值来自同一历史样本；本轮虽不调参，但机制选择受 Stage004 结果启发。
- 继续价值：是。它是正交的单边增仓消融，能验证“高质量紧止损机会本身是否贡献回撤改善”，而不是追逐 `69.7749%` 的小数差距。
