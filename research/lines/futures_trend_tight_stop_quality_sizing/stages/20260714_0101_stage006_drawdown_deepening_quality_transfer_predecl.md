# Stage006 回撤加深日质量风险转移预声明

- 时间：`2026-07-14 01:01 CST`
- line_id：`futures_trend_tight_stop_quality_sizing`
- 阶段：`Stage006`
- 性质：Stage004/005 消融后的账户状态逻辑候选，尚未回测
- 是否重要突破：否

## 机制证据

- Stage004 水下全程 `1.25/0.75` 明显改善四锚点回撤，但 `2021-01` 收益保留仅 `69.7749%`，说明恢复阶段长期降仓会损失有效趋势。
- Stage005 水下只做 quality `1.25x`、other `1.0x`，回撤在四锚点均无改善，说明不能依赖单边增仓控制尾部。
- 因此需要把风险转移限制在亏损仍扩张的时点，并在权益修复时解除，而不是调 `0.75/1.25` 的小数。

## 唯一冻结规则

1. 每个交易日 `_refresh_risk_state` 前的 `portfolio_drawdown_pct` 记为前一交易日回撤，刷新后记为当日信号时点回撤。
2. 仅当 flat-entry 且 `current_drawdown > prior_drawdown` 时启用 Stage003 风险转移：quality `1.25x`，other `0.75x`。
3. 回撤持平、收复或处于前高时，全部候选保持正式版 `1.0x`。
4. 质量阈值、T-1 特征、恢复袖套与非 flat-entry 规则保持 Stage003 不变。
5. 不设最小回撤阈值，不扫描增量、倍率、品种、方向、年份或窗口，不新增 AI 特征。

## 四锚点硬门

- 固定 `2020-01/2021-01/2022-01/2024-01 -> 2026-06-30`。
- 每个锚点收益为正且收益保留 `>=70%`。
- 至少 `3/4` 回撤改善 `>=3pp`，必须包含 `2022-01`；任何锚点不得恶化超过 `1pp`。
- 共享严格分钟 patch、正式 AI 月池、资本、成本、0.5R 开仓日止损与一次重试。
- 回测后由新的独立 agent 全面审计，影响结果的问题必须修复重跑。

## 运行前反思

- 过拟合：是，高风险。状态逻辑来自本样本上的 Stage004/005 结果；但它没有任何拟合数值，且机制可在前瞻期直接证伪。
- 继续价值：是。它回答“只在亏损扩张阶段降普通机会风险，能否兼顾收益修复和尾部控制”这一明确、可执行的问题。
