# Stage010 趋势证明-回踩-再确认紧止损归因结果

- line_id：`futures_trend_tight_stop_quality_sizing`
- 记录时间：`2026-07-14 17:10 CST`
- 当前模式：`research / day`
- 阶段性质：分钟级因果归因与路线终止审计，不改变策略、不做组合回测
- 是否重要突破：否；这是分钟紧止损方向的最终负向机制证据
- 是否触发 A/B：否；预声明硬门失败，真实引擎未运行
- 正式实盘影响：无；正式策略、AI 月池、CTP、邮件和 launchd 均未修改

## 本次版本变更

- 新增规则：原始成交后先达到 `+0.5R` 证明趋势，再回踩原计划入场位，收复后在下一根真实分钟 open 反事实执行；结构止损放在回踩 episode 不利极值外一历史 tick。
- 新增时序约束：证明 bar 不得同时作为回踩；至少完成 `30` 根分钟 bar；早于 30 根的收复直接标为 `early_reclaim`，不得等待到第 30 根伪造候选。
- 新增风险约束：micro stop 风险不超过原始实际风险的 `0.5`；同一分钟同时触及止损和目标时固定止损优先。
- 修改参数：无策略参数修改；历史 `lc.GFEX` tick 继续使用 Stage009 已修复的生效日规则。
- 删除参数：无。
- 新增 AI 特征：`0`；正式 AI 月池只作为母路径的共同输入。
- 新增输出：discovery 逐事件、候选、年度分解、状态汇总、后段 feature-only seal、图表和输入/输出 manifest。
- 修改/删除回测结果：无；本阶段没有策略组合回测。

## 数据与完整性结果

- Stage008 closed lots：`404`；聚合根开仓事件：`300`，PnL 与风险守恒，不一致组 `0`。
- 分段：discovery/validation/holdout = `171/78/51`。
- discovery 状态：candidate `20`、early_reclaim `13`、micro_stop_too_wide `2`、no_proof `19`、no_retest `32`、prior_half_r_stop `85`。
- 历史 tick：current metadata `297`；`lc.GFEX` 历史规则 `3`。
- 后段只冻结特征：`129` 行，SHA256 `9730ea413d597712e0ffccca413befafcd71fa26642947421eb8efcb07dc654f`；未计算或落盘 validation/holdout 的 micro first-touch 结果。
- input manifest：`19/19` 文件 bytes/SHA256 一致；output manifest：`10/10` 非自指输出一致。
- 本研究线测试：`87 passed`。

## 新增归因结果

| 指标 | 结果 |
| --- | ---: |
| 候选事件 | 20 |
| 品种 / 方向 / 年份 | 12 / 2 / 3 |
| 1R resolved / target / stop / no-touch | 19 / 10 / 9 / 1 |
| 1R target-first | 52.6316% |
| 1R Wilson 95% | 31.7078% - 72.6702% |
| 2R resolved / target / stop / no-touch | 18 / 6 / 12 / 2 |
| 2R target-first | 33.3333% |
| 2R Wilson 95% | 16.2788% - 56.2505% |
| 母策略事件总 PnL / 总 R / 中位 R | 1,066,015.4 / 59.4719 / 1.25836 |

年度 2R target-first：2020 `25.00%`、2021 `37.50%`、2022 `33.3333%`。2020 与 2022 不高于无成本 `-1R/+2R` 的 `33.3333%` 盈亏平衡点；2022 候选的母策略总 R 为 `-0.946162`。样本只有 `20`，也低于预声明的 `30` 个最低覆盖门。

## 预声明硬门

- 覆盖门失败：`20 < 30`；尽管覆盖 12 个品种、2 个方向、3 个年份，样本量不足。
- first-touch 门失败：总体 2R 命中率恰好等于 `1/3`，不满足严格 `>1/3`；Wilson 下界 `16.2788%` 也不高于 `1/3`。
- 年度稳定门失败：2020、2022 的 2R 点估计不高于 `1/3`。
- 母策略右尾门失败：2022 候选母策略总 R 为负。
- 因此 `stage011_real_engine_predecl_allowed = false`。没有组合候选，也没有真实引擎资金曲线；这是按合同停止，不是漏跑。

## 组合回测指标

Stage010 没有改变策略或运行组合候选，因此期末权益、总收益、最大回撤、Sharpe、总滑点、总交易次数和胜率均为 `N/A`。Stage008 正式母基准仅作不变参考：

- 期末权益：`5,167,871.60`
- 总收益：`3345.247733%`
- 最大回撤：`-65.350816%`
- Sharpe：`1.260107`
- 总滑点：`1,712,120`
- 总交易次数：`793`
- 事件胜率：`109/300 = 36.3333%`
- 手数胜率：`32.4257%`

## 独立复核

- reviewer：`Jason / 019f5fd8-7c0f-7f62-b213-7e465eb25d33`
- 终审：`P0/P1/P2/P3 = 0/0/3/1`；没有会改变 Stage010 数值或关闭结论的问题。
- 数值复算置信度：`99.7%`；数据/实现闭环置信度：`94%`；关闭路线结论置信度：`99.5%`。
- reviewer 确认 `stage010_attribution_closed = true`、`stage011_real_engine_predecl_allowed = false`。
- 非结果影响 P2：当前 manifest 对冻结原始数据库的传递闭包仍不完整；要彻底关闭需独立数据库快照和重建上游 Stage000/008，但当前已封存输出哈希一致，无需同结果重跑。
- 非结果影响 P2：后段 guard 能阻止 first-touch 结果，但 baseline PnL/R 仍在内存；seal 明确不包含这些列，也没有落盘后段 micro outcome。未来若复用工具，应收紧内存和 guard 边界。
- 非结果影响 P2：`gap_beyond_stop` 状态因 `next_open_lost_reclaim` 的判断顺序不可达；本次 300 个事件均不受影响。未来若复用状态机，应调整判断顺序并补测试。
- 非结果影响 P3：没有显式覆盖“恰好第 30 根”和“恰好 1/3”边界的正式单测；reviewer 临时复算确认第 30 根允许、第 29 根 early、风险恰好 0.5 允许、2R 恰好 1/3 失败。

## 决策

- `stage010_attribution_gate_failed_close_minute_tight_stop_branch`。
- 不修改分钟数、R 倍数、micro 风险比例、结构止损、品种、方向或年份救参。
- 不运行 Stage011 真实引擎，不生成候选收益曲线，不接 A/B、shadow 或正式版。
- 整条“分钟级高质量机会 + 极小止损加仓”方向关闭。若未来获得真正新的前瞻数据，只能另开研究线、重新预声明独立假设，不能把新实验写成 Stage010 的参数修补。

## 反思

- 过拟合：是，高。分钟形态自由度高，2020-2022 已被多次观察，2023-2026 也只是历史锁定段而非真正未来 OOS；继续换阈值会直接变成后验搜索。
- 继续价值：当前方向无继续价值。Stage009 的开盘推进和 Stage010 的证明-回踩-再确认均未显示足够覆盖、2R 正期望与跨年稳定性，真实引擎只会放大一条尚未成立的代理信号。

## TODO

1. 本线停止新增分钟紧止损实验；保留 Stage008 主策略基准与 Stage009/010 负向机制证据。
2. 不修复上述不影响本次结果的 P2/P3，除非未来有独立研究线确实复用该工具；复用时必须先处理并重新 review。
3. 后续若继续降低回撤，必须从新的、入场前可见且有跨周期机制解释的信息源出发，而不是继续扫描分钟 K 线阈值。
