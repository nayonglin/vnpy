# Stage041：历史盈亏尾部 HTML 双选择器

## 基本信息

- 时间：2026-08-17 19:12（Asia/Shanghai）
- line_id：`futures_trend_winner_trade_forensics`
- 是否重要突破：否；这是只读复盘图集的对照面扩展，不是策略突破。
- 决策：`readonly_profit_loss_tail_atlas_no_strategy_change`
- 正式版本：`official_live_stage847_c9_15w_stage819_05r_stop_retry_once`
- 数据有效末日：`2026-07-22`

## 本阶段改动

- 将原来只展示盈利右尾的 HTML 扩展为盈利、亏损双尾部逐笔复盘。
- 新增“盈利交易 / 亏损交易”选择器。
- 新增 R 排序选择器：
  - `R绝对值从大到小`：盈利侧先看最大正 R，亏损侧先看最负 R。
  - `R绝对值从小到大`：两侧都从靠近 0R 的入选尾部交易开始看。
- 仍先按 `open_trade_id` 聚合一次开仓的全部部分平仓，再在 episode 层筛选，避免拆 lot 重复计数。
- 盈利侧保持原口径：正 R 的前 20%，并补入 R 缺失时盈利额前 20%；亏损侧采用对称口径：负 R 最差 20%，并补入 R 缺失时亏损额最差 20%。
- 周 K、日 K、15 分钟 K、三层成交量、统一交易日横轴、开平仓标记、MA5/10/20/40 均保持不变。
- 增量补齐亏损 episode 所需的 15 分钟行情，并新增 `selected_profit_loss_episodes.csv`；`big_winners.csv` 继续保留兼容。

## 参数变化

- 新增策略参数：无。
- 修改策略参数：无。
- 删除策略参数：无。
- 可视化筛选阈值：盈利 episode 正 R 的 q80=`5.8346273292R`；亏损 episode 负 R 的 q20=`-2.3626967075R`。这些阈值只定义图集样本，不进入交易逻辑。

## 样本与行情结果

- 全部 closed lots：`418`；聚合后 episode：`402`。
- 盈利 episode：`158`；亏损 episode：`238`；持平 episode：`6`。
- 入选盈利 episode：`30`。
- 入选亏损 episode：`43`。
- 入选 episode 对应 closed lots：`78`。
- 15 分钟行情缓存：`46,013` 根；实际绘图：`27,693` 根。
- 15 分钟缺失交易日：`0`。
- 逐 episode-day 将 15 分钟 K 聚合回日 OHLCV，与页面日 K 比对：差异 `0`。
- 输出文件 SHA 清单逐项复算：差异 `0`。
- 浏览器交互复核：盈利/亏损切换、R 绝对值正序/倒序、年份/品种筛选、上一笔/下一笔均正常；亏损极端排序首笔为 `SH409.CZCE, R=-41.00`，靠近 0R 排序首笔为 `SH607.CZCE, R=-2.37`。

## 正式回测指标（未重跑、未改变）

- 初始资金：`150,000`
- 期末权益：`12,652,824.10`
- 总收益：`8,335.2161%`
- 最大回撤：`-56.2069%`
- Sharpe：`1.3410`
- 总滑点：`1,611,870`
- 总交易次数：`821`
- 非零日胜率：`52.4577%`
- closed-lot 盈利率：`173 / 418 = 41.3876%`

## 外部调研与判断

- 参考 Plotly 官方 function reference；页面继续使用 `Plotly.react` 在筛选或排序后更新同一张图，避免重建多份 DOM。
- 判断：只看赢家会产生幸存者偏差；用完全对称的亏损尾部作为反例，更适合识别“赢家独有结构”与“趋势交易共同结构”。

## 反过拟合反思

- 是否过拟合：否。本阶段没有修改策略、参数、信号、成本或回测结果，只扩展只读可视化样本。
- 风险：尾部图集仍是事后筛选，不能把图上共同形态直接转成规则；亏损尾部也不能据此倒推品种或年份禁入。

## 是否值得继续

- 当前扩展有价值：它补齐了赢家图集最重要的反例面，能减少视觉归因偏差。
- 页面功能到此应收口，不再继续做低价值 UI 打磨；后续若继续，只应把清晰候选转成事前可见、跨年份/跨品种的定量验证。
